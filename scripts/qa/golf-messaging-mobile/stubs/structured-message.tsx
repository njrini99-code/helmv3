// The four fixtures are text-only, so the product StructuredMessage branch is
// intentionally dormant. Keeping its server-heavy analytics graph out of this
// browser-only runner does not replace any rendered messaging UI.
export function StructuredMessage() {
  return null;
}
