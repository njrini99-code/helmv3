# `.helm` Archive Notice

This directory is historical context only. It is not the current task system.

Use these sources of truth instead:

- GitHub Issues: current work.
- GitHub Project: current priority.
- `memory/registry.yml`: feature routing.
- `AGENTS.md` and `CLAUDE.md`: agent rules.
- `docs/operations/`: current runbooks and incident history.
- `docs/audits/`: historical audits.

Do not reopen work just because it appears as open in old `.helm` files. Create or update a GitHub issue when current work needs tracking.
