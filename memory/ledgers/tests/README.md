# memory/ledgers/tests/

One file per feature: `<feature-id>.md` — when that feature's test
guarantees or contracts changed. Append-only; never rewrite a past entry,
correct it forward instead. See `memory/ledgers/README.md` for the full
ledger contract.
