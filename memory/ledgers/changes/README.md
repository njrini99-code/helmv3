# memory/ledgers/changes/

One file per feature: `<feature-id>.md` — what changed, why, and at what
SHA. Append-only; never rewrite a past entry, correct it forward instead.
See `memory/ledgers/README.md` for the full ledger contract.
