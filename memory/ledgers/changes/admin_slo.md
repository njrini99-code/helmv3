<!-- markdownlint-disable MD004 MD007 MD012 MD013 MD022 MD032 MD034 MD036 MD037 MD038 MD040 MD041 MD050 MD060 -->
# Admin SLO Center change ledger

## 2026-09-03 — New sub-capability: SLO/Error Budget Center (Bridge Control Plane Phase D)

- SHA: recorded on merge of `agent/controlplane-d`.
- New registry entry `admin_slo`, added alongside `admin_incidents`,
  `admin_reliability_collector`, `admin_selfheal` under the
  `admin_platform` split. New route `/admin/slo`
  (`src/app/admin/_components/admin-nav.ts` key `'O'`,
  `src/app/admin/_components/AdminShell.tsx` icon `Target`).
- New page (`src/app/admin/slo/page.tsx`) and services
  (`src/lib/admin/slo/**`): `error-budget.ts` (lives under
  `admin_reliability_collector`'s `src/lib/reliability/**`, not duplicated
  here), `golden-path-health.ts`, `silence-detection.ts`,
  `trace-funnels.ts`, `fetch.ts` (the `cache()`-memoised orchestration
  point), `golden-paths.generated.ts` (AUTOGENERATED —
  `scripts/knowledge/gen-golden-paths.mjs` from
  `memory/journeys/golden-paths.yml`, because `memory/**` is not in the
  Vercel function bundle).
- New declarative indices: `memory/invariants/registry.yml` (the two
  round-graph invariant checks, `scripts/knowledge/check-invariants.mjs`)
  and `memory/analytics/event-contracts.yml` (10 behavioral-event contracts,
  all `status: planned` — see that file's header for why: the natural
  wire-up point, `loginActionImpl` in `src/app/golf/actions/auth.ts`, is
  shared by coach and player logins and role is not resolved at the point
  the proven `DEMO_ENTER_EVENT` capture lives, so no live event was forced
  in under time pressure — `scripts/knowledge/check-event-contracts.mjs`).
- Extended (not replaced) `src/lib/admin/triage/invariant-lattice.ts`'s
  `buildInvariantLattice()` with an optional `roundGraphChecks` input,
  wired from `/admin/health`'s existing `HeartbeatAndInvariantsBody`.
