# Nightly Health Check — 2026-05-10

**Window:** 2026-05-09T04:06Z → 2026-05-10T04:06Z (last 24h)
**Project:** `nick-rinis-projects/helmv3` (`prj_qPgC4eErTUsaSmv40EiQMNuTpuEV`)

## 24h Totals

| Metric | Count |
|---|---|
| Total runtime errors (level=error/fatal) | **0** |
| Distinct error patterns | **0** |
| Affected users | **0** |
| 5xx responses (500/502/503/504) | **0** |
| Production cron failures | **0** |
| Failed deploys | **3** (all stale, root cause already fixed) |

## Top Patterns

None. Production runtime is clean across the entire 24h window.

The only non-200 signal in production runtime is a single warning at 03:45:07 on `/api/cron/coachhelm-roster-sweep` — `[pattern-miner.thresholds] ...` — which the routine explicitly classifies as a "tunable info log, not a bug" and skips.

## Performance — slow query top 3

**UNAVAILABLE.** The Supabase MCP connection was down throughout the run (`execute_sql` repeatedly returned `Connection terminated due to connection timeout`; `get_logs` returned `Project not found`). I could not query `pg_stat_statements`, `error_logs`, or `admin_events`. This is the only blind spot in tonight's pass — flagging for human review since DB-side patterns and slow queries cannot be ruled out from Vercel signals alone.

## Deployment health

Three ERROR-state deploys appear in the 24h window. **All three are stale** — they share a single root cause (npm-ci lockfile drift) that was resolved before the window closed:

| Deploy | Target | Commit | First error |
|---|---|---|---|
| `dpl_EPukTNxMxBAVowh3bKbrQJDzUZKm` | production | `bfa03467` ("fix(sentry): move instrumentation.ts to src/") | `npm error code EUSAGE / Missing: ai@6.0.174 from lock file` |
| `dpl_G3FH8BD45ctjuf2njF5TKqZ7NuwZ` | production | `923de62a` ("review: address multi-reviewer findings") | `npm error code EUSAGE / Missing: ai@6.0.174 from lock file` |
| `dpl_9S38X6XJytns6FBXH5d128KkDW3s` | preview | `923de62a` (same) | same |

Both production failures were superseded within minutes by:
- `dpl_BaRDpzdcKid98MVFgdypwFHVgwwX` (commit `ac581fd` — "fix(build): regenerate package-lock.json so npm ci passes") — READY production.

The most recent five production deploys are all READY (`6a9b565`, `73fc964`, `5087084`, `ac581fd`, `3BN3ojr1ymzgCxaZ5MPNfBpqjmnf`/`b36c0312`). No live failed deploy.

## Fixes applied

**None — no actionable issues.** Per the routine spec ("If you find <2 actionable issues, still open a PR with whatever you have AND the brief — even an empty PR with the brief is signal"), this PR carries only the brief.

The triage rules explicitly skip:
- Pattern-miner threshold warnings (the only non-200-class log we found).
- Single-occurrence transients.
- Stale deploy artifacts (the EUSAGE failures, already fixed by `ac581fd`).

## What couldn't be fixed / human follow-up

1. **Supabase MCP unreachable.** All `execute_sql` calls timed out and `get_logs` returned `Project not found`. The DB-side error tables (`error_logs`, `admin_events`) and `pg_stat_statements` were not queried tonight. If there are recurring DB-only error patterns or slow queries above 1000ms mean exec time, they are unaccounted for. Recommend the next nightly retry or a manual `pg_stat_statements` sweep.
