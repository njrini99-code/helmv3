# Nightly Health Check — 2026-05-13

**Window:** 2026-05-12T04:11Z → 2026-05-13T04:11Z (last 24h)
**Project:** `nick-rinis-projects/helmv3` (`prj_qPgC4eErTUsaSmv40EiQMNuTpuEV`)

## 24h Totals

| Metric | Count |
|---|---|
| Total runtime errors (level=error/fatal) | **0** |
| Distinct error patterns | **0** |
| Affected users | **0** |
| 5xx responses | **0** (filter rejected; warning sweep also clean) |
| Production cron failures | **0** (all `/api/cron/*` returning 200) |
| Failed deploys | **1** (preview only, already remediated on same PR) |

## Top Patterns

None actionable. The full 24h warning sweep returned only four entries, every one on the routine's explicit skip list:

| Time | Path | Status | Pattern | Disposition |
|---|---|---|---|---|
| 03:45:23 | `/api/cron/coachhelm-roster-sweep` | 200 | `[pattern-miner.thresholds] ...` | Skip — tunable info log, not a bug (routine rule) |
| 01:46:49 | `POST /` | 404 | `Failed to find Server...` | Skip — stale-server-action 404s are deploy artifacts (routine rule) |
| 01:46:49 | `POST /` | 404 | `Failed to find Server...` | Same pattern, same minute — single transient burst |
| 01:46:48 | `POST /` | 404 | `Failed to find Server...` | Same pattern, same minute — single transient burst |

The three `Failed to find Server Action` 404s are co-located in a single second, consistent with one client hitting a stale bundle right after a deploy went live (commit `52bda11` shipped at 01:38:43 UTC, ~8 minutes before the burst). No follow-on occurrences anywhere else in the window.

## Performance — slow query top 3

**UNAVAILABLE.** The Supabase MCP connection was down throughout the run (same symptom as the 2026-05-10 nightly):

- `execute_sql` returned `Connection terminated due to connection timeout` on every attempt, including a trivial `SELECT 1`
- `get_logs` returned `Project not found`
- `get_advisors` returned `Project not found`

`pg_stat_statements`, `error_logs`, and `admin_events` could not be queried. Two consecutive nights of the same MCP outage — flagging for human follow-up so the DB-side blind spot does not persist.

## Deployment health

One ERROR-state deploy in the window — **already remediated by the next commit on the same PR, so nothing for this routine to fix:**

| Deploy | Target | Commit | Branch / PR | First error |
|---|---|---|---|---|
| `dpl_Cg5uPqUq7zEMqQD3wEh9rce8Eqyy` | preview | `6f7cd500` ("fix(notifications): use service-role client for cross-user prefs lookup") | `claude/awesome-babbage-5Uw5X` / PR #10 | TypeScript build failure — `getRecipientGreeting` still referenced `createClient` after the import was dropped (TS2304) |

Superseded ~8 minutes later by `dpl_YsaWVBCTLs8Ece2HfQTgQzviHdXY` (commit `f1da02d8`, "fix(notifications): switch getRecipientGreeting to admin client too") on the same PR branch — that deploy is READY. PR #10 itself is still open (draft) so this never touched production; it's a preview-only failure that has already been fixed by the author.

The most recent five production deploys are all READY: `52bda11` (ios chooser cleanup), `136417c` (mobile header refine), `1b457bb` (mobile nav redesign), `2e323c4` (admin user-activity fix), `6a9b565` (security: untrack context7.json).

## Fixes applied

**None — no actionable issues.** Per the routine spec (*"If you find <2 actionable issues, still open a PR with whatever you have AND the brief — even an empty PR with the brief is signal"*), this PR carries only the brief.

The triage rules explicitly skip every signal that did surface:
- Pattern-miner threshold warnings (tunable info logs).
- Stale-server-action 404s (deploy artifacts).
- Single-occurrence transients without correlated recurrence (the three 404s burst in one second from what looks like a single client).
- Failed deploys whose root cause has already been fixed by a subsequent commit on the same PR.

## What couldn't be fixed / human follow-up

1. **Supabase MCP unreachable (second night in a row).** Tonight as on 2026-05-10, `execute_sql` timed out on every attempt and `get_logs` / `get_advisors` returned `Project not found`. The DB-side error tables (`error_logs`, `admin_events`) and `pg_stat_statements` were not queried. If there are recurring DB-only error patterns or slow queries above 1000ms mean exec time, they are unaccounted for. Two consecutive misses make this worth investigating — possible MCP credential drift or project-ref mapping issue rather than a transient outage.

2. **PR #10 (`claude/awesome-babbage-5Uw5X`) still open as draft.** The notification-prefs service-role swap is fixed on its branch but hasn't merged. The 2026-05-10 nightly already noted that this exact warning was leaking via PR #8 (also open). The same pattern keeps reappearing because the underlying fix sits on an unmerged branch. Recommend merging PR #10 (the consolidated fix that also covers `getRecipientGreeting`).
