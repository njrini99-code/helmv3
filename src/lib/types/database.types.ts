// Re-export of the canonical generated types so legacy imports keep working.
// Regenerate via Supabase MCP `generate_typescript_types` into `database.ts` —
// this file simply forwards everything.
export * from './database';
