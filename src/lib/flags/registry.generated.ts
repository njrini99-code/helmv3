// AUTOGENERATED — DO NOT EDIT BY HAND.
//
// Generated by `npm run flags:generate` (scripts/flags/generate-flags.mjs)
// from `config/feature-flags.yml`. A stamp saying so is not evidence this
// file is current — `npm run flags:check` re-parses the YAML independently
// and fails if this file has drifted (shipping.md §1: "verify the
// generator, not the stamp").
//
// Compiled to a typed constant on purpose: reading the flag registry at
// request time never touches the filesystem or parses YAML.

import type { FlagDefinition } from './types';

export const FLAG_REGISTRY: readonly FlagDefinition[] = [
  {
    feature_id: "coachhelm_v2_availability",
    owner: "golf/coachhelm",
    purpose: "Global product-availability switch for the CoachHelm V2 AI layer. Off disables the whole surface for every coach and player account; the underlying check is a fail-open string comparison (any value except the literal string \"false\" leaves it on).",
    type: "release",
    status: "active",
    created_at: "2026-09-03",
    expires_at: null,
    default: true,
    environment: {
      production: true,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "No planned removal — this has been the live product-availability switch for CoachHelm V2 since its rollout; this registry entry documents it without wiring the call site, which lives outside this PR's scope. A future session may wire src/lib/coachhelm/v2/gate.ts through isFlagEnabled('coachhelm_v2_availability') as a small, independent follow-up.",
  },
  {
    feature_id: "flight_recorder",
    owner: "platform (Bridge)",
    purpose: "Arms per-request golf round mutation tracing (client to server action to RPC to in-transaction checkpoints) so /admin/traces has data to show. An operability lever only — disabling it stops the mutation from being observed, never stops the mutation from running.",
    type: "operations_kill_switch",
    status: "active",
    created_at: "2026-09-03",
    expires_at: null,
    default: true,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: "Off: no helm_debug_start_trace / helm_debug_record_trace_step / helm_debug_finalize_trace RPCs are called from the golf round action path. The round mutation itself is unaffected — this switch only controls whether it is observed. Changed by setting HELM_FLIGHT_RECORDER_ENABLED at the environment level (deploy-time, not a runtime toggle); auditable via the Vercel environment-variable change log.",
    cleanup_plan: "No planned removal — permanent ops tooling. This registry entry documents the existing toggle; it does not wire the call sites, which sit outside this PR's file-ownership boundary. Wiring is left to the Sentry session that owns those two files.",
  },
  {
    feature_id: "jev_bug_triage",
    owner: "judgment",
    purpose: "Bug-triage judge: shadow actionability/priority judgments over grouped self-heal triage queue groups, recorded beside the existing verdict.",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_coachhelm_insight_quality",
    owner: "coachhelm",
    purpose: "CoachHelm insight quality judge at the tentative->detected lifecycle boundary (not yet wired; reserved).",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_judgment_layer",
    owner: "judgment",
    purpose: "Master switch for the Helm Judgment Layer (src/lib/ai/judgment). Off means zero Jev provider calls from any judgment use case regardless of the per-use-case flags.",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_release_canary",
    owner: "judgment",
    purpose: "Release/canary behavioural judge for release-intel (not yet wired; reserved).",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_self_heal_router",
    owner: "judgment",
    purpose: "Self-heal router judge before runRcaAnalysis (not yet wired; reserved).",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_semantic_regression",
    owner: "judgment",
    purpose: "Semantic-regression judge over E2E evidence packets (not yet wired; reserved so the flag id is stable).",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_shot_trace",
    owner: "judgment",
    purpose: "Shot-tracking trace judge: shadow semantic verdicts over finalized helm_debug flight-recorder traces (warning/failure/missing-step) — never in the shot-entry path.",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "jev_state_integrity",
    owner: "judgment",
    purpose: "State-integrity watchdog judge over deterministic candidate sets (not yet wired; reserved).",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Each use case is shadow until scripts/judgment/run-calibration.ts justifies enforcement (memory/features/helm-judgment-layer.md). Then either promote it in src/lib/ai/judgment/policy.ts and archive this flag, or delete the use case module and this flag by the expiry date.",
  },
  {
    feature_id: "typesafe_judgments",
    owner: "coachhelm",
    purpose: "Enables shadow-mode Jev judgments beside the chat grounding audit, round-recap citation check, insight upsert, chat intent and the self-heal provider-fault guard; off means zero TypeSafe calls.",
    type: "experiment",
    status: "active",
    created_at: "2026-09-17",
    expires_at: "2026-12-31",
    default: false,
    environment: {
      production: false,
      preview: true,
      development: true,
    },
    kill_switch_behavior: null,
    cleanup_plan: "Once the shadow telemetry has settled thresholds (or shown the judgments are not worth their cost), either promote each call site to act on its verdict under its own flag or delete src/lib/typesafe and this flag together.",
  },
  {
    feature_id: "verification_ensemble",
    owner: "platform (Bridge)",
    purpose: "Runs a multi-pass adversarial/security/product review chain over an already-produced root-cause analysis before it reaches the repair queue; default off, no recurring cost.",
    type: "operations_kill_switch",
    status: "active",
    created_at: "2026-09-03",
    expires_at: null,
    default: false,
    environment: {
      production: false,
      preview: false,
      development: false,
    },
    kill_switch_behavior: "Off (the default everywhere): runVerificationEnsemble returns status: 'disabled' immediately, before resolving a model provider or calling generateObject — zero additional model calls beyond today's single Diagnose call. On: adds up to four more model calls (ADVERSARY, conditional SECURITY, PRODUCT, JUDGE) per invocation, on the same Anthropic account Diagnose already uses. Changed by editing this file's environment block and running npm run flags:generate; auditable via git history on config/feature-flags.yml.",
    cleanup_plan: "No planned removal — permanent ops lever gating a genuinely optional verification pass, not a migration bridge.",
  },
] as const;
