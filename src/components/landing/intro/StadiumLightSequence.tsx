'use client'

interface StadiumLightSequenceProps {
  phase: string
}

export function StadiumLightSequence({ phase }: StadiumLightSequenceProps) {
  // Component kept for potential future use, but no visual effects
  void phase
  return null
}
