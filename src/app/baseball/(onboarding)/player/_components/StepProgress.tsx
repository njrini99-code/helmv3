// Moved to src/components/baseball/onboarding/StepProgress.tsx — was
// byte-identical (except docblocks) to
// (onboarding)/coach-onboarding/_components/StepProgress.tsx, which
// re-exports from the same shared module. See that file's docblock for why
// this was promoted to a shared PUBLIC location rather than one route
// importing the other's private `_components` folder.
export { StepProgress, type StepProgressProps } from '@/components/baseball/onboarding/StepProgress';
