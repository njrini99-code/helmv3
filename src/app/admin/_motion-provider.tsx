// Moved to src/components/admin/motion-provider.tsx — was byte-identical
// (except its docblock) to src/app/golf/admin/_motion-provider.tsx, which
// re-exports from the same shared module.
export { AdminMotionProvider } from '@/components/admin/motion-provider';
