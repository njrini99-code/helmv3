{
  "1659": {
    "disposition": "HUMAN_TEST_PENDING",
    "worktree_policy": "PARK_IF_REPRODUCIBLE",
    "note": "Product fix awaiting a physical-iPhone test. Branch and PR kept; the checkout may be parked because the work is explicitly waiting on a human with a device, not on an agent session. This is the case the park/retire split exists for — it stops an open PR costing ~3.8 GiB while it waits."
  },
  "1678": {
    "disposition": "ACTIVE",
    "worktree_policy": "KEEP",
    "note": "Opened 2026-08-30 by a CONCURRENT session. Adds two claim rows to the enforcement generator recording that a production deploy TYPED as a vercel command is CONFIGURED, while the same deploy run through scripts/deploy-prod.sh is UNENFORCED — deny rules prefix-match the command an agent submits, and the script is what gets submitted. Not touched from here: it modifies gen-enforcement-inventory.mjs, which no other branch may edit concurrently. Until it lands, main's generated enforcement inventory is incomplete by design, not by drift."
  },
  "1681": {
    "disposition": "ACTIVE",
    "worktree_policy": "KEEP",
    "note": "Owned by a CONCURRENT session. This row was transcribed from observable state — PR OPEN, worktree checked out and in use — by the lifecycle session on 2026-08-30, NOT by its owner; the owning session should confirm or correct it. Recorded rather than left blank because leaving it blank is what let --retire park this exact checkout. Carries the qualifier-slot picker fix, a prepared-but-unapplied R3 migration (20260830120000), the unfinished-round reclassification defect, RPC integrity moved toward the database, and a separately identified coach/unfinished-round navigation defect. Those facts must reach feature memory and the incident system when it lands; they must not survive only in the PR description."
  },
  "$comment": [
    "CURRENT STATE, not history. The keys here must equal the set of OPEN PRs",
    "exactly — control-plane:verify FAILS on a missing entry AND on a stale one.",
    "",
    "Why both directions. An unclassified open PR is background state: #1623 sat",
    "open for weeks while main carried a stale route count, and nobody was wrong,",
    "because nobody owned it. A leftover row is the mirror image — this file",
    "carried ACTIVE rows for #1623, #1638, #1679 and #1680 after they closed or",
    "merged, so a current-state registry was asserting things that had stopped",
    "being true. Git holds the history; this holds the present.",
    "",
    "disposition — what the PR IS:",
    "  ACTIVE, HUMAN_TEST_PENDING, STALE, SUPERSEDED, AUTOMATION_RESIDUE, UNKNOWN",
    "",
    "worktree_policy — what may HAPPEN to its checkout:",
    "  KEEP                    never park it automatically",
    "  PARK_IF_REPRODUCIBLE    park it once clean and pushed; the branch stays",
    "",
    "The second field exists because the first was being asked to imply it. On",
    "2026-08-30 `--retire` parked a concurrent session's worktree (#1681) purely",
    "because the checkout was clean, pushed, and no process was visible to lsof",
    "at that instant. Negative process detection is not proof of inactivity. For",
    "an OPEN PR, dispensability now has to be stated, not inferred.",
    "",
    "ACTIVE and UNKNOWN always KEEP, whatever this field says — a mis-typed",
    "policy beside them fails safe."
  ]
}
