# Dependencies
node_modules/

# Git worktrees
.worktrees/

# Full-stack-feature planning files
.full-stack-feature/
.full-stack-feature-archived-*/
.pnp
.pnp.js

# Next.js
.next/
out/

# Production
build/
dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo

# IDE
.vscode/
.idea/
.cursor/mcp.json

# OS
.DS_Store
Thumbs.db

# Misc
*.pem
*.zip

# Supabase
supabase/.branches
supabase/.temp

# Temporary/bundled docs (keep docs/ but not bundles)
helm-docs-bundle/
helm-cli-complete.zip
next-shadcn-dashboard-starter/

# TaskMaster
.taskmaster/logs/*.log
.taskmaster/tasks.backup.json
.vercel
.env*.local
.playwright-mcp/
.full-review-archive-*/
helm-vid/
.full-review/
.claude/scheduled_tasks.lock
*.png
# Hosted email assets must reach prod — exempt them from the global *.png ignore.
!public/email/*.png
GolfHelm-Prospect-Deck.pptx
.claude/worktrees/

# Context7 MCP config — contains secret api_key, see context7.json.example
context7.json

# Local agent/codex tooling (machine-specific, not shared)
.agents/
.codex/

# ignore a root node_modules SYMLINK too (the "node_modules/" dir pattern misses symlinks)
node_modules

# GolfHelm UI screenshots
# Auth storage states (contain Supabase session cookies — never commit)
playwright/.auth/
# Captured screenshots + per-route metadata (large, regenerated by the capture step).
# index.html, catalog.json, reports/, and README.md under ui-intelligence/ stay tracked.
ui-intelligence/screenshots/
