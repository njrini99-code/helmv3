# Notification And Task Model

Every operational message can optionally create tasks or acknowledgements. Tasks should have assignee, due date, source object, completion status, reminder state, visibility, and audit trail.
