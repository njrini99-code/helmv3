# Generic CSV Mapper

## Requirements

- Auto-detect headers.
- Suggest mappings using aliases.
- Allow saved mapping presets by team/vendor/import type.
- Show confidence per mapped column.
- Support required/optional fields.
- Normalize dates, names, jersey numbers, positions, handedness, booleans, percentages, innings pitched.
