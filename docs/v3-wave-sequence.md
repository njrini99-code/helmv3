# CoachHelm v3 — Wave Sequence Tracker

> **Live ship-status doc.** The full per-wave spec lives in [`docs/v3-master-plan.md` Part XXIII](./v3-master-plan.md). This file is the running ledger of what has shipped, what is in flight, and what is next. Update it as part of every wave's PR.

---

## Pre-Wave Verifications

| Task | Status | Resolved | Outcome |
|---|---|---|---|
| **A** — W29 qualifying UI scope | ✅ Resolved | 2026-05-24 | New v3 route at `dashboard/coachhelm/qualifying/[id]`; existing `qualifiers/[id]` gets only a "Manage selections" link. See plan Part XV.0. |
| **B** — web-push client already installed | ✅ Resolved | 2026-05-24 | `web-push@^3.6.7` and `resend@^6.7.0` already installed; only `@growthbook/growthbook` still needs install. Subscription persistence endpoint to be confirmed in W9-pt3. |

---

## Wave Ledger

Legend: `⬜ pending` · `🟡 in-flight` · `✅ merged + deployed + verified` · `⚪ deferred`

| # | Wave | Status | Branch / PR | Notes |
|---|---|---|---|---|
| W9-pt1 | Foundation docs (this PR) | ✅ merged 2026-05-25 | PR #41 | 6 docs + 3 baseline docs |
| W9-pt2 | RLS helpers + `golf_metrics` table + seed + extend `golf_coachhelm_settings` | ✅ merged + applied + verified 2026-05-25 | PR #42 | Prod verify: helpers=5, metrics=28, sg=5, putting=9, new_cols=3, new_checks=2 |
| W9-pt3 | Provider clients (push/email/flags) + Web Push subscription endpoint + local seed | ✅ merged 2026-05-25 | PR #43 | + Database types regen for W9-pt2; VAPID env vars set on Vercel (prod + dev; preview pending dashboard) |
| W10 | PGA standards table + seed | ✅ merged + applied + verified 2026-05-25 | PR #44 | Prod verify: 28 rows, 24 Tour-populated, 19 D1-populated, 0 orphan FKs. + Database types regen for `golf_pga_standards` |
| W11 | Player standing table + nightly cron | ✅ merged + applied + verified 2026-05-25 | PR #45 | Prod verify: RPC against all teams produced 171 rows across 15 metrics for 14 active players. + Database types regen for `golf_player_standing` + `refresh_player_standing` RPC. Cron schedule added in followup PR. |
| W12 | Standing backfill (one-shot, chunked) | ✅ merged + executed 2026-05-25 | PR #47 | `/api/cron/v3/standing-backfill` route shipped. Backfill formally executed against prod RPC: 171 rows refreshed across 15 metrics for 14 active players. Most-recent computed_at = 2026-05-25 03:36 UTC. |
| W13 | StandingBar component (3 variants, all states) | 🟡 in-flight | `wave13` | `src/components/golf/coachhelm/v3/StandingBar/` — index dispatcher + Card/Inline/Hero + types + utils. 38 tests passing (5 render-state matrix × 3 variants + utils unit tests). Visual review still needed on running dev server. |
| W14 | Wire standing into v2 generators | 🟡 in-flight | `wave14` | 1 helper (`standing-injection.ts`) + 6 v2 mining file modifications. Active injection in PuttDistance, Scrambling, ParType, WarmupHole, PressureGap. Future-ready imports in approach-analytics + tee-strategy. Helper no-ops when standing data isn't populated yet. |
| W15 | StandingBar adoption — coach surfaces | 🟡 in-flight | `wave15` | New `metric-config.ts` lookup; EvidencePanel renders v3 StandingBar (compact + expanded) when `evidence.standing` exists, falls through to legacy BenchmarkScale otherwise. Covers all coach insight surfaces (InsightCard → InsightsFeed → CoachAlertCenter → PlayerInsightClient). 2 new tests. |
| W16 | StandingBar adoption — player surfaces + `/my-standing` | 🟡 in-flight | `wave16` | New `/dashboard/my-standing/{page,loading}.tsx` — player-facing matrix grouped by category (SG / Putting / Approach / Short Game / Scoring / Course Mgmt / Pressure). Player CoachHelm dashboard auto-gets StandingBar via W15's EvidencePanel modification (no new wiring needed). |
| W17 | Counterfactual + secondary-line UI | 🟡 in-flight | `wave17` | `v3/counterfactual/` module (types + lookup-tables for 28 metrics + compute + baseline-loader) + `CounterfactualLine` component. Auto-suppressed below 0.3 strokes/round per locked decision. Wired into `/my-standing`. EvidencePanel integration deferred until parent surfaces fetch baseline. 14 unit tests. |
| W18 | Goals schema + RLS | 🟡 in-flight | `wave18` | golf_goals table created in prod (30 cols, 7 CHECKs, 3 indexes, 3 RLS policies, window_days generated). Database types regenerated (+142 lines). Empty until W19 service/UI + W20 focus_areas backfill. |
| W19 | Goals service + creation flow + suggestions | 🟡 in-flight | `wave19` | golf_goal_suggestions table live + RLS. v3/goals/ types + loader, server actions (createGoal, pauseGoal, abandonGoal, resumeGoal, accept/dismiss suggestion). GoalCard + GoalCreationModal components. 6 GoalCard tests. Engine-driven suggestion writer + evaluator cron deferred to a W19-followup. |
| W20 | Focus areas → goals migration | 🟡 in-flight | `wave20` | One-shot SQL: 5 of 7 active focus_areas mapped + migrated to golf_goals (2 unmapped — "Bounce-back %" + "Fairways hit %" have no v3 metric). All as coach-assigned mandatory + shared. Rename `_deprecated_` deferred until /my-development reads from goals. |
| W21 | v3 generator base + `engine_version` + first generator | 🟡 in-flight | `wave21` | engine_version column + check constraint + index live; 266 existing insights labeled 'v2' via default. BaseGenerator abstract class auto-injects standing + counterfactual + v3: prefix + engine_version stamp via upsertInsightV3 wrapper. PuttDistanceGenerator (3 buckets). 5 unit tests. |
| W22 | v3 generators: putt-bias + scrambling + approach-miss | 🟡 in-flight | `wave22` | 2 of 3 generators built: PuttBiasGenerator (diagnostic, requiresStanding=false), ScramblingGenerator (sand only — rough/fairway need shot-level). ApproachMissGenerator deferred — needs a shot-source helper (cache only has overall approach_proximity_average). BaseGenerator extended with `requiresStanding` flag for diagnostic-only generators. 8 new tests (13 generator total). |
| W23 | v3 generators: tee-strategy + par-type + course-mgmt | 🟡 in-flight | `wave23` | 2 of 3 generators: ParTypeGenerator (par 3/4/5) + CourseMgmtGenerator (penalty + big_number). TeeStrategyGenerator deferred — needs shot+hole-level data (driver-vs-3W on penalty-prone holes), same constraint as ApproachMiss. 8 new tests (21 generator total). |
| W24 | v3 generators: pressure-gap + warmup-hole | 🟡 in-flight | `wave24` | NEW `refresh_player_standing_round_metrics` RPC populates standing for practice_tournament_delta + opening_hole_delta (199 total standing rows / 17 metrics in prod). PressureGapGenerator + WarmupHoleGenerator use default requiresStanding=true. Refresh + backfill routes updated to call both RPCs. 10 new tests (31 generator total). |
| W25 | v2 → v3 generator CUTOVER | 🟡 in-flight (partial — 7 of 9 swapped) | `wave25` | Orchestrator now invokes 13 v3 BaseGenerator instances + 1 NET-NEW CourseMgmt (penalty + big_number) + 3 deferred v2 (approachMiss, teeStrategy, worstHoles). Coverage regression: v2 puttDistance covered 5 buckets / puttMissBias 4 directions; v3 covers 3 buckets / 2 directions until cache alignment. NO v2 file deletions (W26). Zero test regressions. |
| W26 | v2 sunset (code only — NO table drops) | 🟡 in-flight (partial — scope-reduced) | `wave26` | Deleted 4 v2 generator files (putt-analytics, scrambling-analytics, scoring-context, pressure-gap) + 4 tests. Could NOT delete v2/reasoning + v2/nlg per plan — confidence-calibrator still used by coachhelm-calibration cron; orchestrator still instantiates ReasoningEngine + InsightComposer for non-Tier-1 pipeline. course-management + approach-analytics + tee-strategy stay (still active per W25 deferrals). Build verified clean. |
| W27 | Coach intent + roster pill + drawer | 🟡 in-flight | `wave27` | golf_coach_player_intent table live (RLS Pattern 2 — coach-owned, player-invisible). Types + ALERT_POSTURE_MULTIPLIER + NARRATIVE_GOAL_PRESENTATION. loadCoachIntents/loadIntent loaders. setIntent + bulkSetIntent server actions. IntentPill (button + span variants, 6 narrative goals). IntentDrawer (modal w/ segmented narrative + radio posture + notes). 6 IntentPill tests. Roster row integration deferred (1 small follow-up — easier to land alongside next roster-touching wave). Engine alert_posture multiplier deferred (touches v2 upsert; cleaner after v2/reasoning is sunset). |
| **wave-3bucket-fix** | Doc constraint + ApproachMiss generator | 🟡 in-flight | `wave-3bucket-fix` | **CORRECTION wave.** Master plan amended with 3-bucket club model (driver / non_driver / putter — no per-iron distinction). Part V.1.5 + Part IX.1.5 + Part XXVI updated. Composite rules in Part IX.2 reworked (10 of 12 implementable). NEW shot-source helper (`v3/engine/shot-source.ts`) reads golf_shots. NEW ApproachMissGenerator (3 buckets — diagnostic, standing-row population is a follow-up RPC). Orchestrator updated: v3.approachMiss replaces v2.approachMiss (3 instances; 8 v3 + 2 v2 fallbacks remaining). 41 v3 generator tests total. |
| W28 | Composite insights v1 (12 rules + synthesis) | 🟡 in-flight (partial — 3 of 10 rules implementable post-3-bucket-fix) | `wave28` | NEW `v3/composite/` module: types + loader (30-day insight window) + 3 rules (pressure_decel_chain, bunker_miss_side_amplifier, long_approach_3putt_cascade) + synthesis runner with subset-suppression conflict resolution. Wired into v2 orchestrator AFTER Tier-1 batch (failure-isolated). Composites use `v3:composite:<rule_id>:` signature prefix; rule.category drives persisted InsightCategory. 12 new tests (all detect/compose + suppression logic). Remaining 7 rules deferred to W28-followup (mostly need hole-sequence or shot-source data). |
| W29-pt1 | Qualifying schema additions + RLS | 🟡 in-flight | `wave29-pt1` | Migration A: `golf_qualifiers` + 4 cols + 3 CHECKs + partial index. Migration B: `golf_qualifier_selections` table + 2 RLS policies (coach write, player read when selection_state='selected'). Prod verify: 4 cols, 3 checks, 1 idx, 1 new table, 2 policies. + Database types regen (+69 lines). |
| W29-pt2 | Qualifying service + actions + selection workspace UI | 🟡 in-flight | `wave29-pt2` | NEW `v3/qualifying/` module: types + state-machine (open→scoring→closed→selected lifecycle + classifySlots + canConfirmSelection) + loader (computes leaderboard rank in-proc by to_par asc then total_score asc tiebreak) + service (transitionSelectionState, setCoachPick, removeCoachPick, confirmSelection — slot ceiling enforced, reasoning required). NEW server actions in `actions/v3/qualifying.ts` (4 wrappers: advance/setPick/removePick/confirm — auth + team-coach guard + revalidatePath). NEW page `dashboard/coachhelm/qualifying/[id]` + loading shell. NEW components `QualifyingBoard/` (orchestrator + SelectionStateBar + LeaderboardWithSlots + CoachPickPanel — 4 files). Single-line link insertion in existing `qualifiers/[id]/page.tsx` (coach-only "Manage selections →"). 15 new tests (state-machine + classifySlots + canConfirmSelection + rankCandidates). Travel-brief chat push deferred to W32 (TODO comment in service). |
| W30 | LLM service (Haiku) + round-review composer + budget tables (admin dashboard deferred) | 🟡 in-flight | `wave30` | **Plan amendment (Part XI.5 + XI.6):** Haiku for round_review + hero_narrative (was Sonnet); Sonnet retained only for coach_chat; admin cost dashboard dropped from W30 scope (~$33/team/yr vs ~$106). 2 migrations applied to prod: `golf_coachhelm_llm_budget` (per-coach daily cap + task_class_usage jsonb) + `golf_coachhelm_llm_calls` (append-only call log). Prod verify: 2 tables, 2 RLS policies (coach-read), 5 indexes (2 explicit + 3 PK). NEW `v3/llm/` module: types (MODEL_FOR_TASK, FALLBACK_PRIORITY, estimateCostUsd) + budget gate (checkBudget + recordSpend, lazy-seeds from `golf_coachhelm_settings.llm_budget_usd_per_day`) + citation verifier (numeric-token extraction against EvidenceClaim set, safe-token allowlist for 0/1/2/3/100) + compose() wrapper (budget check → generateText via gateway → verify → log + record spend → fallback path on budget exhaustion or LLM error) + composeRoundReview() task. NEW server action `actions/v3/llm.ts` (generateLlmRoundReview — auth + round/player lookup + billing-coach resolution via team_members + primary staff). 12 unit tests (model map + priority + cost estimation + citation verification). Surface integration deferred — composer is callable; rendering on the existing round-review page is a follow-up. |
| W30.5 | W28-followup: 7 composite rules + hole-sequence loader + lie-type shot-source | 🟡 in-flight | `wave30-5` | Framework extension: `CompositeRule.detect()` now optionally receives a `CompositeContext` (hole_scores + short_game_shots + flyer_lie_shots) pre-loaded once by the synthesis runner. 7 new rules shipped: lag_distance_3putt + short_approach_proximity_gap (insight-driven); closing_hole_fatigue + front_9_starter + doubles_after_bogey (hole-sequence ctx); short_side_scrambling_chain + flyer_lie_over_the_green (lie-typed shot ctx). Registry now exposes 10 of 11 implementable rules (only "Tee-club mismatch" still deferred — needs hole-level outcome joining). 17 new tests (29 total composite); typecheck + build clean. |
| W31 | LLM hero narrative on player dashboard | 🟡 in-flight | `wave31` | NEW `v3/llm/hero-narrative.ts` — composeHeroNarrative() task (Haiku per amended Part XI.5): 2-3 sentence second-person paragraph grounded in top insight + standing + optional goal + optional counterfactual. NEW `HeroNarrativeCard` client component — renders the fallback text on mount, swaps to LLM prose once the action resolves; failure-silent (fallback stays put on budget gate or error). Server action `generateHeroNarrative` added to existing `actions/v3/llm.ts`. M:1 — PlayerCoachHelmDashboard renders the card above the existing HeroInsightCard when topInsight is present. Citation verifier hardened to strip unit suffixes (ft/yd/strokes) so display values like "28 ft" cite cleanly. 5 new tests (evidence builder ↔ verifier integration, 46 v3 LLM tests total). Typecheck + build clean. |
| W32-pt1 | Coach chat schema + RLS | 🟡 in-flight | `wave32-pt1` | 2 migrations applied to prod: `golf_coachhelm_chat_conversations` (coach-owned threads, pinned + archived_at flags) + `golf_coachhelm_chat_messages` (role enum user/assistant/tool, tool_calls/tool_results jsonb, append-only). RLS Pattern 2 — coach-owned, no player access (Part XII.5 deferral). Prod verify: 2 tables, 2 policies, 4 indexes (2 PK + 2 explicit). Database types regenerated. Typecheck clean. |
| W32-pt2 | Coach chat tool handlers + agent + send-message endpoint | 🟡 in-flight | `wave32-pt2` | NEW `v3/chat/` module — types, persistence (conversations + messages), 10 in-process tool handlers in tools.ts with zod-validated inputs (get_player_context, get_player_insights, get_player_standing, get_player_recent_rounds, compare_players, get_team_overview, get_team_patterns, list_player_goals, get_goal_details, create_goal_for_player — the only mutating tool, UI-gated). ToolLoopAgent wiring with Sonnet via Vercel AI Gateway + system instructions requiring explicit coach confirmation before goal creation. POST `/api/coachhelm/v3/chat/send` — auth + coach lookup → load/create conversation → append user msg → run agent → persist tool ledger + assistant msg → mirror call into `golf_coachhelm_llm_calls` for cost attribution. **Extended `src/types/ai-shim.d.ts`** with ToolLoopAgent + tool() + ModelMessage declarations (the shim wins over package types on Vercel; this keeps prod typecheck green). 9 schema-validation tests. Typecheck + build clean (new route compiles). 10 HTTP debug endpoints at `/api/coachhelm/v3/agent-tools/<name>` deferred. |
| W32-pt3 | ChatDrawer + full-history page UI | 🟡 in-flight | `wave32-pt3` | NEW `components/golf/coachhelm/v3/Chat/` cluster: ChatMessageList (3 bubble variants user/assistant/tool + Thinking affordance) + ChatComposer (textarea + Enter-to-send) + ChatDrawer (slide-over right desktop / full-screen mobile, optimistic user bubble + rollback on error, "New chat" + "History →" affordances). NEW page `/dashboard/coachhelm/chat` with left-rail conversation list + main message pane (`ChatHistoryClient` orchestrator), loading skeleton. NEW GET endpoints: `/api/coachhelm/v3/chat/conversations` (list) + `/api/coachhelm/v3/chat/conversations/[id]` (load). M:1 — dashboard layout async-loads session + mounts ChatDrawer coach-only. 6 new render-state tests (15 tests across chat-tools + chat-message-list). Typecheck + build clean; all 4 chat routes compile. |
| W33-pt1 | Player genome schema + framework + first dimension | 🟡 in-flight | `wave33-pt1` | Migration applied to prod: `golf_player_genome` (player_id PK, vector jsonb, rounds_basis, computed_at). RLS: player reads own row + team coaches read all. Prod verify: 1 table, 2 policies, 2 indexes. NEW `v3/genome/` module: types (GENOME_CATEGORIES 8 categories × 10 dims target, GenomeDimension interface, DimensionResult, GenomeContext), registry (1 dim shipped — W33-pt2 adds 7 more), orchestrator (loads 90-day round context + holes + shots once, runs every registered dimension failure-isolated, upserts vector). First dimension: `miss_side_bias` (miss_tendencies category — -1=left to +1=right across approach shots, requires ≥30 directional approaches + 8 rounds, includes "Left/Right/Symmetric" label). NEW endpoint `POST /api/coachhelm/v3/genome/compute` — authz: player themselves OR coach of their team. Database types regenerated. 9 unit tests (registry sanity + dimension behavior across edge cases). Typecheck + build clean. Nightly cron + 7 more dimensions in pt2. |
| W33-pt2 | 7 more genome dimensions + nightly cron + backfill | 🟡 in-flight | `wave33-pt2` | Extended `GenomeContext` with `rounds: GenomeRound[]` so dims with per-round metadata work (orchestrator now loads round_type + score_to_par + round_date for the 90-day window). 7 new dims (one per remaining category): `pressure_delta` (tournament vs practice score-to-par, ≥4/side, "Tightens up / Steady / Thrives"), `scrambling_rate` (chips/pitches from rough/bunker landing ≤10ft, ≥15 attempts, "Wizard / Reliable / Leaky"), `par3_proficiency` (avg par-3 score-to-par, ≥16 par-3 holes, "Under par / Even / Bleeds shots"), `weather_sensitivity_stub` (always-null per Part IX.2 deferred — keeps the 8th radar slot present), `back_nine_delta` (holes 10-18 vs 1-9, ≥5 rounds, "Fades late / Steady / Closes strong"), `scoring_trend` (last 30 days vs 31-90 days, ≥3 each, "Improving / Flat / Regressing"), `driver_usage` (driver vs non_driver tee rate per 3-bucket model, ≥40 tee shots, "Bomber / Mixed / Layback"). All 8 categories now covered (one dim each). NEW cron `/api/cron/v3/genome-nightly` (chunked 25 players, null-computed-first ordering, self-balancing) + `/api/cron/v3/genome-backfill` (one-shot all-players). 14 new dim tests + 1 registry-coverage test (24 genome total). Typecheck + build clean. |
| W34 | Player genome UI (player + coach + compare) | 🟡 in-flight | `wave34` | NEW v3/genome/ data layer: `normalize.ts` (per-dim normalizers map heterogeneous value spaces to a single [0,1] good-axis for the radar — symmetric for miss_side_bias, signed-delta inversion for pressure/back-9/par3/trend, identity for rates, neutral-around-0.6 for driver_usage), `persona.ts` (derivePersona — strengths/watchouts confidence-gated ≥0.5 + norm thresholds 0.7/0.3, course_profile sentence built from driver_usage + par3_proficiency + miss_side_bias), `loader.ts` (single + batch genome reads). NEW visual components: `GenomeRadar` (pure SVG, 8 spokes, 4 concentric rings, polygon path + per-spoke dots, framer-motion entrance, hover tooltip, supports 1-2 series for overlay), `GenomePersonaPanel` (strengths/watchouts/course-profile editorial), `GenomeDimensionGrid` (responsive 2/3/4-col cards with 8-cell score bars + confidence labels, staggered entrance). NEW pages: coach `/dashboard/coachhelm/genome/[playerId]` (radar+persona side-by-side, dim grid below), player `/dashboard/my-game-profile` (phone-stacked layout), compare `/dashboard/coachhelm/genome/compare?p1=&p2=` (two-series overlay with player picker columns). 12 new tests covering normalize + persona logic (36 genome total). Typecheck + build clean; all 3 new pages + 1 existing endpoint compile. |
| W35 | Outcome causality schema + attribution cron | 🟡 in-flight | `wave35` | 2 migrations applied to prod: `golf_insight_outcome_attribution` (PK insight_id, baseline + post + delta + lift, attributed_at) + `golf_coachhelm_coach_weights` (PK coach_id+insight_type+intent, EMA-updated weight + sample_n). RLS: attribution gated by insight.coach_id join; weights by current_coach_id. Prod verify: 2 tables, 2 policies. NEW `v3/causality/attribute.ts`: `computeAttribution` (14d pre + 21d post window, lift subtracts ambient 90-day trend; v1 only wires score_to_par — other metrics return null and the cron logs them as deferred) + `nextWeight` (exponential moving average toward 1.5 on positive lift / 0.5 on negative, clamped to [0.25, 2.0]). NEW cron `/api/cron/v3/causality-attribute` — picks 50 insights surfaced ≥21d ago without attribution, computes + writes row + EMA-updates per-(coach, insight_type) weight. 7 unit tests on the EMA logic. Database types regen. Typecheck + build clean. |
| W36 | Outcome causality wired into ranking | 🟡 in-flight | `wave36` | NEW `v3/ranking/score.ts`: `scoreInsight(insight, weights) = |strokes_impact| × confidence × coach_weight`. `loadCoachWeightsForPlayer` resolves the player's primary coach (membership → primary staff), pulls per-(insight_type, intent='general') rows where sample_n ≥ MIN_CALIBRATED_SAMPLES (10) so under-calibrated weights default to 1.0. Wired into `loadEvidenceBackedInsights` in `actions/insights.ts` — added insight_type to the SELECT, projected through, then rankInsights applied before return. v2 in-memory insights stay in their original order (frozen). 9 unit tests on the score + rank formula (stable sort, weight promotion behavior). Typecheck + build clean. |
| W37 | Weekly coach email + cron + template | 🟡 in-flight | `wave37` | NEW `v3/recap/` module: builder (queries the 7-day window for rounds played + avg score-to-par + insights count + active goals + top-3 active players + top-3 insight patterns) + template (HTML + plain text via inlined-style table layout, big-number stat row + per-player list + pattern bullets + footer; HTML-escapes team name for XSS safety). NEW cron `/api/cron/v3/weekly-coach-email` — picks every team's primary coach, builds recap, sends via Resend (already-installed wrapper at `v3/foundation/email.ts`). Degrades cleanly when RESEND_API_KEY is unset (counted as `skipped_provider_unset` not error). 8 unit tests on template formatting (signed to-par, "E" zero, em-dash null, empty state, HTML escape, text version). Typecheck + build clean. Schedule lives in vercel.json (operational follow-up). |
| W38 | Practice Rx + `impacts_metric_id` on drills | ⬜ | — | LLM-driven plans |
| W39 | Auto-ingest: Arccos | ⬜ | — | |
| W40 | Auto-ingest: Garmin | ⬜ | — | |
| W41 | Auto-ingest: TrackMan + `golf_practice_sessions` | ⬜ | — | |
| W42 | Notifications preferences + per-category routing | ⬜ | — | |

---

## Update Protocol

Every wave PR must update this file:
1. Set the wave's status to 🟡 when the PR is opened.
2. Set to ✅ when merged + deployed + verified in prod (with the prod SELECT that proves it landed).
3. Add a notes column entry for any deviation from the plan.

If a wave's scope changes vs. Part XXIII, the master plan is amended in the same PR — never lie to this file by silently adjusting.
