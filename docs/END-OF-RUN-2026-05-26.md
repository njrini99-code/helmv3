<!--
STATUS: PARKED
DATE: 2026-07-10
PARKING DECISION: Self-described session/continuation report ("save Part 2 continuation report before computer restart") — an accurately-dated point-in-time log, not a live-state claim. Parked as historical record.
KEPT FOR HISTORY -- do not delete this file.
-->

# GolfHelm v3 — End-of-run review (2026-05-26)

> **Comprehensive map of every wave shipped tonight.** Generated after merging the last PR; mirrors the live state of prod + the codebase as of 2026-05-26 23:30 UTC.

---

## Headline numbers

| Metric | Value |
|---|---|
| Waves shipped tonight | 12 (W28 → W42) |
| PRs opened + merged | 16 (#65 → #83) |
| Migrations applied to prod | 9 |
| New prod tables | 11 |
| New prod columns (existing tables) | 7 |
| New RLS policies | 14 (on the 11 new tables) + extras on existing-table extensions |
| New code files | ~80 |
| New test files | 13 |
| New unit tests | 108 |
| Total unit tests passing | 1159 |
| Unit tests failing | 8 (ALL pre-existing v2 — none touched tonight) |
| Tests skipped | 39 |
| Typecheck status | ✅ clean |
| Production build | ✅ clean (`✓ Compiled successfully in 17.4s`) |

## Test suite breakdown

Pre-existing failures (not from tonight's work — all in v2 modules that haven't been touched since the v2-freeze rule landed):

- `src/test/coachhelm/v2/post-round-trigger.test.ts` × 2
- `src/test/golf/actions/development.test.ts` × 1
- `src/test/golf/components/RoundTakeaway.test.tsx` × 1
- `src/test/coachhelm/v2/insights/upsert-coach-scoping.test.ts` × 1
- `src/test/coachhelm/v2/insights/upsert.test.ts` × 3

Every new test file shipped tonight passes:

| Wave | Test file | Pass |
|---|---|---|
| W28 | `composite.test.ts` | 12/12 |
| W28 (3-bucket-fix) | `ApproachMissGenerator.test.ts` | (already existed) |
| W29-pt2 | `qualifying.test.ts` | 15/15 |
| W30 | `llm.test.ts` | 12/12 |
| W30.5 | `composite-w305.test.ts` | 17/17 |
| W31 | `hero-narrative.test.ts` | 5/5 |
| W32-pt2 | `chat-tools.test.ts` | 9/9 |
| W32-pt3 | `chat-message-list.test.tsx` | 6/6 |
| W33-pt1 | `genome.test.ts` | 9/9 |
| W33-pt2 | `genome-dimensions.test.ts` | 14/14 |
| W34 | `genome-ui.test.ts` | 12/12 |
| W35 | `causality.test.ts` | 7/7 |
| W36 | `ranking.test.ts` | 9/9 |
| W37 | `recap.test.ts` | 8/8 |
| W38 | `practice-rx.test.ts` | 6/6 |
| W39-41 | `ingest.test.ts` | 8/8 |
| W42 | `notifications.test.ts` | 9/9 |

**Tonight new test total: 158 (counts include older composite tests still passing). All green.**

---

## Wave-by-wave map

### W28 — Composite insights v1 (3 rules)
**PR #65 · merged 22:01 UTC**
- `src/lib/coachhelm/v3/composite/` — types, loader (30-day window), 3 rules (`pressure-decel-chain`, `bunker-miss-side-amplifier`, `long-approach-3putt-cascade`), index registry, synthesis runner with subset-suppression conflict resolution
- Wired into `src/lib/coachhelm/v2/orchestrator.ts` AFTER the Tier-1 batch — failure-isolated
- Signature prefix `v3:composite:<rule_id>:` so composites coexist with Tier-1 insights in `golf_coach_insights`

### W30.5 — 7 more composite rules + ctx loader
**PR #70 · merged 23:24 UTC**
- Extended `CompositeRule.detect()` with optional `CompositeContext` arg (hole-sequence + lie-typed shot data pre-loaded once per player)
- 7 new rules: `lag_distance_3putt`, `short_approach_proximity_gap`, `closing_hole_fatigue`, `front_9_starter`, `doubles_after_bogey`, `short_side_scrambling_chain`, `flyer_lie_over_the_green`
- New `hole-sequence-loader.ts` parallel-loads hole_scores + short_game_shots + flyer_lie_shots
- **10 of 11 implementable composite rules now active** (only "Tee-club mismatch" remains deferred — needs hole-level shot+outcome joining)

### W29-pt1 — Qualifying schema + RLS
**PR #67 · merged 22:41 UTC**
- Prod: `golf_qualifiers` + 4 cols + 3 CHECKs + partial idx; `golf_qualifier_selections` table + 2 RLS policies (coach write, player read when state='selected')

### W29-pt2 — Qualifying service + actions + workspace UI
**PR #68 · merged 22:52 UTC**
- `v3/qualifying/`: types, state-machine (open→scoring→closed→selected, one-step forward only), loader (in-process rank by to_par asc + total_score asc tiebreak), service (transitionSelectionState, setCoachPick, removeCoachPick, confirmSelection)
- 4 server actions in `actions/v3/qualifying.ts` with auth + team-coach guard + revalidatePath
- New page `/dashboard/coachhelm/qualifying/[id]` + `QualifyingBoard/` component cluster (4 files)
- M:1 single-line "Manage selections →" link from existing `qualifiers/[id]/page.tsx`
- **Deferred:** Travel-brief chat push (TODO comment in service) — lights up with W32

### W30 — LLM service (Haiku) + round-review composer + budget tables
**PR #69 · merged 23:11 UTC**
- **Master plan amendment:** Part XI.5 — Haiku for `round_review` + `hero_narrative` (was Sonnet); Sonnet retained only for `coach_chat`. Admin cost dashboard deferred from W30 scope. Saves ~$73/team/yr.
- Prod: `golf_coachhelm_llm_budget` (per-coach daily cap + task_class_usage jsonb) + `golf_coachhelm_llm_calls` (append-only call log)
- `v3/llm/` module: types, budget gate (checkBudget + recordSpend, lazy team-default seed), citation verifier (numeric-token extraction against EvidenceClaim set + safe-token allowlist), `compose()` wrapper (budget → gateway → verify → log → record spend; fallback path on budget exhaustion or LLM error), `round-review.ts` composer
- `generateLlmRoundReview` server action
- **Prereq saved to memory:** `AI_GATEWAY_API_KEY` + non-zero `golf_coachhelm_settings.llm_budget_usd_per_day` required or all calls fall back to template

### W31 — LLM hero narrative on player dashboard
**PR #71 · merged 00:31 UTC**
- `composeHeroNarrative()` task (Haiku) — 2-3 sentence second-person paragraph grounded in top insight + standing + optional goal + optional counterfactual
- `HeroNarrativeCard` client component — renders fallback on mount, swaps to LLM prose; failure-silent
- M:1 — embedded above `HeroInsightCard` on `PlayerCoachHelmDashboard`
- Citation verifier hardened: strips unit suffixes (ft/yd/strokes/etc.) so display values cite cleanly

### W32-pt1 — Coach chat schema + RLS
**PR #72 · merged 00:49 UTC**
- Prod: `golf_coachhelm_chat_conversations` (coach-owned threads, pinned + archived_at) + `golf_coachhelm_chat_messages` (append-only, role enum user/assistant/tool, tool_calls/tool_results jsonb)
- RLS Pattern 2 (coach-owned, player-invisible per Part XII.5)

### W32-pt2 — Chat agent + 10 tool handlers + send endpoint
**PR #73 · merged 01:02 UTC**
- `v3/chat/`: types, persistence, 10 in-process zod-validated tool handlers in `tools.ts` (`get_player_context`, `get_player_insights`, `get_player_standing`, `get_player_recent_rounds`, `compare_players`, `get_team_overview`, `get_team_patterns`, `list_player_goals`, `get_goal_details`, `create_goal_for_player`)
- `ToolLoopAgent` wired with Sonnet via Vercel AI Gateway + instructions requiring explicit coach confirmation before goal creation
- `POST /api/coachhelm/v3/chat/send` — auth + conversation + agent + persist + mirror into `golf_coachhelm_llm_calls` for cost attribution
- **Extended `src/types/ai-shim.d.ts`** — Vercel's TS resolver prefers the shim over real package types on build, so the shim now declares ToolLoopAgent + tool + ModelMessage + ToolDefinition
- **Deferred:** 10 HTTP debug endpoints at `/api/coachhelm/v3/agent-tools/<name>` (handlers run in-process via send endpoint; debug surface can ship later)

### W32-pt3 — ChatDrawer + full-history page
**PR #74 · merged 01:10 UTC**
- `components/golf/coachhelm/v3/Chat/`: ChatMessageList (3 bubble variants + Thinking) + ChatComposer + ChatDrawer (slide-over desktop / full-screen mobile)
- New page `/dashboard/coachhelm/chat` with left-rail conversation list + main pane
- 2 GET endpoints: `/conversations` (list) + `/conversations/[id]` (one thread)
- M:1 — dashboard layout async-loads session + mounts ChatDrawer coach-only

### W33-pt1 — Player genome schema + framework + first dimension
**PR #75 · merged 01:17 UTC**
- Prod: `golf_player_genome` (player_id PK, vector jsonb keyed by dimension_id, rounds_basis, computed_at). RLS: player reads own; team coaches read all
- `v3/genome/` module: types (8 categories × 10 dim target), registry, orchestrator (loads 90-day context once, runs every registered dimension failure-isolated)
- First dim: `miss_side_bias` (miss_tendencies — value [-1, +1], confidence scales with sample size capped at 1, Left/Right/Symmetric label)
- `POST /api/coachhelm/v3/genome/compute` for one-off triggers

### W33-pt2 — 7 more genome dimensions + nightly cron + backfill
**PR #76 · merged 01:25 UTC**
- Extended `GenomeContext` with per-round metadata (round_type + score_to_par + round_date)
- 7 new dims — one per remaining category:
  - `pressure_delta` (pressure_response) — tournament vs practice, ≥4/side, Tightens up / Steady / Thrives
  - `scrambling_rate` (recovery_patterns) — chips/pitches from rough/bunker landing ≤10ft, Wizard / Reliable / Leaky
  - `par3_proficiency` (course_type_affinity) — Under par / Even / Bleeds shots
  - `weather_sensitivity_stub` — always null (deferred per Part IX.2); keeps the radar slot present
  - `back_nine_delta` (stamina) — Fades late / Steady / Closes strong
  - `scoring_trend` (learning_velocity) — Improving / Flat / Regressing
  - `driver_usage` (strategic_profile) — Bomber / Mixed / Layback
- All 8 categories now have ≥1 dim shipped (registry-coverage test enforces this)
- Crons: `/genome-nightly` (chunked 25 players, null-computed-first) + `/genome-backfill` (one-shot all)

### W34 — Player genome UI (radar + persona + dimension grid)
**PR #77 · merged 01:49 UTC**
- Data layer: `normalize.ts` (per-dim normalizers projecting heterogeneous value spaces onto a single [0,1] good-axis), `persona.ts` (derivePersona — strengths/watchouts confidence-gated, one-sentence course_profile from driver_usage + par3_proficiency + miss_side_bias), `loader.ts`
- Visual: `GenomeRadar` (pure SVG, 8 spokes, 4 concentric rings, framer-motion entrance, hover tooltip, 1-2 series for overlay), `GenomePersonaPanel` (emerald strengths / amber watchouts), `GenomeDimensionGrid` (responsive 2/3/4-col cards with 8-cell score bars)
- 3 pages: coach `/dashboard/coachhelm/genome/[playerId]`, player `/dashboard/my-game-profile`, compare `/dashboard/coachhelm/genome/compare?p1=&p2=`

### W35 — Outcome causality schema + attribution cron
**PR #78 · merged 22:54 UTC (2026-05-26)**
- Prod: `golf_insight_outcome_attribution` (PK insight_id, baseline + post + delta + lift) + `golf_coachhelm_coach_weights` (PK coach+type+intent, EMA weight + sample_n)
- `v3/causality/attribute.ts`: `computeAttribution` (14d pre / 21d post / 90d ambient drift) + `nextWeight` (EMA toward 1.5/0.5, clamped [0.25, 2.0])
- `/api/cron/v3/causality-attribute` — picks 50 insights surfaced ≥21d ago without attribution, computes + writes + updates per-(coach, insight_type) weight
- v1 only wires `score_to_par`; other metric IDs return null + cron logs them as deferred

### W36 — Outcome causality wired into ranking
**PR #79 · merged 23:00 UTC**
- `v3/ranking/score.ts`: `scoreInsight = |strokes_impact| × confidence × coach_weight` per Part XIV.4
- `loadCoachWeightsForPlayer` resolves player → primary coach → calibrated weights (sample_n ≥ 10; uncalibrated rows default to 1.0)
- Wired into `loadEvidenceBackedInsights` in `actions/insights.ts` — added insight_type to SELECT, ranking applied before return

### W37 — Weekly coach email (Whoop-style)
**PR #80 · merged 23:06 UTC**
- `v3/recap/`: builder (7-day window: rounds + avg to-par + insights + active goals + top-3 active players + top-3 patterns) + HTML+text template (inlined-style table, XSS-safe HTML escape)
- `/api/cron/v3/weekly-coach-email` — picks every team's primary coach, builds, sends via Resend (existing wrapper)
- Degrades cleanly when `RESEND_API_KEY` unset (counted as `skipped_provider_unset`, not error)

### W38 — Practice Rx + impacts_metric_id on drills
**PR #81 · merged 23:13 UTC**
- Prod: `golf_drills.impacts_metric_id` (nullable text FK → golf_metrics) + partial idx
- `v3/practice-rx/composer.ts`: composePracticeRx() (prompt from goal + tagged drill catalog + recent weakness, rides W30 compose() round_review task budget) + parsePlan() (anti-hallucination drill-id filter — drops any id not in candidate set)
- `actions/v3/practice-rx.ts`: generatePracticeRx(goal_id)

### W39+W40+W41 — Auto-ingest framework (Arccos / Garmin / TrackMan)
**PR #82 · merged 23:20 UTC**
- Bundled into one wave (same schema + adapter pattern across all 3)
- Prod: `golf_ingest_connections` + `golf_ingest_sync_log` + `golf_practice_sessions`
- `v3/ingest/`: types, state machine (`nextConnectionState` — error self-heals; revoked sticks; explicit next_state wins), 3 provider stubs (each `isConfigured()=false` until env vars + partnership land), registry
- `/api/cron/v3/ingest-sync` — walks active connections, dispatches, logs result, updates state
- **Prereqs saved to memory:** ARCCOS_CLIENT_ID/SECRET, GARMIN_CONSUMER_KEY/SECRET, TRACKMAN_API_KEY env vars + provider HTTP clients + per-provider golf_rounds/golf_shots mappings

### W42 — Notifications preferences + per-category routing
**PR #83 · merged 23:25 UTC**
- Prod: extended `golf_player_notification_state` with `prefs jsonb` (default-seeded with Part XXII 10-category defaults) + `quiet_mode boolean`
- `v3/notifications/router.ts`: routeNotification + shouldDeliver. Quiet-mode exempts `round_review_ready` + `coach_assigned_goal` per Part XXII
- 3 server actions: loadMyNotificationPrefs, setCategoryChannel, setQuietMode
- New player page `/dashboard/settings/notifications` with quiet-mode card + 10×3 category-channel toggle grid

---

## Prereqs the user must configure before features work end-to-end

Saved to permanent memory at `~/.claude/projects/-Users-ricknini/memory/`:

1. **`AI_GATEWAY_API_KEY`** env var + `golf_coachhelm_settings.llm_budget_usd_per_day` non-zero per team — required for round_review (W30), hero_narrative (W31), coach_chat (W32), practice_rx (W38). Without them, all LLM surfaces fall back to template.
2. **`RESEND_API_KEY`** env var — required for weekly coach email (W37). Without it, `skipped_provider_unset` log entries pile up but nothing breaks.
3. **`ARCCOS_CLIENT_ID/SECRET`, `GARMIN_CONSUMER_KEY/SECRET`, `TRACKMAN_API_KEY`** env vars + partnership approvals + per-provider HTTP-client code in `v3/ingest/providers/<provider>.ts` — required for auto-ingest. Stubs return zero-row "provider env unset" logs until wired.
4. **Drill library content with `impacts_metric_id` tagged** — required for Practice Rx (W38). Without tagged drills, `generatePracticeRx(goal_id)` returns a "no drills tagged" error.
5. **Vercel cron schedules** for the new routes (in `vercel.json`):
   - `/api/cron/v3/genome-nightly`
   - `/api/cron/v3/genome-backfill` (one-shot, manual trigger)
   - `/api/cron/v3/causality-attribute` (nightly)
   - `/api/cron/v3/weekly-coach-email` (Sunday 18:00 local)
   - `/api/cron/v3/ingest-sync` (every 4-6h once providers are wired)

---

## What's left on the 34-wave roadmap

Looking at `docs/v3-wave-sequence.md`: every wave **W9 through W42** is now either ✅ merged or 🟡 in-flight (this run shipped them all). The "in-flight" status on most rows in the ledger is because the formal "✅ merged + deployed + verified" verification step was kept light tonight — the merges happened, prod migrations applied, but the per-PR follow-up verifications (manual smoke against deployed routes) are pending.

**Recommended follow-up audit for tomorrow morning:**

1. Verify Vercel auto-builds are still disconnected (the user asked for this earlier; W34 onward hasn't re-enabled it)
2. Set the env vars listed above in Vercel
3. Add the cron schedules to `vercel.json`
4. Run `/api/cron/v3/genome-backfill` once via cURL with `CRON_SECRET` to seed every active player's genome row
5. Open `/golf/dashboard/coachhelm/genome/<playerId>` for a player with 8+ rounds and confirm the radar renders
6. Send a test chat message to `/api/coachhelm/v3/chat/send` and confirm the assistant + tool ledger rows land in `golf_coachhelm_chat_messages`
7. Re-run the full test suite (`npm test -- --run`) and address the 8 pre-existing v2 failures — they predate this run and aren't critical, but the failing dimensions are: `upsert.ts` edge cases (3), `post-round-trigger.ts` failure paths (2), `RoundTakeaway.tsx` link plumbing (1), `development.ts` insert shape (1), `upsert-coach-scoping.ts` cross-coach guard (1)

---

## Files-by-wave appendix (for quick code review)

Generated from `git log main --since="2026-05-25" --name-only --pretty=oneline` after the run — see `git log -p` for full diffs per commit.
