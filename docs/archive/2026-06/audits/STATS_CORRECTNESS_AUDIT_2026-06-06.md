# GolfHelm Stats / Visuals / CoachHelm Correctness Audit — 2026-06-06

Multi-agent audit: 15 dimensions, 50 raw findings, 30 confirmed after adversarial verification.

FALSE POSITIVE (excluded): 'leak-maps cap at 1000' — verified stats-leak-maps.ts:218,275 use .limit(20000).

Already fixed earlier this session: PostgREST 1000-row truncation (23 sites), dashboard KPI windowing, team-page FW% NULL denominator.

## 1. [HIGH] Overall Putt Miss Direction percentages use an inflated denominator (includes multi-putt holes with no recorded miss_direction)
- **Dimension:** putting | **Category:** denominator | **Confidence:** 0.95
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:1795-1808 (denominator), 2240-2245 (output)
- **Expected:** Demo University Golf team (completed rounds), first putts on multi-putt holes with leave>0: denominator should be the 409 rows that HAVE a miss_direction. Correct percentages: Short 123/409=30.1%, Long 60/409=14.7%, Low 134/409=32.8%, High 118/409=28.9%, Left/Right 0%. (left/right tags absent in this data; low/high dominate.)
- **Actual:** Code denominator puttMissTotal = 1006 (all multi-putt first putts with leave>0), but only 409 have a miss_direction. Displayed values are ~41% of true: Short 123/1006=12.2%, Long 60/1006=6.0%, Low 134/1006=13.3%, High 118/1006=11.7% — the six bars sum to ~43% instead of ~100%+, understating every miss bias by ~59%.
- **Fix:** Move the `puttMissTotal++` increment inside the miss_direction guard so the denominator counts only tagged misses, matching the by-break (1831-1832) and approach-miss (1730-1731) patterns. Concretely, at golf-stats-calculator-shots.ts:1795-1808 change:\n\n    if (hole.putts > 1 && hole.firstPuttLeave && hole.firstPuttLeave > 0) {\n      const firstPuttShot = hole.shots.find(s => s.shot_type === 'putting');\n      if (firstPuttShot?.miss_direction) {\n        puttMissTotal++;            // <-- move increment here\n        const pm = firstPuttShot.miss_direction;\n        ... (left/right/short/long/low/high increments unchanged) ...\n      }\n    }\n\nThis makes overall and per-break consistent and restores the intended '% of missed putts' semantics (Short 30.1%, Long 14.7%, Low 32.8%, High 28.9% for the Demo team). No schema change needed.

## 2. [HIGH] Holed shots never detected in any production SG call — `result` argument is never forwarded to calculateShotSG
- **Dimension:** strokes-gained | **Category:** formula | **Confidence:** 0.97
- **Location:** src/lib/coachhelm/v2/shot-analysis/shot-level-sg.ts:284-291 (analyzeShotsByContext); yardage-curves.ts:110-116 (buildYardageCurve); coachhelm-data.ts:693-701 (sgValues); team-category-insights.ts:565,586
- **Expected:** Holed putt example (made 5-footer): baselineStart=green_3-5=1.05, baselineEnd=0 -> SG=+0.05. Nick Rini green-context avgSG (correct, holed=>baselineEnd 0) = -0.180.
- **Actual:** Made 5-footer scored as 1.05-(1+green_0-3=1.00) = -0.95. Nick Rini green-context avgSG (code) = -0.726. team-category-insights.ts maps `result` into ShotData (line 557) but buildYardageCurve/analyzeShotsByContext discard it.
- **Fix:** Forward the `result` argument in all three production callers so detectHoled can fire on result==='hole' (the only holed signal in the DB). Specifically: (1) shot-level-sg.ts analyzeShotsByContext line 284-291 — add `shot.result` as 6th arg; (2) yardage-curves.ts buildYardageCurve line 110-116 — add `shot.result`; (3) coachhelm-data.ts sgValues line 694-701 — add `shot.result`. ShotData already carries `result` (interface line 25) and both DB mappings populate it (coachhelm-data.ts:662, team-category-insights.ts:557), so no schema/query change is needed. Optionally add a regression test that runs analyzeShotsByContext/buildYardageCurve over a fixture containing a holed putt (result='hole', distanceAfter=0, lieAfter='green') and asserts SG = baselineStart - 1 (e.g., tap-in -> 0.0, not -1.0). Consider also defaulting detectHoled to treat lie_before='green' with distance_after=0 as holed only if a holed result is absent, but the minimal, correct fix is simply passing result through.

## 3. [HIGH] Resilience / compounding metrics consume holed-shot-corrupted SG, misclassifying made putts as 'bad shots'
- **Dimension:** strokes-gained | **Category:** consistency | **Confidence:** 0.95
- **Location:** src/app/golf/actions/coachhelm-data.ts:693-704 (feeds sequence-analysis.ts classifySG, BAD_THRESHOLD=-0.5)
- **Expected:** A made 5-foot putt (true SG +0.05) should classify as 'neutral'/'good', not 'bad'. Resilience based on correctly-signed SG.
- **Actual:** Made putts scored ~-0.95 classify as 'bad' (sg < -0.5), corrupting compounding/recovery/resilience.
- **Fix:** At coachhelm-data.ts:693-701, pass the result argument: calculateShotSG(shot.distanceBefore, shot.lieBefore, shot.distanceAfter, shot.lieAfter, sgBaseline, shot.result). This makes detectHoled fire on result==='hole' (which is how 100% of holed shots are encoded), setting baselineEnd=0 so made putts score ~0/positive instead of ~-0.93. Verified effect: made-putt 'bad' classifications drop from 1273/1350 to 0/1350. Apply the identical fix to analyzeShotsByContext (shot-level-sg.ts:284) which has the same omission and feeds the weakness/yardage-curve surface. Add a regression test asserting a made 5ft putt classifies neutral/good, not bad.

## 4. [MEDIUM] Putt miss-direction percentages use an inflated denominator (every 2+ putt hole) while numerators only count putts that recorded a miss_direction — directions don't sum and are systematically understated
- **Dimension:** core-calculator | **Category:** denominator | **Confidence:** 0.92
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:1795-1808, 2240-2245
- **Expected:** Denominator should be the count of missed first putts that have a recorded miss_direction (putts where a direction numerator can actually fire), i.e. increment puttMissTotal only inside the `if (firstPuttShot?.miss_direction)` block. With ~595 directional records vs ~1006 total 2+putt holes, the corrected make-direction percentages would be ~1.7x higher than what the code reports.
- **Actual:** puttMissTotal counts all 2+putt holes (~1006 for the team) but numerators only count the ~595 with miss_direction, so reported percentages are ~40% too low and never sum to 100%.
- **Fix:** Move the puttMissTotal++ at line 1796 inside the `if (firstPuttShot?.miss_direction)` block (lines 1799-1807), so the denominator only counts missed first putts that actually have a recorded direction. After this change, the make-direction percentages will rise to their correct values (e.g. team Short 12.2%->30.1%, Low 13.3%->32.8%). Note: this aligns the overall block with the already-correct puttStatsByBreak block at lines 1831-1836. Separately, since short/long, left/right, low/high are orthogonal axes (and putt data here only ever records short/long/low/high), do not change the UI to imply the six tiles sum to 100% — they are independent miss-tendency rates, which the corrected denominator makes them.

## 5. [MEDIUM] Tee miss-direction % uses (totalDrives - fairwaysHit) as denominator instead of (left+right) misses — percentages understate and never sum to 100%
- **Dimension:** driving | **Category:** denominator | **Confidence:** 0.92
- **Location:** src/app/golf/actions/shot-analytics.ts:344-345
- **Expected:** For Demo University Golf (completed rounds), leftMisses=230, rightMisses=165 → correct leftMissPct = 230/395 = 58.2%, rightMissPct = 165/395 = 41.8% (sum 100%). This matches the calculator's missLeftPct/missRightPct (~58.3/41.7).
- **Actual:** Denominator = totalDrives - fairwaysHit = 1071 - 649 = 422. leftMissPct = 230/422 = 54.5%, rightMissPct = 165/422 = 39.1% (sum only 93.6%). Both understated by ~3-4 points; the gap widens as null-fairway / direction-less misses grow.
- **Fix:** In shot-analytics.ts, change the miss-direction denominator from the missed-fairway count to the directional-miss count, mirroring golf-stats-calculator-shots.ts:2108-2110. e.g. const directionalMisses = leftMisses + rightMisses; then leftMissPct: calculatePercentage(leftMisses, directionalMisses), rightMissPct: calculatePercentage(rightMisses, directionalMisses). calculatePercentage already returns 0 when total===0, so direction-less players are handled. The two bars will then sum to 100% (modulo rounding) and correctly express the L/R tendency, matching the player-stats calculator and the bar-width rendering in ShotTypeBreakdown.tsx.

## 6. [MEDIUM] Player Driving tab Fairway % counts every par-4/5 hole (incl. NULL fairway_hit) as an opportunity, disagreeing with the team page and the ground-truth FW formula
- **Dimension:** driving | **Category:** consistency | **Confidence:** 0.9
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:1639-1641, 2101
- **Expected:** Owen Carter expected FW% = 110/167 (non-null) = 65.9% (matches prompt ground truth and team page). Team expected = 649/1050 = 61.8%.
- **Actual:** Calculator/Driving tab: Owen Carter = 110/168 = 65.5%; team = 649/1071 = 60.6%. Off by ~0.4-1.2 pts here and grows with the count of NULL-fairway par-4/5 holes (21 team-wide).
- **Fix:** In golf-stats-calculator-shots.ts, exclude NULL fairway_hit from the fairway denominator so it matches the post-fix-#3 team page and the ground-truth formula. Change the line-1639 guard from `if (hole.par !== 3)` to `if (hole.par !== 3 && hole.fairwayHit !== null)` (so fairwayOpportunities, fairwaysPar4/5.total, fairwaysDriver/NonDriver.total all count only holes with a known tee result). Keep the numerator (`if (hole.fairwayHit) fairwaysHit++`) as-is. Apply the same NULL-exclusion to shot-analytics.ts:313-314 (`const par4And5Holes = holes.filter(h => h.par >= 4 && h.fairway_hit !== null)`) so the CoachHelm tee stats agree as well. Add a calculator unit test with a par-4/5 hole carrying NULL fairway_hit asserting it is excluded from fairwayOpportunities. On the Demo data this brings the Driving tab to Owen 65.9% / team 61.8%, matching the team page and prompt ground truth. Alternatively (less preferred), if the every-par-4/5-is-an-opportunity convention is the intended single source of truth, revert fix #3 instead — but that would contradict the prompt's stated ground-truth formula, so converging on NULL-exclusion is the correct direction.

## 7. [MEDIUM] GIR-by-distance and Proximity-by-distance assign boundary yardages to DIFFERENT bands (same Approach tab, inconsistent edges)
- **Dimension:** approach | **Category:** boundary | **Confidence:** 0.95
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:1698-1705 vs 499-511
- **Expected:** Both distance breakdowns should use ONE bucketing function with consistent edges so a given approach yardage lands in the same labeled band everywhere. SQL over Demo University (status=completed): 185 approach shots sit on exact boundary yardages (75:20, 100:40, 125:31, 150:40, 175:16, 200:31, 225:7). For Nick Rini alone, 20 GIR-attempt approaches land in a different band per scheme (e.g. his 4 approaches from exactly 100 yd -> GIR band 100_125, proximity band 75_100).
- **Actual:** GIR-by-distance uses inline half-open `[lo,hi)` edges (1698-1705); proximity/efficiency/miss-by-band use `getApproachDistanceBucket` closed-upper `(lo,hi]` edges (499-511). Boundary-valued approaches are shifted one band between the two charts.
- **Fix:** Replace the inline half-open bucketing at golf-stats-calculator-shots.ts:1698-1705 with a call to getApproachDistanceBucket(distYards) so GIR-by-distance uses the exact same edges as proximity/efficiency/miss-by-band. This also fixes the first-band domain mismatch: rename the GIR output field girPct50_75 (and the UI label '50-75 yards' in ApproachStats.tsx:60 and FairwayStatsCockpit.tsx:1478/1775) to '30-75 yards'/girPct30_75 so the 30-50yd approaches are no longer dropped and the band label matches the proximity row. If a single canonical bucketer is undesirable, at minimum align the edge convention (both closed-upper or both half-open) and the lowest-band start (both 30 or both 50), then update the three UI label sites and the *Stats type field name to match. Add a unit test asserting getApproachDistanceBucket and the GIR bucketer agree on the set {75,100,125,150,175,200,225}.

## 8. [MEDIUM] Approach miss-direction % denominator differs between player Stats tab and player CoachHelm (GIR-miss-only vs all-shots-with-miss)
- **Dimension:** approach | **Category:** denominator | **Confidence:** 0.88
- **Location:** src/app/golf/actions/shot-analytics.ts:366-377
- **Expected:** Both surfaces should use the same definition of an approach miss (missed-green approaches only) and the same per-hole granularity, so the miss-direction distribution and the 'Based on N missed greens' count agree across the Stats tab and CoachHelm.
- **Actual:** shot-analytics.ts denominator = count of all approach shots with any miss_direction (no green filter, multiple shots/hole); calculator denominator = missed-green holes only (one GIR-attempt shot/hole). Percentages and counts diverge between the two player surfaces.
- **Fix:** Make the CoachHelm approach-miss denominator and granularity match the Stats-tab calculator: count one miss per missed-green hole using the GIR-attempt approach shot, not every approach/iron shot. Concretely, in shot-analytics.ts replace the allApproachMisses = approachShots.filter(s => s.miss_direction) per-shot accumulation (lines 366-377) with a per-hole pass that, for each hole where gir !== true, selects the GIR-attempt shot (shot_number === max(par-2,1), with the par-5 earlier-green-finder reassignment) and only counts its miss_direction when that shot did NOT find the green — mirroring golf-stats-calculator-shots.ts:947 and :1730. Ideally have CoachHelm consume the shared calculator output (approachMissTotal + approachMiss*Pct) so the 'Based on N missed greens' count and the distribution are identical across both player surfaces. Add a cross-surface test asserting the two denominators agree for a fixture player.

## 9. [MEDIUM] Sand Save % uses 3 different definitions across surfaces — shot-analytics denominator includes made-GIR holes
- **Dimension:** scrambling | **Category:** denominator | **Confidence:** 0.9
- **Location:** src/app/golf/actions/shot-analytics.ts:414-415, 432
- **Expected:** Greenside sand-save attempts should be restricted to missed-GIR holes. For the Demo team the stored column yields 83 missed-GIR bunker holes with 11 saves (gir=false,sand_save=true=11; gir=false,sand_save=false=72) => 11/83 = 13.3%; the shot calculator yields 16/84 = 19.0%. shot-analytics.ts should at minimum exclude GIR holes from sandSaveAttempts (filter h.sand_save !== null && h.gir === false) and ideally share ONE sand-save definition with the calculator so all surfaces agree.
- **Actual:** sandSaveAttempts = holes.filter(h => h.sand_save !== null).length = 112 (includes 29 gir=true holes); sandSavesMade = 20 (includes 9 made on GIR holes); sandSavePct = 20/112 = 17.9% — different from the 19.0% shown on the Stats page for the same data.
- **Fix:** In shot-analytics.ts:414-415, gate sand-save attempts on missed greens to match every other code path: sandSaveAttempts = holes.filter(h => h.sand_save !== null && h.gir === false).length and sandSavesMade = holes.filter(h => h.sand_save === true && h.gir === false).length. Better still, share ONE definition: have shot-analytics reuse the shot-derived sand-save logic (golf-stats-calculator-shots.ts:994) or round-review-system.ts:467 so Player CoachHelm, the Stats page, the profile, and FairwayStatsCockpit all report identical Sand Save %. Note this surfaces an upstream seeder/write anomaly too: golf.ts:1208 writes sand_save = sandSaveAttempt ? sandSaveMade : null, yet 29 GIR-true Demo holes carry a non-null sand_save — a sand save cannot occur on a hole hit in regulation, so the seeder (and/or the round-entry capture that lets sandSaveAttempt be set on a GIR hole) should also be corrected.

## 10. [MEDIUM] aroundGreenShot selection does not exclude 'approach' shots — long fairway-bunker/approach shots mislabeled as greenside sand saves and corrupt scramble-by-lie split
- **Dimension:** scrambling | **Category:** other | **Confidence:** 0.85
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:970-995
- **Expected:** The greenside up-and-down shot should be the chip/pitch/around_green shot, not a full approach. Excluding shot_type==='approach' from the generic around-green branch (or preferring the LAST qualifying around_green shot over an earlier long approach) yields ground-truthed sand-save attempts of 77 (around_green-from-sand) rather than 84, and removes 7 fairway-bunker-approach holes from the sand split; the scramble-by-lie 'sand' total drops from 84 to ~77 and those 7 holes move to their true greenside lie.
- **Actual:** aroundGreenShot can be a 42-50yd 'approach' shot. sandSaveAttempt/chipLie computed from it: e.g. demo hole 14 counted as a sand-save attempt with chipLie='sand' though the real scramble shot was a fairway chip; team sand-save attempts = 84 (7 attributable to approach-from-sand shots, not greenside bunker shots).
- **Fix:** In the generic around-green branch at src/lib/utils/golf-stats-calculator-shots.ts:974-981, exclude shot_type==='approach' so a long approach shot hit from <=50yd is never selected as the up-and-down shot: add `s.shot_type !== 'approach'` to the conditions (alongside the existing putting/tee/penalty exclusions). For robustness prefer the actual greenside shot: select the LAST qualifying around_green shot before the first green-hit, rather than the first .find() match, so an earlier approach-from-sand can never override a genuine later chip. Mirror the same exclusion in the scramble-distance fallback at lines 1921-1929, which has the identical loose predicate. Add a regression test: par-5 with shot#3 approach FROM sand @40yd then shot#4 around_green chip FROM fairway -> assert chipLie='fairway' and sandSaveAttempt=false.

## 11. [MEDIUM] Yardage curve blends putt distances (feet) into yard buckets, mixing putts with full shots
- **Dimension:** strokes-gained | **Category:** unit | **Confidence:** 0.85
- **Location:** src/lib/coachhelm/v2/shot-analysis/yardage-curves.ts:98-127, 155 (label 'y' at ShotAnalysisCard.tsx:154-156)
- **Expected:** Yardage curve should contain only non-green (tee/fairway/rough/sand) shots whose distanceBefore is genuinely in yards; putts belong in a separate feet-based putting curve. Low-yardage buckets should reflect ~98 real short-game shots, not ~431 putts.
- **Actual:** Green putts (feet) are floor-divided by 25 yards and merged into 0-25y/25-50y buckets; proximity for those rows is also wrong (see finding #3). Surfaces as red/dead-zone bars on player ShotAnalysisCard and coach TeamShotOverview.
- **Fix:** In buildYardageCurve (or at the call sites in coachhelm-data.ts:678 and team-category-insights.ts:565), exclude shots with lieBefore==='green' before bucketing so the yardage curve contains only tee/fairway/rough/sand/recovery shots whose distanceBefore is genuinely yards. Putting performance should be shown via the existing feet-based green buckets (getGreenDistanceBucket / analyzeShotsByContext green_* contexts), not merged into yard buckets. After the fix the "0-25y" bucket reflects ~98 real short-game shots for Nick Rini instead of ~529 mixed rows, and the "y" label becomes truthful. Optionally also drop or relabel the unused avgProximity (which applies yardsToFeet×3 to distanceAfter regardless of lie) since it is currently dead code that would be wrong if ever surfaced for putts.

## 12. [MEDIUM] Player dashboard GIR% uses unweighted mean and Putts/Rd uses RAW (non-18-normalized) mean — disagrees with player stats page, coach dashboard, and team page
- **Dimension:** dashboard | **Category:** normalization | **Confidence:** 0.95
- **Location:** src/app/golf/actions/dashboard-data.ts:836-841
- **Expected:** For Nick Rini (15 completed rounds, 1 nine-hole round), grounded from SQL over the latest 20: Putts/Rd normalized-to-18 = 32.9 (matches stats page); GIR% weighted = 64.8.
- **Actual:** Player dashboard renders Putts/Rd = 31.8 (raw mean, ~1.1 putts low because the 9-hole round is counted un-normalized) and GIR% = 64.1 (unweighted per-round mean) — both inconsistent with the 32.9 / 64.8 the player sees on the stats page and the coach sees on the dashboard for the same player.
- **Fix:** In src/app/golf/actions/dashboard-data.ts, replace the player-dashboard KPI block (lines 836-841) to mirror the coach-dashboard fix: (1) GIR% — compute weighted aggregate sum(total_gir)/sum(total_gir_possible)*100 over the round set instead of averaging per-round percentages; (2) Putts/Rd — normalize each round to 18 holes before averaging (hp>0 && hp<18 ? total_putts*18/hp : total_putts), matching the normalizedPutts logic at lines 532-540 and stats.ts lines 603-613. Reuse the same window basis the team page/coach dashboard use for consistency. The putts normalization is the higher-impact correction (~1.1 putts); the GIR weighting is the smaller (~0.7 pt) but same-class fix. Consider extracting a shared helper so all four surfaces (player hub, player stats page, coach dashboard, team page) cannot drift again.

## 13. [MEDIUM] scrambling_rate excludes ALL bunker/sand recoveries (lie filter uses non-existent values)
- **Dimension:** genome | **Category:** denominator | **Confidence:** 0.95
- **Location:** src/lib/coachhelm/v3/genome/dimensions/scrambling-rate.ts:31-34
- **Expected:** Filter lie_before IN ('rough','sand') (the canonical recovery lies, matching hole-sequence-loader.ts:99). Corrected sand-inclusive, unit-corrected save rates: Ethan 0.563, Jackson 0.576, Mason 0.444, Nick 0.576, Owen 0.462, Tyler 0.614.
- **Actual:** Only 'rough' lies are counted (the other three literals match 0 rows). Stored genome vectors confirm the inflated rates: Ethan 0.674, Jackson 0.636, Mason 0.667, Nick 0.70, Owen 0.645, Tyler 0.697 — all six labeled 'Wizard'. Corrected, Mason (0.444) and Owen (0.462) are only 'Reliable', and Nick falsely clears the 0.7 persona-strength threshold (corrected 0.576).
- **Fix:** Change the lie filter at scrambling-rate.ts:31-34 to the two canonical recovery lies, matching the already-fixed sibling loader: s.lie_before === 'rough' || s.lie_before === 'sand' (drop the three dead literals 'heavy_rough'/'light_rough'/'bunker'). Separately (and ideally in the same pass, since it materially changes the save count), normalize the save proximity to feet before comparing to SAVE_PROXIMITY_FT: convert distance_to_hole_after to feet when distance_unit_after === 'yards' (×3) rather than comparing the raw mixed-unit number to 10. Then recompute/refresh the stored golf_player_genome vectors so personas and labels reflect the corrected, sand-inclusive, unit-normalized rates.

## 14. [MEDIUM] scrambling_rate blends yards into feet (10 ft save threshold applied to yards-unit rows)
- **Dimension:** genome | **Category:** unit | **Confidence:** 0.9
- **Location:** src/lib/coachhelm/v3/genome/dimensions/scrambling-rate.ts:35-43
- **Expected:** Convert yards rows to feet (×3) before the <=10 ft comparison (or fetch distance_unit_after and gate on it). Rough-only unit-corrected rates: Ethan 0.558, Jackson 0.615, Mason 0.467, Nick 0.615, Owen 0.645, Tyler 0.667.
- **Actual:** All distance_to_hole_after values are compared against 10 as if feet; yards-unit short misses are counted as made-saves, inflating the rate. Combined with the missing-sand defect, every Demo player lands at 'Wizard' (0.636-0.70).
- **Fix:** Add distance_unit_after to the orchestrator golf_shots select (orchestrator.ts:79) and to the GenomeShot type (types.ts), then in scrambling-rate.ts convert to feet before the threshold compare: const feet = s.distance_unit_after === 'yards' ? (s.distance_to_hole_after ?? Infinity) * 3 : (s.distance_to_hole_after ?? Infinity); count save when feet <= SAVE_PROXIMITY_FT. Mirror the unit-aware handling already used in shot-analytics. This drops Rivers from 0.667 to 0.467 (Wizard->Reliable) and corrects the other five.

## 15. [MEDIUM] short_side_scrambling_chain composite proximity blends yards into feet (wrong displayed ft + under-fires its 15 ft gate)
- **Dimension:** genome | **Category:** unit | **Confidence:** 0.92
- **Location:** src/lib/coachhelm/v3/composite/rules/short-side-scrambling-chain.ts:29-31
- **Expected:** Average distance_to_hole_after with yards rows converted to feet (×3), or select+gate on distance_unit_after. Unit-corrected avg post-shot proximity (rough+sand, dist_before<=40): Ethan 12.9, Jackson 12.5, Mason 15.0, Nick 12.5, Owen 17.0, Tyler 12.0 ft.
- **Actual:** Raw feet-assumed averages: Ethan 9.3, Jackson 10.8, Mason 11.4, Nick 10.8, Owen 12.5, Tyler 10.8 ft — systematically understated, so the rule under-fires (suppressed below its 15 ft threshold) and prints understated proximity when it does fire.
- **Fix:** In loadShortGameShots (hole-sequence-loader.ts) add distance_unit_after to the .select(...) at line 84-85, and add distance_unit_after to the ShortGameShot type (composite/types.ts:93-99). Then in short-side-scrambling-chain.ts:29 convert before summing, e.g. const ft = s.distance_unit_after === 'yards' ? s.distance_to_hole_after * 3 : s.distance_to_hole_after; (reuse the existing toYards/feet helper used elsewhere in coachhelm). The 15 ft gate and the `{avgProximity} ft` prose then evaluate on true feet. Add a unit comment to distance_to_hole_after in the type to match the `// yards` label already on distance_to_hole_before, mirroring shot-source.ts:26-27.

## 16. [MEDIUM] Putt + approach leak-map queries cap at PostgREST's 1000-row server limit, not .limit(20000) — team buckets silently truncated and undersized
- **Dimension:** leak-maps | **Category:** denominator | **Confidence:** 0.88
- **Location:** src/app/golf/actions/stats-leak-maps.ts:212-218 (buildPuttBuckets) and 263-275 (buildApproachBuckets)
- **Expected:** All gradeable putt rows counted. Demo team 5-10 ft band: sample_n=390 (make% 41.5). Full team putting sample = 2475 gradeable putts. Approach 125-175 band over its full population. Achieved by paginating with fetchAllRows() + a stable .order('id') (per fetch-all-rows.ts contract) or chunking .in(round_id) into <1000-row batches.
- **Actual:** Truncated to the first 1000 rows returned by PostgREST. Demo team 5-10 ft band reports sample_n=154 instead of 390 (a 60% undercount); only 1000 of 2510 putt rows are aggregated. Across all teams, max team putt footprint is 2510 rows and max approach footprint is 1630 rows (2 teams exceed 1000 putts, 2 exceed 1000 approaches), so multiple teams are affected. Single-player surfaces (getPuttMakeLeakMap etc.) stay under 1000 and are unaffected; the defect is team-level (getTeamLeakMaps).
- **Fix:** In buildPuttBuckets (and, defensively, buildApproachBuckets) replace the single un-paginated `.in('round_id', roundIds).limit(MAX_SHOT_ROWS)` with the repo's existing pager: fetchAllRows<PuttRow>((from,to) => supabase.from('golf_shots').select('round_id, putt_distance_feet, putt_made').in('round_id', roundIds).eq('shot_type','putting').not('putt_distance_feet','is',null).order('id',{ascending:true}).range(from,to)). The STABLE `.order('id')` is mandatory per the fetch-all-rows.ts contract so page boundaries don't drift. Alternatively chunk roundIds into batches small enough that each chunk's result stays under 1000 rows (mirrors admin-tracer-data.ts). Add a regression assertion that the Demo team's 5-10 ft putt band reports sample_n=390. Approach can keep the single query at current data volumes (max 876 < 1000) but should be migrated to the pager for consistency and future-proofing.

## 17. [MEDIUM] avgPuttsPerRound has no 18-hole normalization — 9-hole rounds depress the average and distort the Putts/Round trend
- **Dimension:** player-profile | **Category:** normalization | **Confidence:** 0.95
- **Location:** src/app/golf/actions/shot-analytics.ts:499-501, 620-621
- **Expected:** Normalize each round's putts to 18 holes before averaging (putts * 18/holes_played), matching dashboard-data.ts and the personal stats page. Ground truth specifies putts/rd normalized to 18 holes (team 32.8 normalized vs 32.2 raw).
- **Actual:** Raw totalPutts / rounds.length with 9-hole rounds counted as full rounds. For Owen's 30-day window all 7 rounds happen to be 18-hole (31.7 correct there), but the formula is wrong for any window containing a 9-hole round.
- **Fix:** Normalize each round's putts to an 18-hole equivalent before averaging, and join holes_played (or count holes) into the rounds query. Concretely: (1) add holes_played to the roundsData and previousRoundsData selects (lines 258, 269), or count holes per round_id from the holes already fetched; (2) replace avgPuttsPerRound (499-501) with the mean of per-round (total_putts * 18 / holes_played) for holes_played in (1..18), guarding holes_played > 0; (3) replace prevPuttsPerRound (620-621) with the same per-round 18-normalized mean over previousRounds. This matches dashboard-data.ts:530-539 and the personal stats page, eliminating both the deflated KPI and the distorted trend. Optionally enforce holes_played > 0 to avoid divide-by-zero on malformed rounds.

## 18. [MEDIUM] AroundGreen totalShots double-counts: adds around-green shot count to up-and-down hole count
- **Dimension:** player-profile | **Category:** denominator | **Confidence:** 0.92
- **Location:** src/app/golf/actions/shot-analytics.ts:430
- **Expected:** A single coherent count — either upAndDownAttempts (holes where up_and_down is not null) or aroundGreenShots.length, not their sum. For Owen 30-day: atg shots=44, up-and-down attempts=50 (real scrambling situations ~50).
- **Actual:** 44 + 50 = 94 reported as totalShots.
- **Fix:** Replace `totalShots: aroundGreenShots.length + upAndDownAttempts` (line 430) with a single coherent count. Best choice for a "shots" label is the shot-level count, `totalShots: aroundGreenShots.length` (Owen: 44), which matches the "X shots" wording in ShotTypeBreakdown.tsx:295. If the intent is to report scrambling situations, use `upAndDownAttempts` (50) and relabel the UI to "attempts/situations." Either way, do not add the two — they overlap and are dimensionally mismatched. After the fix, re-check the two insight gates (lines 694, 703) still use a sensible threshold against the chosen single count.

## 19. [LOW] GIR-by-distance band boundaries are upper-EXCLUSIVE while approach-proximity / efficiency / miss bands are upper-INCLUSIVE — the same boundary-distance shot lands in different bands across the approach surface
- **Dimension:** core-calculator | **Category:** boundary | **Confidence:** 0.9
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:1698-1705 (inline GIR bucketer) vs 499-511 (getApproachDistanceBucket)
- **Expected:** A single shared bucketer (getApproachDistanceBucket) should drive ALL distance-banded approach stats (GIR, proximity, efficiency, miss). Demo University Golf has 158 approach shots landing exactly on boundary distances (75:20, 100:40, 125:31, 150:40, 175:16, 200:31, 225:7) that bucket differently between GIR and proximity, plus 58 approaches at 30-50yd that appear in proximity bands but are dropped from GIR-by-distance entirely (SQL-confirmed).
- **Actual:** Two independent bucketers with mismatched edge inclusivity (exclusive-upper inline for GIR vs inclusive-upper getApproachDistanceBucket for everything else) and mismatched lower bounds (50 vs 30), so the same approach distance is attributed to different bands across co-displayed stats.
- **Fix:** Route the GIR-by-distance bucketing through the shared getApproachDistanceBucket so all distance-banded approach stats use one consistent boundary scheme. Replace the inline if/else block at golf-stats-calculator-shots.ts:1696-1705 with `const bucket = getApproachDistanceBucket(hole.approachDistance);` and key girByDistance by the returned label (the GIR result is then assigned to girByDistance[bucket]). Reconcile the type fields/labels so GIR uses the same band keys as proximity (rename girPct50_75 -> a single shared lowest band, or relabel the proximity/efficiency '30-75' UI string to '50-75' to match the true 50yd lower bound so the cosmetic label mismatch also disappears). Keep the AROUND_GREEN_THRESHOLD_YARDS=50 lower cutoff (both schemes already share it — no change needed there). Add a unit test asserting that an approach at each integer boundary (75,100,125,150,175,200,225) lands in the SAME band for GIR, proximity, and efficiency. Impact is bounded (only exact-integer-boundary shots shift, ~1-3 pt swings), so this is a correctness/consistency cleanup rather than a high-severity miscalc.

## 20. [LOW] drivingDistanceAvg includes shot_distance = 0 tee shots (data artifacts), creating a small cross-surface gap with shot-analytics which filters > 0
- **Dimension:** driving | **Category:** boundary | **Confidence:** 0.9
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:1626-1633, 2087-2090
- **Expected:** Excluding the 3 zero-distance tee shots, team avg drive = 284.69 yards (shot-analytics value).
- **Actual:** Calculator drivingDistanceAvg includes the 3 zeros → 283.87 yards. ~0.8 yard discrepancy between the Driving tab and the CoachHelm analytics panel for the same data; larger for players with more zero/placeholder tee entries.
- **Fix:** In golf-stats-calculator-shots.ts, change the line 1626 gate from `if (hole.drivingDistance !== null)` to `if (hole.drivingDistance !== null && hole.drivingDistance > 0)` so all three accumulators (drivingDistances, drivingDistancesDriverOnly, drivingDistancesNonDriverOnly) exclude 0-yard placeholder tee shots, matching shot-analytics.ts:339. This collapses the cross-surface gap to zero (team 284.69; Owen 267.88 on both surfaces). Optionally do the same upstream at line 879 by treating shot_distance===0 as null when deriving hole.drivingDistance, but the line-1626 guard is the minimal, targeted fix and keeps the driver/non-driver splits consistent. The DrivingStats.tsx:22 falsy guard (stats.drivingDistanceAvg ? ...) needs no change once 0s are excluded.

## 21. [LOW] Proximity/efficiency '30-75 yds' band actually contains only 50-75 yd shots (floor mislabeled)
- **Dimension:** approach | **Category:** boundary | **Confidence:** 0.85
- **Location:** src/components/golf/stats/sections/ApproachStats.tsx:112, 141
- **Expected:** Label the band '50-75 yards' (its actual content) to match the GIR-by-distance row and the 50-yd around-green threshold. SQL: 58 team approach shots are <50 yd; these never populate the '30-75' row despite the label implying they would.
- **Actual:** UI label says '30-75 yards'/'30-75 yds' but the band only ever holds 50-75 yd shots (floor is 50 via AROUND_GREEN_THRESHOLD_YARDS and the >=50 efficiency gate).
- **Fix:** In ApproachStats.tsx change the proximity-by-distance row label (line 112) from "30-75 yards" to "50-75 yards" and the efficiency-by-distance row label (line 141) from "30-75 yds" to "50-75 yds", so they match the band's true 50-yd floor and the GIR-by-distance section's "50-75 yards" label (line 60). Additionally, reconcile the upper-edge convention: the proximity/efficiency bucketer (getApproachDistanceBucket, line 503) uses dist<=75 while the GIR inline bucketer (line 1698) uses dist<75 — pick one boundary rule for all three sections so a 75-yd shot lands in the same band everywhere (recommend a single shared bucketing helper to eliminate the divergence). No numeric recompute is required; the underlying aggregates are correct.

## 22. [LOW] firstPuttDistance uses normalizeToFeet (×3 on 'yards' rows) instead of normalizePuttFeet — latent unit/clamp bug for putts mis-tagged as yards
- **Dimension:** putting | **Category:** unit | **Confidence:** 0.82
- **Location:** src/lib/utils/golf-stats-calculator-shots.ts:953-954
- **Expected:** firstPuttDistance should use normalizePuttFeet(firstPutt.distance_to_hole_before) (treat raw value as feet, clamp to 120), matching the documented putt convention and the leak-map's putt_distance_feet source.
- **Actual:** Uses normalizeToFeet(...), which ×3's 'yards'-tagged putt rows and never clamps; benign on current Demo data only because putt_distance_feet was stored as before×3.
- **Fix:** Replace the normalizeToFeet calls in the firstPutt block with the documented putt convention so the per-hole stats path matches the SG path and the write-path. At golf-stats-calculator-shots.ts:953-955 use firstPuttDistance = (firstPutt && firstPutt.distance_to_hole_before !== null) ? normalizePuttFeet(firstPutt.distance_to_hole_before) : null, and at 956-958 use firstPuttLeave = (firstPutt && firstPutt.result !== 'hole' && firstPutt.distance_to_hole_after !== null) ? normalizePuttFeet(firstPutt.distance_to_hole_after) : null (putt leaves are also on the green, so feet-no-convert-clamp applies). Apply the identical change to the second instance at shot-helpers.ts:252-254 and 258-260 (treat raw as feet, clamp to 120). This treats raw putt values as feet regardless of unit and clamps to MAX_PUTT_FEET, eliminating the 390-ft 'putt', removing the misbanding of the 13/25-yd rows, and bringing firstPuttDistanceAvg / make%-by-band / efficiency-by-band into agreement with the SG and leak-map paths. Note: the stored putt_distance_feet seed values (before×3) are themselves stale-by-convention; if any surface reads putt_distance_feet directly it should be re-derived via derivePuttDistanceFeet, but that is out of scope for this fix.

## 23. [LOW] Yardage-curve avgProximity multiplies putt distances (already feet) by 3 again
- **Dimension:** strokes-gained | **Category:** unit | **Confidence:** 0.9
- **Location:** src/lib/coachhelm/v2/shot-analysis/yardage-curves.ts:49-51, 118, 136-137
- **Expected:** Proximity for a shot whose distanceAfter is already feet (putts) should be distanceAfter, not distanceAfter*3. Non-green shots whose distanceAfter is yards should be *3.
- **Actual:** All distanceAfter values are *3, so any putt's proximity contribution is 3x inflated.
- **Fix:** In yardage-curves.ts:118, stop unconditionally tripling distanceAfter. distanceAfter is already in the correct unit per coachhelm-data.ts: feet when the shot finishes on the green, yards otherwise. Since avgProximity is documented as feet, convert only the non-green case: `const proxFeet = shot.lieAfter === 'green' ? shot.distanceAfter : yardsToFeet(shot.distanceAfter); entry.proximities.push(proxFeet);`. (Holed shots whose distanceAfter is 0 are fine either way.) Add a unit test asserting a putt finishing at 3 ft contributes 3, not 9, to avgProximity, and an approach finishing at 15 ft on the green contributes 15, not 45. Optionally drop the field entirely since nothing consumes it, but fixing it is safer for future readers given the "feet" contract in the type comment.

## 24. [LOW] Team scoring trend buckets rounds by LOCAL-time month parse → date-only rounds on the 1st misattributed to the prior month (TZ-dependent off-by-one)
- **Dimension:** dashboard | **Category:** boundary | **Confidence:** 0.93
- **Location:** src/app/golf/actions/dashboard-data.ts:478-489
- **Expected:** Demo University Golf has 5 completed rounds dated 2026-05-01. Correct (UTC) monthly buckets: Apr 2026 = 35 rounds (avg 74.9), May 2026 = 42 rounds (avg 76.5), Jun 2026 = 1 round (70.0). Bucketing must be timezone-agnostic for a date-only column (parse the YYYY-MM substring directly, or force UTC).
- **Actual:** When the server process runs in a timezone west of UTC (e.g. America/New_York), the 5 May-1 rounds shift into April: April becomes 40 rounds and May 37, and each month's plotted average shifts accordingly. Output is environment-dependent (correct only when the runtime TZ is UTC, which Vercel Node happens to default to but is not guaranteed and is not true in local/dev/other deploys).
- **Fix:** Make the month bucketing timezone-agnostic for the date-only column. Simplest: parse the YYYY-MM substring directly instead of constructing a Date — e.g. `const [y, m] = round.round_date.slice(0,7).split('-'); const sortKey = `${y}-${m}`;` and derive the label from `new Date(`${round.round_date}T00:00:00Z`)` with `toLocaleString('default', {month:'short', year:'2-digit', timeZone:'UTC'})`, or simply build the label from a fixed month-name array indexed by `Number(m)-1`. Either approach removes the local-timezone dependence and aligns the trend chart with the UTC-pinned dateCutoff window and shortDate convention used elsewhere.

## 25. [LOW] DirectionalSummary renders avgRemainingDistance as "avg leave" for the Driving family, where distanceAfter is the full remaining distance to the hole, not a leave
- **Dimension:** spray-charts | **Category:** consistency | **Confidence:** 0.83
- **Location:** src/components/golf/stats/sections/DispersionStats.tsx:360-364
- **Expected:** For family==='driving' the per-sector secondary distance should be avgForwardDistance (~284 yd, the tee-shot distance) labeled e.g. 'avg distance'; for approach it should remain avgRemainingDistance (~26.8 yd) labeled 'avg leave'. SQL over the Demo team par>=4 tee shots: avg(shot_distance)=283.9 yd (forward) vs avg(distance_to_hole_after)=~155 yd (remaining).
- **Actual:** Both families display band.avgRemainingDistance with the fixed label 'avg leave'; driving sectors therefore show the ~155-yd remaining-to-hole value, never the 284-yd forward distance surfaced elsewhere.
- **Fix:** In DirectionalSummary (DispersionStats.tsx:360-364), branch on the shot family like the rest of the component does. For family==='driving', render `band.avgForwardDistance` with a label such as "avg distance" (the ~284y tee-shot distance shown everywhere else for driving); for family==='approach', keep `band.avgRemainingDistance` labeled "avg leave" (~26.8y). DirectionalSummary currently does not receive `family`, so thread it through (it is available in the parent at the `family` state and the call site is line 531). Concretely: const secondary = group.family === 'driving' ? band.avgForwardDistance : band.avgRemainingDistance; const secondaryLabel = group.family === 'driving' ? 'avg distance' : 'avg leave'; then guard on `secondary !== null`. group.family is already on SprayChartShotGroup, so no new prop is strictly needed.

## 26. [LOW] Approach dispersion 'radius' compares std-dev of remaining distance-to-hole against a lateral-dispersion benchmark (apples-to-oranges + mixed on/off-green population)
- **Dimension:** coachhelm-patterns | **Category:** other | **Confidence:** 0.82
- **Location:** src/lib/coachhelm/v2/mining/lie-specific-analysis.ts:1955-2034 (calculateDispersionPatternForShots stdDeviation) + 2112-2133 (generateBenchmarkComparisons) + 398-409 (DISPERSION_BENCHMARKS.approach)
- **Expected:** Dispersion radius should be computed from only off-green misses (or from a true directional spread), and the benchmark it is compared against must measure the same thing. For Owen Carter's 150-200y bracket (n=84): std-dev of distance_to_hole_after over all shots = 24.4yd (mean 21.3yd), but this number conflates 'hit the green and stopped close' with 'missed and ended 50yd away'. A defensible dispersion metric over the missed-green subset only would be a different value.
- **Actual:** stdDeviation = sqrt(variance of distance_to_hole_after across all bracket shots, on-green + off-green), ~24.4yd for that bracket, then compared to a 32yd 'dispersionRadius' benchmark and turned into percentileRank = 50 - (differential/benchmark)*50.
- **Fix:** Make the dispersion radius measure a single, well-defined quantity and compare it against a benchmark of the same quantity. Two concrete options: (a) Compute stdDeviation/radius over off-green misses only (s.result NOT IN ('green','hole')) — mirroring the existing on-green guard in calculateAvgProximity (822-823) and analyzeApproachByBracket (1355-1357) — and re-baseline DISPERSION_BENCHMARKS.approach.dispersionRadius to represent average miss-distance spread of college players; or (b) replace remaining-distance std-dev with a true directional/lateral spread metric (e.g., from miss_direction + leftDispersion/rightDispersion/shortDispersion/longDispersion already computed at 2023-2026) so it actually measures shot dispersion. In either case keep the benchmark semantically matched to whatever the player metric now measures, and recompute percentileRank from that. As a minimal interim fix, at least exclude on-green finishers from the std-dev so the value stops being a function of GIR rate.

## 27. [LOW] correlation-discovery fairway% uses hardcoded /14 opportunities instead of the per-round total_fairways column that is loaded on the round
- **Dimension:** coachhelm-patterns | **Category:** denominator | **Confidence:** 0.92
- **Location:** src/lib/coachhelm/v2/mining/correlation-discovery.ts:444-453 (STANDARD_FAIRWAY_OPPORTUNITIES = 14; fairwayPcts = total_fairways_hit / 14 * 100)
- **Expected:** fairwayPct = total_fairways_hit / total_fairways * 100 (per-round denominator). Demo team avg total_fairways = 13.7, so a player on a 12-fairway course at 8 hits should read 66.7%, not 8/14=57.1%.
- **Actual:** fairwayPct = total_fairways_hit / 14 * 100, ignoring the loaded-but-unused real denominator; correlation is barely affected but the displayed avg fairway% and the <50% threshold split can be wrong on non-14-fairway courses.
- **Fix:** In correlation-discovery.ts: (1) add total_fairways to the golf_rounds SELECT (lines 187-201) and to RoundCorrelationData (lines 51-60) plus the mapping at 240-249; (2) replace the hardcoded denominator at 451-453 with a per-round one: fairwayPct = ((r.total_fairways_hit ?? 0) / ((r.total_fairways ?? 0) > 0 ? r.total_fairways : 14)) * 100, mirroring orchestrator.ts:344 and outcome-validator.ts:198-199. This automatically corrects both the displayed avgFwPct (459/483) and the <50% bucket split (462-463). Optionally also gate validRounds on total_fairways > 0 to avoid divide-by-zero on legacy null rows.

## 28. [LOW] Driving 'dispersion radius' is a synthetic constant per result bucket, then compared to a real-yards benchmark and surfaced as a percentile
- **Dimension:** coachhelm-patterns | **Category:** other | **Confidence:** 0.88
- **Location:** src/lib/coachhelm/v2/mining/lie-specific-analysis.ts:1288-1332 (calculateDrivingDispersion estimateMissDistance) + 1659-1675 (Wide Driving Dispersion insight) + 2096-2109 (driving percentileRank)
- **Expected:** A dispersion metric compared to a 25yd benchmark must be measured in the same units/quantity (actual lateral spread of drive landing positions). With only result categories available, no meaningful std-dev-in-yards can be derived, so the >1.5x-benchmark insight and percentile should not be driven by these constants.
- **Actual:** dispersionRadius = std-dev of {0,15,20,35,10} weighted by result counts (max ~17.5yd), compared to a 25yd benchmark and converted to percentileRank; the 'Wide Driving Dispersion' insight (radius > 37.5yd) can essentially never fire and the percentile is meaningless.
- **Fix:** Fix the "Wide Driving Dispersion" insight to stop comparing a synthetic, bounded result-category std-dev (max ~17.5yd) against a real-yards lateral-spread benchmark of 25yd. Two clean options: (1) Drive this insight off the genuinely-measured DispersionPattern.stdDeviation (calculateDispersionPatternForShots, lines 1955-1972) that already powers the percentileRank, instead of DrivingAnalysis.dispersionRadius from calculateDrivingDispersion (1288-1332). That makes the >37.5yd trigger meaningful. OR (2) if no landing-coordinate/lateral data exists, retire the synthetic dispersionRadius and its >benchmark*1.5 insight entirely (the dangerZoneRate / penaltyRate / fairwayHitRate insights are already sound), and base "wide driving" on fairwayHitRate vs DISPERSION_BENCHMARKS.driving.fairwayHitRate(55) instead. Either way, calibrate the benchmark/threshold to the same metric the player value is actually measured in. Leave the driving percentileRank at 2096-2109 as-is — it is fed by real measured distances, not the synthetic constants, so it is not broken.

## 29. [LOW] Cross-surface dedupe inconsistency: getInsightsForCoach collapses (player:category:metric-subject) duplicates but getInsightsForPlayer does not — a player sees duplicate-subject cards a coach viewing the same player would see merged
- **Dimension:** coachhelm-delivery | **Category:** consistency | **Confidence:** 0.9
- **Location:** src/app/golf/actions/insight-delivery.ts:520-530
- **Expected:** Either both surfaces dedupe by the same (player:category:subject) key, or neither does — consistent card sets for the same underlying rows. Coach feed → 1 scrambling_pct_sand card; player feed should also be 1 (or both 2).
- **Actual:** Coach feed dedupes (1 card); player feed does not (2 cards) for player 49ffe06d's scrambling_pct_sand pair. Only 1 colliding pair exists on this team, so impact is small here, but the asymmetry is structural.
- **Fix:** Make the two read paths symmetric by extracting the dedupe into a shared helper and applying it in getInsightsForPlayer too. In insight-delivery.ts getInsightsForPlayer (lines 429-435), replace the bare sort+slice with the same `seenSignatures` filter used in getInsightsForCoach (520-530), keyed on `${player_id}:${category}:${canonicalMetricSubject(metric)||title}`, before `.slice(0, limit)`. Alternatively, if the product intent is to keep distinct composite patterns visible (the composite carries source_insight_ids and is a different finding), remove the dedupe from getInsightsForCoach instead so both surfaces show both rows — but the dedupe should be IDENTICAL on both. Note the canonicalMetricSubject key cannot distinguish a single-metric leak from a composite sharing the same metric; if both should survive, the dedupe key should incorporate composite_rule_id / signature class so genuine composites are not silently dropped on the coach feed. Add a regression test asserting getInsightsForPlayer and getInsightsForCoach(opts.player_id=X) return the same card set for a player with a colliding pair.

## 30. [LOW] Distance-range avgProximity blends feet (green finishes) and yards (off-green finishes) — inflates proximity up to ~15x
- **Dimension:** player-profile | **Category:** unit | **Confidence:** 0.9
- **Location:** src/app/golf/actions/shot-analytics.ts:544-546, 569-571
- **Expected:** On-green finishes only (feet), e.g. for Owen Carter (8091da4b..., 30-day window): 0-50y=9ft, 50-100y=16ft, 100-130y=17ft, 130-160y=20ft, 160-190y=21ft, 190+y=19ft. Fix: filter rangeShots to result in ('green','hole','gir') before mapping toFeet (mirror lines 383-386), or use the unit-aware conversion only for off-green and never blend.
- **Actual:** Code produces (same player/window): 0-50y=18ft (2x), 50-100y=63ft (~4x), 100-130y=21ft, 130-160y=50ft (2.5x), 160-190y=61ft (~3x), 190+y=295ft (~15x — a physically impossible 'proximity'). Verified by SQL: avg(CASE WHEN distance_unit_after='yards' THEN d_after*3 ELSE d_after END) vs avg(d_after FILTER green/hole/gir).
- **Fix:** Mirror the approachStats fix: filter rangeShots to on-green finishes before computing proximity, i.e. proximities = rangeShots.filter(s => s.result === 'green' || s.result === 'hole' || s.result === 'gir').map(s => toFeet(s.distance_to_hole_after, s.distance_unit_after)).filter(...). This makes the value correct should it ever be surfaced. Given it is currently a dead field with no consumer, this is low-urgency hygiene; alternatively wire it into ShotAnalyticsPanel only after the on-green guard is added, never before.

---
## Dismissed by adversarial verification (20)
- (core-calculator) Putt miss-direction is gated on `firstPuttLeave > 0` (distance_to_hole_after), silently dropping miss tracking for any 3-putt missing leave data; break-type miss path uses a DIFFERENT gate, so the two putting-miss surfaces disagree — The finding accurately observes that the overall putt-miss block (golf-stats-calculator-shots.ts:1795) fires only when `hole.putts > 1 && hole.firstPuttLeave && hole.firstPuttLeave > 0`, while the per-break-type block (:1831) fires on `hole.putts > 1 && firstPuttShot.miss_direction`. But the alleged consequence — the two miss surfaces disagreeing because the leave-distance gate drops rows — is not demonstrable on ANY data and, separately, is not even the mechanism by which the two surfaces actually differ.\n\nEVIDENCE (SQL, re-derived independently):\n(a) Demo team (id 6ecdd1a6, 78 rounds, 1377 holes): of all multi-putt holes, first_putt_holed=0, leave_after_null=0, leave_after_zero=0, and miss_dir_but_no_leave=0. So the `firstPuttLeave > 0` gate drops exactly zero rows that carry a miss_direction.\n(b) ENTIRE database (all teams, 2254 multi-putt holes, 1116 with a recorded miss_direction): latent_divergence_rows=0, leave_null_all=0, leave_zero_all=0. The scenario the finding describes (miss_direction present but leave null/0) occurs on 0 rows DB-wide. The only structural way firstPuttLeave is null on a >1-putt first putt is result='hole' (impossible: a holed first putt yields 1 putt) — so the gate is effectively unreachable as a divergence source.\n\nFINDING MISATTRIBUTES THE REAL DIFFERENCE: The two surfaces DO produce different numbers today, but for a different reason the finding never mentions. The overall `puttMissTotal` (:1796) increments for every multi-putt-hole-with-leave REGARDLESS of whether a miss_direction was recorded (the ++ sits outside the `if (firstPuttShot?.miss_direction)` check at :1799), whereas the break-type `missTotal` (:1832) increments ONLY when miss_direction exists. For Owen Carter: overall puttMissTotal=154 vs break missTotal sum=52 (the 52 = exactly the holes with a recorded miss_direction). The overall miss percentages therefore divide the same direction-counts by ~3x the denominator. That is the active discrepancy; the `firstPuttLeave > 0` gate contributes 0 to it. The finding's stated mechanism (\"overall path additionally excludes 2+putt holes whose leave is missing/0\") removes nothing on real data.\n\nAdditionally, the two views are not \"the same stat shown twice\": overall = 6 directions (L/R/short/long/low/high) over all multi-putt holes; break-type = 3 directions (short/low/high) partitioned by putt_break — different scopes shown in different UI sections, so line-for-line reconciliation isn't an expected invariant. safePercent (:474) returns null on attempts=0, so no NaN/div-by-zero risk.\n\nConclusion: the SPECIFIC defect alleged is a latent, never-triggered code smell with no observable miscalculation on 100% of existing data, and the genuinely active inconsistency is a separate denominator-semantics matter the finding did not identify. Defaulting to is_real_bug=false per instructions, since the defect cannot be demonstrated.
- (core-calculator) Par-3 tee shots compute SG:Approach expected-strokes from the TEE benchmark curve, not the fairway/approach curve — All factual claims in the finding are verified. (1) getStrokesGainedCategory returns 'approach' for par-3 tee shots — line 739, asserted by the test at lines 510-511. (2) calculateStrokesGainedForShot computes expectedBefore via getExpectedStrokes(lieBefore, distBefore) with lieBefore='tee' (line 690), which routes to STROKES_GAINED_BENCHMARKS.tee. (3) SQL confirms 296 par-3 first shots on the demo team carry shot_type='approach', lie_before='tee', avg 183.1yd. (4) The two curves differ at par-3 distances (175yd tee=3.12 vs fairway=3.08; 200yd tee=3.20 vs fairway=3.19; at 183yd interpolated tee=3.146 vs fairway=3.115, diff ~0.030). (5) calculateHoleStatsFromShots does remap par-3 tee lie to 'fairway' at line 925 — but ONLY for the categorical approachLie breakdown (which exposes fairway/rough/sand buckets), NOT for SG expected-strokes.\n\nThe verdict is that this is a defensible, documented, deliberately-tested convention rather than a net-new miscalculation. The tee table here was purpose-built to cover par-3 distances: the comment at lines 551-559 (ticket DC-SG-3) explains the team REMOVED the old '<400yd tee -> fairway re-route' and replaced it with one continuous tee curve from 100yd, and the test at lines 415-418 explicitly locks in 'uses the continuous tee curve for short tee shots' (150yd -> 3.05). The par-3 anchors (2.92@100, 3.05@150, 3.12@175) are sensible standalone expected-strokes values for a par-3 tee shot — not the long-game tee values — and sit only ~0.01-0.10 strokes from the fairway curve. A par-3 tee shot is genuinely played from a tee, so a tee baseline is a reasonable modeling choice; the line-925 fairway remap serves a different (categorical) purpose and is not a contract the SG path must mirror. Notably, neither of the codebase's other two SG implementations remaps par-3 tee lie to fairway either: the display-only strokes-gained.ts feeds 'tee', and the server-authoritative SQL calculate_round_strokes_gained also feeds 'tee' to sg_expected_strokes. (Side note: the SQL sg_expected_strokes tee table only spans 260-600yd, so it clamps a 183yd par-3 to 3.620 vs fairway 2.874 — a ~0.75-stroke discrepancy — but that is a separate function and a separate, much larger issue, not the sub-0.04-stroke item described here.)\n\nMagnitude: at the demo team's 183yd average the divergence is ~0.030 strokes per par-3 tee shot, ranging ~+0.10 (120yd) to -0.03 (225yd) across the realistic par-3 band — below the calibration noise of these hand-authored baselines (the tee and fairway anchors were authored independently and differ by amounts comparable to their own rounding). This is a defensible convention, not a demonstrable defect.
- (driving) drivingDistanceAvg / fairway denominators have latent par-3 and null-club tee-shot edge cases not present in the demo data — I independently re-derived both sub-claims from code and SQL.

CLAIM 1 (par-3 tee shots averaged into driving distance): The driving-distance accumulator at golf-stats-calculator-shots.ts:1626-1634 indeed sits OUTSIDE the `hole.par !== 3` guard (that guard begins at line 1639), so it relies on hole.drivingDistance being null on par 3. drivingDistance (line 879) = teeShot?.shot_distance, and teeShot (line 873) = normalizedShots.find(s => s.shot_type === 'tee'). Whether a par-3 tee shot can exist is decided at the data-entry layer: use-shot-state-machine.ts:863-864 deterministically returns shot_type 'approach' for par-3 shot #1 and only 'tee' when par !== 3. The CSV/bulk import path (golf.ts:5711) defaults missing shot_type to 'approach'. SQL across ALL teams: 0 raw 'tee'/'drive' shots on par-3 holes; par-3 shot #1 is always 'approach' (668) or 'penalty' (17). So the par filter for driving distance is enforced at entry time, and a par-3 tee shot is not producible by any current write path.

CLAIM 2 (NULL-club tee shots counted as non-driver): usedDriver = teeShot.club_type === 'driver' (line 877) does yield false (not null) for a NULL club, which would route to non-driver buckets at lines 1630/1654/1669. But a NULL club_type on a tee shot cannot occur: live-entry schema requires clubType z.string().min(1) constrained to ['driver','non_driver','putter'] (golf.ts:239,361); import coalesces club_type ?? 'non_driver' (golf.ts:5712). SQL across ALL teams: 0 null-club tee shots; tee-shot club_type is exclusively 'driver' (1626) or 'non_driver' (715).

CONCLUSION: Both are latent robustness observations, not net-new miscalculations. The conditions that would trigger them produce 0 rows across the entire database (not merely absent in Demo University), and are structurally prevented upstream by the round-entry state machine and Zod schemas for every write path. No team's drivingDistanceAvg, fairwayPctNonDriver, or drivingDistanceNonDriverOnly is currently miscomputed. The finding itself states confidence 0.6, severity low, and 'Neither manifests in Demo University Golf.' Defensible/by-design (par filter applied at entry, not aggregation); does not meet the bar for a real bug.
- (approach) CoachHelm approach proximity (avgProximity) has no blow-up outlier cap, unlike the leak-map/Stats path — Independently re-derived. CODE: shot-analytics.ts:383-386 builds approachProximities from shots where shot_type IN ('approach','iron') AND result IN ('green','hole','gir') AND distance_to_hole_after > 0, mapped through toFeet() (line 191-194: ×3 only when unit=='yards'), then averaged with no upper cap (line 403-405). The leak-map path (stats-leak-maps.ts:267-289) uses shot_type=='approach' only, the same on-green result filter, PLUS beforeYd>=50 (APPROACH_MIN_BEFORE_YD) and afterFt<=150 (APPROACH_PROXIMITY_CEILING_FT). So the finding's factual claim — shot-analytics has no ceiling, leak map does — is TRUE.

But this is NOT a net-new miscalculation, for three demonstrated reasons:

(1) NO CURRENT GAP. SQL on Nick Rini (player 49ffe06d) over completed rounds: shot-analytics avg = 17.79 ft (n=156), leak-map avg = 17.73 ft (n=153). Exactly matches the finding's own stated numbers. The 0.06 ft difference comes from the leak-map's before>=50yd band floor dropping 3 short approaches, NOT from the ceiling. The ceiling fires on zero rows: across the entire team, all 872 on-green approach/iron rows are stored in 'feet', range 1-80 ft, n_over150=0.

(2) STRUCTURALLY IMPOSSIBLE ON REAL DATA. Across the ENTIRE golf_shots table (not just Demo), all 1923 on-green approach/iron rows (result IN green/hole/gir) are stored in 'feet' with max 80 ft and zero over 150. There are zero yards-tagged on-green rows anywhere. The hypothetical 'blow-up green row tagged yards' the finding requires does not exist and is contradicted by the storage convention.

(3) PRIMARY DEFENSE ALREADY PRESENT IN BOTH. The real root-cause fix for the feet/yards blend is the on-green-only filter (result IN green/hole/gir), documented at shot-analytics.ts:379-382 and stats-leak-maps.ts:268-271. BOTH surfaces already apply it. The 150-ft ceiling in the leak map is a redundant belt-and-suspenders guard (proven redundant: on-green rows are always feet<=80). Its absence in shot-analytics is not an active defect.

The finding's own author flags it latent (gap=0 stated explicitly), confidence 0.6, severity low. Per the task brief ('only flag a real defect, not a defensible convention; default to is_real_bug=false unless you can demonstrate the defect'), the defect cannot be demonstrated on any data. This is a reasonable hardening suggestion, not a miscalculation. The already-fixed on-green filter has not regressed in either path.
- (putting) Cross-surface inconsistency: player Putting tab 'Make %' (first-putt 1-putt rate) vs leak-map 'Make %' (per-putt make rate) differ by 9-12 pts for the same distance band — I independently reproduced both code paths and both numbers from raw SQL on the Demo team (golf_teams.id LIKE '6ecdd1a6%').

CALCULATOR (golf-stats-calculator-shots.ts): puttMake bands keyed by getPuttDistanceBucket (496) = 0_3/3_5/5_10/10_15/15_20/20_25/25_30/30_35/35_plus. Denominator = the FIRST putt of each hole, banded by firstPuttDistance (= firstPutt.distance_to_hole_before normalized to feet, line 953-954). made = hole.putts===1 (1778). Surfaced on the PLAYER stats page (stats-client.tsx -> GolfStatsDisplay -> PuttingStats.tsx:137) under the title "Make % by Distance", with NO PGA comparator. My SQL replicating this exactly: 3-5ft 62.5%, 5-10ft 32.1%, 10-15ft 16.7%, 15-20ft 9.9% — matches the finding's "actual."

LEAK MAP (stats-leak-maps.ts:buildPuttBuckets 220-247): bands 0_3/3_5/5_10/10_15/15_25/25_plus keyed by putt_distance_feet. Denominator = EVERY putting shot with putt_made non-null; made = putt_made===true. Surfaced ONLY on the COACH team page (stats/team/page.tsx:350 -> FairwayTeamStats.tsx:418-426) under the title "Putts Made by Distance" / "Team make% vs PGA Tour". My SQL: 3-5ft 73.8%, 5-10ft 41.5%, 10-15ft 20.4%, 15-25ft 10.8% — matches the finding's "expected."

So the ~9-12pt divergence is real and reproduces exactly. BUT this is not a miscalculation, for three reasons:

(1) They are NOT the same stat computed two ways — they answer different questions. Calculator = P(one-putted the hole | first putt started in band) = a per-hole 1-putt conversion, deliberately first-putt-conditioned to match its sibling tiles (First Putt Leave, Putting Efficiency, all keyed on the first putt). Leak map = per-putt holing rate. Both formulas are internally correct; no wrong denominator, unit blend, normalization error, or div-by-zero. I confirmed putt_distance_feet and distance_to_hole_before(feet) are interchangeable for putts (avg abs diff 0.25 ft, 3/1368 rows differ), so the gap is the definition, not a banding/unit bug.

(2) The leak-map definition is the CORRECT per-putt comparator: golf_pga_standards are explicitly per-putt make rates (source text: "Tour avg of 3+4+5 ft: (99.4+91.4+80.7)/3"), and the leak map is the only one that compares to PGA. The player tab never compares to PGA, so there is no apples-to-oranges PGA mismatch.

(3) The finding overstates the conflict: it claims both are "presented to the same user as 'Make % by Distance'" on the same surface sharing a 15-25ft band. In fact the labels differ ("Make % by Distance" vs "Putts Made by Distance"), the surfaces differ (player page vs coach team page, one drill-in apart), and the bands don't even align (calculator splits 15-20/20-25; leak map uses a single 15-25), so the cell-to-cell comparison the finding implies isn't actually rendered.

This is a naming/UX-consistency observation at worst, not a net-new calculation defect, and it is mitigated by the distinct titles and surfaces. Does not meet the bar for a real bug.
- (putting) Putt distance band boundaries differ between player tab (inclusive upper, <=) and leak map ([min,max) exclusive upper) — The factual mechanics in the finding are correct: getPuttDistanceBucket (golf-stats-calculator-shots.ts:486-496) uses an inclusive upper edge (distance<=3 -> '0_3', <=5 -> '3_5', ...), while leak-map bandFor (stats-leak-maps.ts:147-156) with PUTT_BANDS (83-89) uses half-open [min,max) (value<band.max). So a putt at exactly 5.0 ft lands in '3_5' in the calculator and '5_10' in the leak-map, and the Demo-team edge counts in the finding are verified by SQL (110 putts at 3ft, 89 at 5ft, 99 at 10ft, 161 at 15ft, 131 at 20ft, 93 at 25ft).\n\nBut the finding's premise — that the two surfaces compute the same comparable putt make% so a shared edge convention is required — is false. They are two structurally different metrics on two different surfaces:\n(1) Different band schema: the calculator has 9 buckets up to '35_plus' (15_20/20_25/25_30/30_35/35_plus); the leak-map has 6 buckets ('15_25','25_plus'). They cannot align 1:1 above 15ft regardless of edge convention.\n(2) Different input population: the calculator buckets by per-hole hole.firstPuttDistance (one row per hole); the leak-map buckets EVERY putting shot by its own putt_distance_feet (many rows per hole).\n(3) Different make definition: calculator make = hole completed in one putt (hole.putts === 1, golf-stats-calculator-shots.ts:1776-1778); leak-map make = that individual putt holed (putt_made === true, stats-leak-maps.ts:226-230).\nThese definitional differences make the numbers diverge by tens of points independent of the edge rule — e.g. the leak-map per-putt 3-5ft make% (half-open) is 73.8% over 225 graded putts, which has no reason to equal the player tab's per-hole first-putt one-putt rate for the '3_5' bucket. There is no third surface displaying the SAME metric under both conventions.\n\nEach function is internally consistent and independently tested: the calculator's inclusive-edge boundaries are explicitly pinned by a regression-guard test (test file lines 900-924 and the legacy-band guard at 1406+), and the leak-map's half-open convention is documented in its type comment ('exclusive upper edge', stats-leak-maps.ts:80). For interior values (e.g. 4.0 ft) both agree; only exact integer edges differ, and even there the values were never meant to match. Neither value is wrong against its own SQL-derived definition. This is a defensible documented convention difference between two correct, independently-tested band functions, not a net-new miscalculation that makes any displayed stat wrong.
- (scrambling) Scrambling 'by distance' upper bucket labeled '20-30 yards' actually holds 20-50 yards — VERIFIED the code path. golf-stats-calculator-shots.ts:1936-1946 buckets the chip distance (chipDistance, normalizeToYards of the around_green shot, capped at AROUND_GREEN_THRESHOLD_YARDS=50) into <=10 / <=20 / else. The else branch is open-ended up to 50yd. This is intentional and documented: getAtgDistanceBucket comment at line 519 ("despite the name, this bucket captures 20+ yards up to the threshold") and the threshold rationale at lines 526-541. Three UI surfaces label scramblingPct20_30 as "20-30 yards"/"20–30 yds" (ScramblingStats.tsx:61, :69 and FairwayStatsCockpit.tsx:1007), while ScramblingStats.tsx:103 already uses "20-50 yds" for the matrix — so a labeling inconsistency genuinely exists.

HOWEVER, the finding's load-bearing data evidence is FALSE. I re-derived the truth from SQL over the Demo team's 78 completed rounds by replicating the code's exact logic (scrambleAttempt = !gir, with gir = golf_holes.gir ?? derived; first around_green/chip shot per hole, distance normalized feet/3→yards). Result for the upper (>20yd) bucket: 84 holes total, split exactly 42 in the 20-30yd band (min 21, this IS what the label says) and 42 in the 30-50yd band, ZERO over 50. So the "20-30 yards" label correctly describes 50% of the bucket's contents, not 0%. The finding claims "42 holes and ALL 42 are >30yd ... label describes 0% of the bucket." Both the count (84 vs 42) and the distribution (50% correctly labeled vs the claimed 0%) are wrong. A raw around_green-shot query is even more favorable to the label: 69/69 of >20yd shots fall in 20-30yd (max 30), none >30yd.

The finding itself concedes "the math is intentional." No formula, denominator, unit blend, normalization, null-handling, band boundary, PGA baseline, or SG sign is wrong — every computed percentage and the matrix values are correct. This is a cosmetic label imprecision (the upper bucket's range extends to 50yd, so "20-30 yards" undersells the top half), already partially corrected in the codebase (matrix uses "20-50 yds"), and the finding's quantitative justification for severity does not hold. It is not a net-new miscalculation.
- (strokes-gained) lie_before='other' shots fall through to a generic distance-only baseline estimate — The mechanism is partly real but the finding misdiagnoses it on three independently fatal grounds, so it is not a net-new miscalculation.

WHAT'S TRUE: DB has 127 lie_before='other' shots for the Demo team (313 across all data). Both callers pass lie strings through UN-normalized: coachhelm-data.ts:657/659 (lieBefore: s.lie_before ?? 'fairway') and team-category-insights.ts:534/535. So 'other' reaches lookupBaseline (shot-level-sg.ts:99-114), finds no 'other_<bucket>' key in buildDefaultBaseline, and falls back to fairway_<bucket>. That data flow is confirmed.

WHY IT'S NOT A BUG:
1) The 'expected' value is wrong. The finding asserts 'other' should map to recovery (recovery_150-175=4.15). But the lie taxonomy (docs/SHOT_TRACKING_DEEP_SCRUB.md:45) lists `tee|fairway|rough|sand|green|other|recovery|hazard` — 'other' and 'recovery' are SEPARATE, distinct categories. 'other' is a catch-all, not a synonym for recovery. The data confirms: of 127 'other'-before shots, result resolves to fairway(42), rough(33), green(30), penalty(11), other(7), sand(4); avg distance 145–240 yd — these are ordinary full shots from miscellaneous/unclassified lies, NOT trouble/recovery lies. Forcing them all to recovery=4.15 would OVERSTATE difficulty for the 105 that reached fairway/rough/green.

2) The fairway fallback is the codebase's documented, canonical convention. The DB function sg_expected_strokes() explicitly comments `ELSE -- unknown lie → use fairway baseline`, and sg_normalize_lie() maps every unknown/'other' lie to 'fairway'. The client lookupBaseline fairway fallback applies the identical 'unknown lie → fairway' rule. This is deliberate and system-wide, not accidental.

3) The stated impact direction is factually false. The finding claims this 'understates the SG penalty for ENDING in other.' But lie_after is NEVER 'other' in the data (lie_after distribution = green/fairway/rough/sand/null/penalty only). The 'other' key is therefore never consulted for the ending baseline; only the starting baseline is touched, and any error there is two-directional (fairway 3.18 vs the finding's 4.15) and small.

4) Negligible surfaced effect: only 17 of 127 'other' shots fall in the 150-175 bucket, below the rankWeaknessContexts thresholds (15 team / 5–15 player), so 'other' contexts rarely surface as ranked weaknesses anyway.

Net: defensible, documented convention with a mis-stated 'expected' value and a factually incorrect impact claim. Not a net-new defect.
- (team-page) SG hero bars (sum-of-category mean) and SG-total Readout can disagree when player coverage is uneven across SG metrics — I independently re-derived the truth. The code at FairwayTeamStats.tsx:93-107 (teamMeanFor), 255-268 (sgData bars + sgTotalMean), and 339-365 (InstrumentPanel Readout + "sum of every category" copy) is exactly as the finding describes: the four category bars and the sg_total Readout are computed as independent per-metric unweighted means over whichever players carry each metric row.

The finding's "expected" numbers are correct. SQL over golf_player_standing for the Demo University Golf team (6ecdd1a6...) gives team means sg_ott +2.0900, sg_approach -1.4750, sg_around_green +0.1700, sg_putting -3.9667 (sum = -3.1817) and sg_total -3.1817 — an exact match. So the hero is currently CONSISTENT, as the finding concedes.

The finding rests entirely on a LATENT precondition: that SG-metric coverage can be uneven across players (a player with a sg_total row but a missing category row, or vice versa). I tested whether that precondition is realizable and found it is not:
(1) Per-player, sum-of-categories equals total within ±0.01 rounding noise for all 6 Demo players — total IS the sum of the four categories.
(2) Across ALL 20 golf_player_stats_cache rows DB-wide, all five sg_*_per_round columns are populated together (20/20 each); zero rows have total-present-but-category-missing or category-present-but-total-missing. SG is computed in a single shot-level pass that buckets each shot into exactly one category and sums to total, so the cache writes all five atomically.
(3) Across ALL 14 standing-eligible players DB-wide, every player has exactly 5 SG standing rows — the coverage count is uniformly {0,5}, never a partial 1-4. The refresh function writes all five SG standing rows or none.

So uneven SG coverage cannot occur given how SG is derived and persisted; the invariant the copy promises is already guaranteed by the data model, not "unenforced and breaking silently." The 7th roster member (Tyler Passmore, <5 rounds) has none of the 5 metrics and is uniformly excluded from every mean, so it does not perturb the identity.

The secondary observation (means are unweighted by rounds) is true but is a deliberate, uniformly-applied convention: BOTH the bars and the Readout use the same unweighted teamMeanFor, and since per-player total = sum of categories, the mean of totals always equals the sum of category means regardless of weighting. Weighting choice therefore does not break the bars-vs-total identity. This is a documented aggregation convention, not a miscalculation. The only residual is a cosmetic ±0.1 independent-rounding artifact (each bar and the Readout each rounded to 0.1 via fmtSg), which is universal to every components-vs-total display and is not the mechanism the finding alleges.
- (dashboard) teamPulse.roundsThisWeek counts completed rounds without the total_score IS NOT NULL filter the rest of the dashboard applies (latent inconsistency) — The finding's code claim is accurate. In src/app/golf/actions/dashboard-data.ts the weekRoundsResult count query (lines 381-386) filters only .eq('status','completed').gte('round_date', weekAgo) and omits .not('total_score','is',null), whereas recentRoundsQ (line 359) and allRoundsQ (line 373) both include it. The result is assigned to teamPulse.roundsThisWeek (line 419) and rendered as the "{n} this week" pill in FairwayCoachDashboard TeamPulsePanel (lines 616-620).

However this is a latent inconsistency, not a net-new miscalculation:
1) ZERO wrong output today or historically. SQL over the ground-truth team (golf_teams LIKE '6ecdd1a6%'): 78 completed rounds, 0 with NULL total_score. Platform-wide: 173 completed, 0 with NULL total_score. So the pill counts exactly the same set the scored-only filter would. The finding itself concedes "No defect surfaces on the ground-truth team."
2) It is a definitional/consistency asymmetry, not a wrong formula, unit blend, bad denominator, missing normalization, or div-by-zero. The two surfaces it could disagree with measure DIFFERENT windows anyway — the pill is a fixed trailing-7-days count, while the "N rounds in window" footnote and KPIs use the selected dateCutoff window. So they are not expected to match even with identical filters; the only divergence would be the completed-vs-completed-and-scored definition, which is invisible until a NULL-score completed round exists.
3) The metric is defensibly interpreted differently. "Rounds logged this week" is an activity/engagement signal (coach-signal.ts:83-90 phrases it "rounds logged this week"), for which counting any completed round regardless of whether a final score was typed is a reasonable product choice, not clearly wrong. total_score is nullable with no status->score constraint, so a completed-but-unscored round is physically possible, making the asymmetry real but not currently realized.

Per the task's explicit bar ("FIND NET-NEW CORRECTNESS ISSUES ONLY" and "default to is_real_bug=false unless you can demonstrate the defect"), I cannot demonstrate any incorrect computed value. It is a legitimate code smell worth a one-line tidy-up but not a miscalculation.
- (leak-maps) No .order() on leak-map shot fetches → which 1000 rows survive the cap is non-deterministic (reload-to-reload drift) — I independently re-derived the truth at src/app/golf/actions/stats-leak-maps.ts:212-218 (putts) and 263-275 (approach). Both queries indeed apply no .order(); both use .limit(MAX_SHOT_ROWS) where MAX_SHOT_ROWS=20000 (line 110).

Ground-truth row counts for the Demo team (id LIKE '6ecdd1a6%', active members, completed rounds): 2510 putt rows (>1000), 876 approach rows (<1000). Matches the finding's figures.

I then empirically tested the actual PostgREST behavior against the live instance using the service-role key from .env.local. Decisive results:
1) The hosted instance enforces a hard server-side max-rows=1000 (supabase/config.toml line 18 also shows max_rows=1000). A real request with ?limit=20000 returned content-range 0-999/5625 = exactly 1000 rows. So the code's explicit .limit(20000) does NOT lift the cap. The 2510-row putt query IS truncated to 1000.
2) I reproduced the exact code query shape (in(round_id,...) + shot_type=putting + putt_distance_feet not null + limit=20000) four times back-to-back. The surviving 1000-row id set was byte-identical every run (md5 2d9ed73fa2c8), and per-bucket sample_n/make% were stable. The finding's headline claim — 'non-deterministic, reload-to-reload drift in which 1000 survive' — was NOT reproducible; Postgres physical-scan order is stable for a static table+plan.
3) Adding .order('id') (the finding's recommended fix) changed WHICH 1000 survive (overlap only 183/1000 vs unordered) but BOTH remained capped at 1000. So .order alone does NOT fix the actual error — it merely makes a still-wrong, still-truncated subset deterministic.

So there IS a genuine correctness problem in this file (team putt leak-map buckets are computed over only 1000 of 2510 rows: e.g. 0_3 sample_n 383 vs true 945, 5_10 make% 45.2 vs true 41.5, 15_25 12.8 vs true 10.8). BUT: (a) that defect is the truncation, which is the explicitly off-limits 'ALREADY FIXED THIS SESSION item #1' (PostgREST 1000-row cap) — the finding even says 'see prior finding' — so it is not net-new; (b) the finding-as-written frames the bug as an ordering/determinism issue, whose specific mechanism (reload drift) I could not reproduce; and (c) its recommended fix is wrong (would not correct the under-count). Per instructions to default to is_real_bug=false unless the specific finding demonstrates the defect, and not to re-report the already-fixed truncation, this is not a valid net-new finding.

Side note worth flagging to the parent (not this finding): the empirical test shows the session's item-#1 fix (.limit(50000)) is itself INEFFECTIVE against this hosted instance's max-rows=1000 — only fetchAllRows() pagination actually lifts the cap. The 23 .limit(50000) 'fixes' likely still truncate. That is a different issue than the ordering finding under review.
- (leak-maps) Approach-proximity computed two different ways for the same metric: leak-map chart (TS) vs StandingBar (SQL RPC) use divergent on-green filter, before-unit handling, and outlier ceiling — I independently re-derived the truth. The finding's structural premise is correct and verified: getApproachProximityLeakMap -> buildApproachBuckets (TS, stats-leak-maps.ts:253-309) and getPlayerStandingRows -> loadPlayerStandingMap (reads golf_player_standing, populated by refresh_player_standing_shot_metrics, migration 20260605130000:50-79) both surface the SAME metric IDs (approach_proximity_50_125ft/125_175ft/175_plus_ft) for the same player, and the StandingBar renders them as a feet value (metric-config.ts:55-57 unit:'feet'; utils.ts:52 formatValue -> "16 ft"), so the two surfaces show the same numeric quantity computed two ways.\n\nAll four code-level differences the finding lists are real:\n(1) on-green set: TS .in('result',['green','hole','gir']) (line 272) vs SQL result IN (...) OR lower(lie_after)='green' (lines 70-71).\n(2) before-unit: TS uses distance_to_hole_before raw, never reading distance_unit_before (line 278) vs SQL normalizes feet->/3 (lines 72-77).\n(3) ceiling: TS drops afterFt>150 (line 289) vs SQL has none.\n(4) sample gate: SQL HAVING COUNT(*)>=3 (line 79) vs TS shows any n>0 (line 303).\n\nHowever, the decisive question is whether a WRONG number is being produced. I re-derived per-player per-band proximity BOTH ways via SQL on the Demo team (6ecdd1a6...): every one of the 18 player/band pairs has avg_diff = 0.000 with identical sample counts (e.g. Owen 50-125 = 15.95 ft both ways, matching the prompt's 15.9 ground truth), and every band passes MIN_GREENS>=3, so divergence #4 also suppresses nothing. Database-wide I measured each divergence's trigger conditions: lie-only-green rows = 0, on-green after-feet>150 = 0, distance_unit_before='feet' = 1 row total (and that single row is NOT on any team whose player would be viewed here). So neither surface miscalculates today, and no plausible currently-existing data makes them disagree on a visible player.\n\nThis is a genuine latent divergence / maintainability risk (two implementations of one metric that will drift on data with green-by-lie-only rows, feet-before rows, on-green finishes >150 ft, or 1-2 green bands), but it is NOT a net-new miscalculation: there is no wrong value on any surface. Two of the four differences are also defensible by design — the TS 150 ft chart ceiling and the SQL MIN_GREENS=3 standing floor are documented, intentional choices serving different surfaces (a chart that tolerates thin samples vs a ranking that needs a statistical floor). The finding itself self-classifies as latent ('identical only by coincidence', severity low, confidence 0.7), which is the honest framing. Per the is_real_bug=true-only-for-genuine-net-new-miscalculation bar, this does not qualify.
- (coachhelm-insights) Player CoachHelm dashboard read path (loadEvidenceBackedInsights) bypasses the canonical v3/status/lifecycle filters — surfaces stale v2 scale-mixed rows and diverges from insight-delivery.ts — The finding's query-level claims are accurate. loadEvidenceBackedInsights (insights.ts:2551-2559) filters ONLY .in('lifecycle_state',['detected','matured','addressed']) + .eq('dismissed',false), with NO V3_ENGINE_FILTER, NO .neq('status','dismissed'), and excludes 'resolved'. The canonical insight-delivery.ts path applies V3_ENGINE_FILTER (const at :177, applied at :245/:268/:353), VISIBLE_LIFECYCLE_STATES including 'resolved' (:114), and .neq('status','dismissed') (:247/:270/:355). I confirmed the data divergence in DB for Demo University Golf: 93 rows pass the loose filter, 75 pass v3, so 18 v2 rows are kept by the loose path and dropped by the canonical path.

HOWEVER the finding's stated user-facing impact is false. It claims these v2 rows are "prepended as the first insights on the player CoachHelm dashboard." They are not. The merged list (insights.ts:2508 mergedInsights -> dashboardData.insights at :2518) is serialized but NEVER read by any render surface. I grepped both consuming components: the legacy PlayerCoachHelmDashboard.tsx reads only dashboardData.focusAreas/.prediction/.recentRounds/.playerState/.playerName (and renders the insight feed exclusively from the topInsight + secondaryInsights props), and the LIVE flag-ON FairwayPlayerCoachHelm.tsx (the prod redesign path per project memory) likewise reads only data.focusAreas/.prediction/.recentRounds/.playerState/.playerName plus topInsight/secondaryInsights. Neither reads data.insights. Both topInsight and secondaryInsights come from getTopInsightForPlayer/getInsightsForPlayer in insight-delivery.ts (page.tsx:155,157), i.e. the canonical hardened v3 path. The only other .insights references (FairwayBrief.tsx cat.insights) are a TEAM surface sourced from getTeamCategoryInsights, unrelated to this field.

So there is no user-visible cross-surface inconsistency: the loose v2 rows are computed and discarded. Additionally the finding overstated magnitude — the v2 'scoring' family is n=1 at strokes_impact 1.03 (not avg 24.05), and the max loose-passing v2 impact is 3.77 (putting); no 24/42-stroke scale-mixed scoring rows exist in this team's current loose set. This is genuine dead/wasteful code and a latent footgun (a future component reading data.insights would surface stale v2 rows), but it is NOT a net-new miscalculation reaching the screen. Not a real bug under the is_real_bug definition.
- (coachhelm-insights) loadEvidenceBackedInsights displays raw evidence.strokes_impact without the ceiling clamp the rest of the engine applies — The code-structure observation is accurate but the conclusion is not a defect. VERIFIED: insights.ts:2577 reads `strokeImpact` straight from `evidence.strokes_impact`, and rankInsights() (line 2596) applies cappedStrokesImpact only to the SORT score inside scoreInsight (score.ts:128) before returning the original projected objects via `ranked.map(r => r._ref)`, so the displayed value is unclamped. However, three things defeat the finding:

(1) DESIGN, NOT DEFECT: STROKES_IMPACT_CEILING is explicitly documented as a ranking-input-only guard. score.ts:37-38 states "Display magnitudes are untouched; only the ranking input is clamped." cappedStrokesImpact takes abs() and would strip the sign anyway, so it is unsuitable as a display transform. Clamping display would change a real magnitude; the convention is deliberate.

(2) CONTRAST CLAIM IS FALSE: The finding says insight-delivery.ts "applies cappedStrokesImpact to its surfaced rows (lines 203, 1015-1018)." Those are feedRankScore (line 203) and rankScore (line 1021) — both RANKING functions, identical in role to scoreInsight. insight-delivery.ts surfaces EvidenceInsight.evidence.strokes_impact RAW too; it does not clamp the displayed value anywhere. So there is no asymmetry between the two paths — both clamp ranking only and display raw. The premise of the finding (one path protects display, the other doesn't) is incorrect.

(3) DATA CLAIM IS FABRICATED: The 'actual' cites v2 rows with strokes_impact 24.05 (scoring) and pressure_gap rows at 6.05/5.17 that would leak through. SQL ground truth: across ALL 312 eligible rows for every player (evidence not null, lifecycle in detected/matured/addressed, dismissed=false) the MAX |strokes_impact| is 5.0; for the demo team specifically the max is 4.30. ZERO rows exceed the ceiling of 8. The impossible 24/42-stroke scale-mixed v2 rows were already remediated out of the eligible set, so the clamp would never fire on any displayed value and no impossible number is ever rendered.

Net: no observable miscalculation, the documented ranking-only clamp behaves as specified and consistently across surfaces, and the cited leak values do not exist in the data.
- (coachhelm-insights) Legacy v2 adapter (toInsightInput) stamps confidence_factors.variance = the record's self-reported confidence and hardcodes recency=1 with no factors_measured flag — I independently re-derived the full chain and confirmed the finding's mechanics, but the conclusion overstates them and the dominant claim describes a deliberate, documented v2 convention rather than a defect.

CONFIRMED MECHANICS:
- to-insight-input.ts:89 sets `confidence = clamp01(metadata.confidence, 0.5)` (the record's self-reported confidence).
- to-insight-input.ts:151-155 stamps `confidence_factors = { sample_adequacy: min(1, sampleN/20), recency: 1, variance: confidence }` with `factors_measured` omitted, and `confidence: 0` (line 150).
- types.ts:189-207 calcConfidence: with factors_measured undefined it uses the legacy blend `0.4*sample_adequacy + 0.3*recency + 0.3*variance`.
- upsert.ts:78 recomputes and persists `confidence = calcConfidence(input.evidence)`. So for a default record (variance=0.5) confidence = 0.4*sample + 0.3 + 0.15.
- Adapter is live: src/app/golf/actions/insights.ts:1024 and 3463 → upsertInsight.
- evidence.confidence is consumed materially: EvidencePanel.tsx:334 (displayed %) and v3/ranking/score.ts:128 (`|strokes_impact| * confidence`). So an inflated/mislabeled confidence is observable.

WHY IT IS NOT A NET-NEW BUG:
1. The recency=1 hardcode + omitted factors_measured is the ESTABLISHED, DOCUMENTED v2 convention — not a defect. The native v2 miners do exactly the same: approach-analytics.ts:405-417 buildConfidenceFactors hardcodes `recency: 1` (comment: "Window is fully contained in the query window by construction") and omits factors_measured; tee-strategy.ts:316 likewise. calcConfidence's own doc (types.ts:186-187) states: "`undefined` (the default) keeps the legacy blend for backward compatibility: every v2 miner is unchanged." The finding's "+0.3 recency floor that the v3 base guards against" critique misapplies the v3-only factors_measured contract (generator-base.ts:235-237) to the v2 adapter; the v2 contract deliberately keeps the legacy blend.
2. The only element not paralleled by the native miners is `variance = confidence`. The native miners feed a genuine measured dispersion (clamp01(1 - stddev/20)); the adapter feeds the record's self-reported confidence. This is a real conflation, BUT it is a defensible degraded fallback: I verified at the legacy build sites (insights.ts:919-928, 954-962) that legacy v1 records carry only confidence/support/sample_n/stroke_impact — NO per-round stddev/dispersion exists for the adapter to compute a true variance from. The aliased value is at least monotonic with reliability, not a wrong stat formula. There is no unit blend, wrong denominator, missing 18-normalization, division-by-zero, or SG sign error — none of the stat-correctness defect classes this audit targets.
3. The function is explicitly @deprecated (to-insight-input.ts:163-172) and already carries audit-driven fixes (Q-NEW-1/A-NEW-3: it now refuses to emit below MIN_SAMPLE_N rather than inflating, and no longer fabricates comparison_value at 0.9x). The 2026-05-17 audit touched exactly this confidence path and intentionally left the legacy blend in place.

Net: mechanically real, but it is a confidence-modeling choice in a deprecated legacy adapter — the recency/factors_measured behavior is documented convention, and variance=confidence is a defensible fallback given no dispersion data. Not a net-new miscalculation. The variance aliasing is at most a minor quality nit worth a code comment, not a calculation bug.
- (coachhelm-delivery) computeTrend uses a flat 0.5 threshold for all categories — far too sensitive for percentage metrics (Driving FW%, Approach GIR%), making 'stable' effectively unreachable for those cards — VERIFIED the mechanics: team-category-insights.ts:196 sets `const threshold = 0.5` and applies it uniformly in computeTrend (lines 198-204) to the per-round metric values produced by roundMetricValue (lines 145-178). Driving (FW%, lines 146-151) and Approach (GIR%, lines 152-157) are 0-100 percentages; Putting (~32 ppr, 163-169) and Scoring (~3-5 vs par, 170-175) are small numbers. So conceptually the finding is right that 0.5 is a tight band on a percentage scale and a generous band on putts/scoring. The trend feeds player-row chips (line 776), categoryTrend (810-815), and the coach-facing insight copy (218-233), so in principle a too-sensitive threshold could inflate improving/declining counts.

BUT the finding is filed as a concrete miscalculation on the Demo University Golf data, and that specific claim is FALSE. I re-derived per-player recent-5 vs prior-5 deltas in SQL, mirroring the exact code path (status='completed', order round_date desc, global limit n*10, roundMetricValue formula, filter nulls then slice 0-5 / 5-10):
- Driving FW deltas: 0.00, -5.71, +5.71, +8.57, -5.71, +10.00
- Approach GIR deltas: -16.67, -5.56, +7.78, -18.89, -7.78, -15.56
Of the 12 percentage deltas: 1 is <0.5 (stable under ANY threshold), 0 land in the disputed 0.5-3.0pp band, 0 land in 3.0-5.0pp, and 11 are >=5.0pp. Root cause: per-round FW% moves in ~7.1pp steps (~14 fairways) and GIR% in ~5.6pp steps (~18 opps), so 5-round averages differ by either ~0 or several points and essentially never land in the 0.5-3.0pp "noise band" the finding's thesis requires.

Therefore swapping the flat 0.5 for a correct unit-aware 3-5pp threshold yields the IDENTICAL trend label for all 12 percentage cases, hence identical improving/declining counts and identical headline tone. The finding's own grounded example (Jackson Hale FW -5.71 -> declining) is classified 'declining' under BOTH thresholds, so it demonstrates the threshold is irrelevant there, not a defect. The claim 'nearly every player reads improving or declining, almost never stable [because of the tight threshold]' does not hold: the non-stable reads are driven by genuinely large multi-point swings, not by sub-3pp noise crossing a 0.5 line. No net-new miscalculation is demonstrable on the ground-truth data.
- (coachhelm-delivery) getInsightsForCoach DB pre-rank orders by SIGNED evidence->strokes_impact while the in-app rank uses |impact| — a high-magnitude NEGATIVE-impact row can be dropped at PRE_RANK_FETCH before the magnitude re-rank sees it — The code asymmetry is real: getInsightsForCoach (insight-delivery.ts:484) orders the DB candidate window by `evidence->strokes_impact` SIGNED descending and caps at PRE_RANK_FETCH (50-100), then re-ranks in-app by feedRankScore = cappedStrokesImpact(|impact|) x confidence (line 523). The player fetcher (line 356) instead orders by created_at DESC and re-ranks in-app by the same magnitude composite. So the coach pre-rank key is signed while the re-rank key is magnitude. In theory a large-magnitude NEGATIVE-impact v3 row could sink to the bottom of the signed order and be truncated before the magnitude re-rank sees it.

But the premise — "a real leak stored as a negative strokes_impact" — does not hold for v3, verified two ways:
(1) DATA: across the ENTIRE DB (not just Demo University) there are 0 negative strokes_impact values among all 196 v3 rows. The 23 negatives are all engine_version='v2', which V3_ENGINE_FILTER ("engine_version.eq.v3,signature.like.v3:%") excludes before the .order/.limit is applied.
(2) CODE: the v3 engine writes strokes_impact as a non-negative leak magnitude by design. Generators emit `delta * N` only behind a positive-leak guard (e.g. front-9-starter.ts:64 `if (delta < MIN_DELTA) return null`) or a literal 0; RankableInsight.strokes_impact is documented "Magnitude only — sign just indicates direction" (score.ts:74); and the composite backfill applies Math.abs (synthesis.ts:69). A negative v3 strokes_impact is not a state the write path produces.

Even ignoring sign, the cap is never reached. PRE_RANK_FETCH = min(100, max(50, limit*5)) = 50-100; with the default limit=20 it is 100. getInsightsForCoach is RLS-scoped to a coach's team(s), and the largest single-team visible v3 candidate set in the whole DB is 75 (Demo University; others 67/30/3). With <=75 rows under a cap of 100, nothing is truncated and the in-app re-rank sees the full set regardless of DB order. For the truncation class to manifest you would need BOTH >100 visible v3 candidates for one team AND negative-magnitude v3 rows — neither exists. The finding's own 'actual' field concedes it is not triggerable. Additionally, for the dominant all-positive case the signed order IS magnitude order, so it keeps the highest-impact rows first — the opposite of a truncation risk. Per audit guidance (default false unless a defect is demonstrable), this is a latent consistency nit, not a net-new miscalculation.
- (coachhelm-delivery) getTeamCategoryInsights rounds fetch caps at playerIds.length*10 over a GLOBAL newest-first window — latent per-player starvation that silently shrinks a player's trend windows — I independently re-derived the truth at src/app/golf/actions/team-category-insights.ts:707-713. The fetch is `.in('player_id', playerIds).eq('status','completed').order('round_date',{ascending:false}).limit(playerIds.length*10)`, then grouped per player (lines 733-740) and split via `slice(0,5)`/`slice(5,10)` (lines 766-767) for computeTrend (lines 185-205, which early-returns 'stable' when previous.length===0).

SQL ground-truth on Demo University Golf (id LIKE '6ecdd1a6%'):
- 7 active members, 6 with completed rounds -> cap = 7*10 = 70.
- 78 total completed rounds for active members, so the 70-cap DOES truncate 8 rounds (78>70).
- Replicating the exact query (ORDER BY round_date DESC, LIMIT 70) and counting per player inside the window: every round-having player retains 11 or 12 rounds (min 11). Truncation removes 3 rounds each from the two 15-round players (Hale, Rini -> 12 left), 1 from Park (-> 12), 1 from Carter (-> 11). The lowest-volume players (Carter 11, Hayes 11) still clear the 10 needed for the recent(0-5)+previous(5-10) split.

Therefore NO player's trend window is shrunk or emptied; computeTrend receives a full recent+previous split for all 6 players; no stat is miscalculated on the audited data. This exactly matches the finding's own 'actual' statement ('NO starvation is triggered on the current Demo University Golf data') and its self-assigned 0.5 confidence / 'latent footgun' framing.

The audit scope is explicitly 'NET-NEW CORRECTNESS ISSUES ONLY' and 'only flag a real defect, not a defensible convention.' A shared budget that produces zero miscalculation on the data — and only could starve a player under a future, much larger/skewed roster — is a robustness/scalability suggestion, not a present-tense correctness defect. It is not a 'genuine net-new miscalculation.'

Separate observation (NOT this finding, and I do not upgrade the finding on it): the query has no secondary sort key, and SQL shows 60 of 78 rows share a round_date (only 18 distinct dates). That makes the intra-date ordering — and thus the precise recent/previous slice boundary — nondeterministic across page loads, which is a more defensible technical concern than the starvation claim. But it is a different issue (ordering stability, not a global-cap starvation), and it too does not change any aggregate the way the finding describes. The starvation finding as written is not a real bug.
- (coachhelm-delivery) Theme strength/leak state and SG trend are sourced from two different SG pipelines (shot-recomputed sg*PerRound vs stored golf_rounds.strokes_gained_*) that can disagree — CODE PATH VERIFIED. assembleForPlayer (insight-delivery.ts:796-802) sources sgByCategory from getDetailedStatsAsAdmin().sg*PerRound — shot-recomputed via calculateStrokesGainedForShot, summed per category, divided by rounds.length (golf-stats-calculator-shots.ts:2000-2045, 2337-2340). The trend (fetchSgTrendsByCategory, lines 707-743) reads the STORED golf_rounds.strokes_gained_* columns and runs computeSgTrends (trend.ts). These ARE two independent SG computations, exactly as the finding states. assemble.ts:368-389 uses sgPerRound (shot pipeline) for state/sizing; assemble.ts:408-409 attaches the stored-column trend separately. So the structural claim is factually correct.\n\nGROUND-TRUTH SQL (Demo University). Stored per-category columns are implausibly large: e.g. Nick Rini 2026-05-31 sgp=-11.34, sgt=+9.22 in ONE round; team avg sg_putting ~ -4.7 to -6.3/round, sg_tee ~ +5.9 to +7.8/round — far beyond real per-category SG (±1-3). Internally consistent (sum4 = strokes_gained_total, reconciles to score-to-par), but the per-category split is miscalibrated demo/seed data. So the two pipelines indeed will not agree numerically — the finding's premise holds. A renderable direction mismatch is plausible: Nick's stored approach trend is declining (recent5 0.18 vs prior5 2.13 → red 'Declining' chip), while his stored approach level is slightly positive (+0.716), so the shot pipeline could yield a positive sgApproachPerRound → 'Approach — a strength' headline beside a red declining chip.\n\nWHY NOT A BUG: (1) State (current LEVEL: strength/leak) and trend (DIRECTION/trajectory: improving/declining) are orthogonal axes, not contradictory. 'You're a strength here but slipping recently' is a coherent, true statement about two different quantities — not an arithmetic error. (2) The mixed source is DOCUMENTED and DELIBERATE: ThemeCard.tsx:164-167 explicitly says the trend shows direction ONLY (scale-invariant) and intentionally omits the numeric delta because 'the raw golf_rounds SG column is on a different scale than the normalized per-round magnitude shown above, so a numeric delta here would be inconsistent.' trend.ts header and assemble.ts:193-204 both document the trend reads stored columns while state reads sg*PerRound. (3) Because the trend is direction-only, NO number from the stored pipeline is ever shown next to a number from the shot pipeline — the scale-disagreement concern (the finding's strongest numeric point) is already mitigated by design. (4) computeSgTrends math is correct and self-consistent (finding agrees); state derivation (deriveState) is correct. The only residual issue is the implausible stored demo-data SG columns — a DATA-QUALITY problem in the seed dataset, not a wrong formula/denominator/unit/sign/normalization defect in this code path. (5) Finding self-rates 0.4 and concedes it is 'Not directly provable as a contradiction.' This is a defensible, documented level-vs-trajectory design, not a net-new miscalculation.
- (player-profile) approachStats.totalApproaches uses Math.max of two unrelated counts, not a real approach count — I independently re-derived the truth. Code at src/app/golf/actions/shot-analytics.ts:388 sets totalApproaches = Math.max(girEvaluatedHoles, approachShots.length), where girEvaluatedHoles (357) = holes with gir IS NOT NULL and approachShots (355) = shots typed 'approach'/'iron'. SQL over the demo team confirms the two counts are NOT close (contradicting the finding's own rationale): Owen 30d girEvaluatedHoles=126 vs approachShots=168; all 6 players show approachShots 9-33% larger; across all 78 team rounds approachShots > girEvaluatedHoles in 100% of rounds (0 rounds where GIR exceeds approach, 0 rounds with GIR-evaluated holes but zero approach shots, min 10 approach shots/round). Therefore Math.max ALWAYS returns approachShots.length for this data. The two surfaces that DISPLAY the value label it 'approach shots analyzed' (ShotAnalyticsPanel.tsx:277) and 'shots' (ShotTypeBreakdown.tsx:233) — and since the returned value equals the true approach-shot count, the label is accurate. The other three usages (ShotTypeBreakdown.tsx:220 section gate, FairwayPlayerCoachHelm.tsx:1025 radar-axis gate, :1112 Readout live/awaiting gate) use it only as a `> 0` boolean, for which the max is equivalent to either non-negative input. Critically, the max NEVER feeds any percentage or downstream calculation: girPct is computed independently as calculatePercentage(girHoles, girEvaluatedHoles) at line 392, and avgProximity uses approachProximities at line 403. So totalApproaches is purely a display/gating count and cannot distort any stat. The only theoretical mislabel — a hole-summary round with GIR set but zero shots logged, where Math.max would surface girEvaluatedHoles (a hole count) under a 'shots' label — does not occur in any data, and the max is arguably a deliberate defensive guard so the Approach panel still renders when only hole-level GIR signal exists. No competing 'approach shots' count exists on other surfaces (stats.ts/stats-v2.ts/dashboard-data.ts), so there is no cross-surface inconsistency. This is a stylistically inelegant guard (mixing a hole count and a shot count), not a net-new miscalculation; it produces no wrong number for any extant data and corrupts no percentage. Verdict: defensible convention, not a real bug.