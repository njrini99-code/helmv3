# PR #105 split plan (Codex Phase 6)

**Source:** `chore/helm-review-2026-05-27` → `main`
**Size today:** 60 files, +1,252 / −134
**Mandate:** Keep the app fixes the helm-review job actually produced.
Drop the historical-migration whack-a-mole edits, because PR #117's
prod-schema baseline obsoletes them and PR #112's migration-lockdown
workflow now blocks edits to historical migrations anyway.

---

## Split proposal

### PR #105a — App fixes (the intent of helm-review)

The brief at `docs/archive/2026-05/daily-briefs/2026-05-27.md` says this PR is about
the active `synthesizeForPlayer` crash, log-noise downgrades, and
v2-mining GATED_OUT hardening. Keep exactly that:

| File | Change | Why keep |
|---|---|---|
| `src/lib/coachhelm/v3/composite/loader.ts` | +4 / −1 | The 602-event/12h Sentry fix — `row.signature?.startsWith(…)` null guard. **Core of the PR.** |
| `src/lib/coachhelm/v3/composite/hole-sequence-loader.ts` | +6 / −1 | Same null-signature guard family. |
| `src/lib/coachhelm/v3/genome/dimensions/scrambling-rate.ts` | +8 / −1 | App-layer null guard, same theme. |
| `src/lib/coachhelm/v2/mining/approach-analytics.ts` | +4 / −1 | GATED_OUT short-circuit before `attachDrills`. |
| `src/lib/coachhelm/v2/mining/course-management.ts` | +9 / −3 | Same. |
| `src/lib/coachhelm/v2/mining/tee-strategy.ts` | +5 / −1 | Same. |
| `src/test/coachhelm/v2/mining/approach-analytics.test.ts` | +1 / 0 | Mock exposes `GATED_OUT`. |
| `src/test/coachhelm/v2/mining/course-management.test.ts` | +1 / 0 | Same. |
| `src/test/coachhelm/v2/mining/tee-strategy.test.ts` | +1 / 0 | Same. |
| `src/app/golf/actions/insights.ts` | +3 / −2 | Two `logServerError` `'warning'` downgrades for the expected `skip-insufficient` path. |
| `src/app/baseball/actions/games.ts` | +49 / −9 | Substantial null-guard / auth pass — verify scope but treat as app fix. |
| `src/app/baseball/actions/player-peek.ts` | +1 / 0 | Server-action auth/null tightening. |
| `src/app/baseball/actions/stats.ts` | +2 / 0 | Same. |
| `src/app/baseball/actions/tasks.ts` | +2 / 0 | Same. |
| `src/app/actions/demo-request.ts` | +5 / 0 | Same. |
| `src/app/actions/messages.ts` | +2 / 0 | Same. |
| `src/app/golf/actions/golf.ts` | +2 / 0 | Same. |
| `src/app/golf/actions/message-attachments.ts` | +1 / 0 | Same. |
| `src/app/golf/actions/onboarding.ts` | +1 / 0 | Same. |
| `src/app/golf/actions/player-notifications.ts` | +5 / 0 | Same. |
| `src/app/golf/actions/push-notifications.ts` | +1 / 0 | Same. |
| `src/app/golf/actions/tasks.ts` | +2 / 0 | Same. |
| `docs/archive/2026-05/daily-briefs/2026-05-27.md` | +121 / 0 | The brief that explains the PR. |

**~23 files. The clean, mergeable core.**

### PR #105b — Reviewer/lint tooling (optional, can come later)

Mostly self-contained config noise from the helm-review run. Worth its
own focused PR with its own review:

| File | Change | Notes |
|---|---|---|
| `.coderabbit.yaml` | +1 / −1 | |
| `.coderabbit/ast-grep/*` | 5 files | Custom rules. |
| `.coderabbit/semgrep/helmv3.yml` | +121 / −20 | **Fix Greptile's flagged misleading comment** before re-PRing — the listed migration files are not in the 31-function ALTER list. |
| `.github/workflows/ci.yml` | +1 / −1 | |
| `.github/workflows/review-gate.yml` | +22 / −5 | sg/semgrep severity/scope fixes. |
| `.greptile/config.json` | +1 / 0 | |
| `.greptile/instructions.md` | +62 / 0 | New rules — overlaps with the file PR #112 already created on main. **Diff against main before re-PRing — likely now redundant.** |
| `.yamllint.yml` | +43 / 0 | New file. |
| `CLAUDE.md` | +396 / 0 | Big enough to deserve its own PR. May overlap with the inventory-regen workflow in CLAUDE.md "Auto-regen inventory docs" section — diff carefully. |
| `package.json` | +1 / −1 | |
| `scripts/apply-migration.sh` | +2 / −1 | |
| `evals/round-review.yaml` | +4 / −4 | |
| `tools/continuous-improvement/*.py` | 14 files | Lint/format fixups. |
| `tools/ux-flow-auditor/scripts/analyze_flows.py` | +4 / −6 | Same. |
| `tools/overnight.py` | +4 / −4 | Same. |

**~30 files. Low-stakes, independent review.**

### DROP — historical-migration whack-a-mole

These exist because the helm-review job tried to patch the broken
replay by editing the historical files. PR #117's baseline makes them
both unnecessary and forbidden (`.github/workflows/migration-lockdown.yml`
from PR #112 will block them now that it's on main):

| File | Change | Why drop |
|---|---|---|
| `supabase/migrations/040_golf_shot_system.sql` | +45 / −2 | This was the file that broke replay in the first place. Baseline at `20260527000000_prod_public_baseline.sql` (PR #117) carries the actual prod state. |
| `supabase/migrations/042_sport_specific_messaging_tables.sql` | +6 / −1 | Same. |
| `supabase/migrations/050_golf_performance_indexes.sql` | +48 / −9 | Same. |
| `supabase/migrations/056_task_enhancements.sql` | +4 / −1 | Same. |
| `supabase/migrations/057_document_versions.sql` | +20 / 0 | Same. |

These files are now under `supabase/migrations_archive/pre_20260527/`
on the baseline branch. Edits there don't participate in replay.

### DROP for now — new search_path hardening migration

| File | Change | Why drop FROM #105 |
|---|---|---|
| `supabase/migrations/20260527160000_v3_security_definer_search_path_hardening.sql` | +143 / 0 | New forward migration that ALTERs `search_path` on 31 SECURITY DEFINER functions. **Needs verification against PR #117's baseline first.** If the prod schema dump already shows `SET search_path` on these 31 functions (likely), the migration is redundant. If not, re-propose as its own focused follow-up PR after #117 lands. |

Verify with:

```sh
grep -E "SET search_path = '?pg_catalog, public'?" \
  supabase/migrations/20260527000000_prod_public_baseline.sql | wc -l
```

Compare to the 31 functions in the dropped migration. If count matches,
drop permanently. If not, re-PR as `feat(security): pin search_path on
N more SECURITY DEFINER functions` after #117 merges.

---

## Suggested execution order

1. **Wait for #117 to merge** (baseline lands; replay is now safe to validate against).
2. **Open #105a** from a fresh branch off the post-merge `main`:
   ```sh
   git checkout main && git pull
   git checkout -b chore/helm-review-2026-05-27-app-fixes
   # cherry-pick or git checkout the 23 app-fix files from chore/helm-review-2026-05-27
   ```
3. **Close #105** with a comment pointing at #105a (and #105b if opened).
4. **Verify the dropped hardening migration** against baseline as above; only re-PR if a gap exists.
5. **Open #105b** at leisure if any of the lint/tooling changes are still wanted.

---

## Risks / unknowns

- `src/app/baseball/actions/games.ts` (+49 / −9) is the largest non-trivial app change. Worth a focused diff read before bundling into #105a — if it's auth-tightening, ship it; if it's coupled to the dropped baseball-recalc RPC body guards from the original session report, hold for a separate PR.
- `CLAUDE.md` (+396) may overlap with the on-main version after PR #112's `docs/setup/SUPABASE_MCP_SETUP.md` updates and the inventory-regen workflow. Diff against `main` before deciding whether to land any of it.
- `.greptile/instructions.md` (+62) likely conflicts/overlaps with the on-main version landed in PR #112's hard-rules addition. Diff first.
