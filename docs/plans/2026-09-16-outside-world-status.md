<!-- markdownlint-disable MD013 -->
# Outside-world / production player view: execution status

Tracks the production player-view, outside-world detail and non-playing
context rendering spec (2026-09-16) on `agent/golf-course-geometry`, in the
spec's own immediate action order (§42). Status values as in the Meridian
tracker: `done` (implemented and verified, evidence named), `partial`,
`pending`, `blocked` (needs a human review pass or a device).

Authoring contract: `docs/design/meridian-outside-world.md`.
Proof course: Peek’n Peak Upper (package `fdec6ea8…`).

**Open gates (2026-09-18, head `56bceaf05`).** Every agent-doable row is
closed; what remains needs the owner: the §39 per-hole context answers
(the uncertain-share gate passes 1 of 18 holes and moves only with that
classification pass, not with rendering), the device run behind §103 of
the Meridian tracker, and landing #1939. Action 10's lab and production
signoff is current on `4eba1fb9f` (`canaries/v29-package` 96/96,
`player/audit-v12` 36/36, all within the draw-call budget).

## §42 immediate action list

| # | Action | Status | Evidence / remaining |
| --- | --- | --- | --- |
| 1 | Keep the current matrix as an internal QA artifact | done | Course matrix, canaries and the lab stay under `src/test/fixtures/course-geometry/browser` and `scripts/golf/course-geometry/capture-*.cjs`; nothing player-facing links to them (§2.1) |
| 2 | Define the outside-world taxonomy in code and authoring docs | done | `src/lib/golf/course-geometry/context-taxonomy.ts` (9 groups, 50 classes, fidelity tiers §33, render treatment, §39 prompts), shared OSM rules `context-rules.json`, doc `docs/design/meridian-outside-world.md`; test `context-layer.test.ts` |
| 3 | Add a context/landcover pass to the pipeline | done | `fetch-osm-context.py` (bounded, retained: `sources/peek-n-peak-upper-osm-context`, 607 elements) → `prepare-context-layer.py` → `peek-n-peak-upper-context.json` (`golfhelm-context-layer-v1`, 168 zones, locked to the package hash) + `peek-n-peak-upper-context-report.json`; `parseContextLayer` refuses other packages (`MERIDIAN_CONTEXT_LAYER_MISMATCH`); scenes carry `contextZones` |
| 4 | Review the worst “anonymous green space” holes first | partial | Report ranks holes by unexplained share of their drawn context (beyond the derived rough bands and every source zone): 2 (49 %), 8 (43 %), 7 (41 %), 4 (39 %), 6 (35 %), 3 (35 %). Rendering of the derived rough hierarchy and zones follows; the human §39 pass stays open Terrain response (fidelity §35, `terrainTone`, 2026-09-17) now varies that anonymous rough by DEM shelter/exposure, so the open flanks carry terrain-following tone without claiming a class. The §39 prompt sheet gives each of these holes its evidence and draft answers (2026-09-17). |
| 5 | Add cart path and built-context support | done | `three-context.ts`: terrain-conforming ribbons (cart/service/road/crossing/bridge/stream/drainage, style `contextObjects.ribbons`), building/clubhouse/maintenance extrusions from the terrain, fence/wall/lift lines; counts in telemetry (`contextRibbons`, `contextStructures`, `contextZones`); lab `ow1-*`, `rh1-*` captures. Open: minor ribbon gaps at OSM way ends |
| 6 | Improve forest-edge generation and understory logic | done | Understory shrub clusters inside the reviewed forest edge (1.5–10 m band, budget 220, `three-landscape.ts`, style `vegetation.understory`); OSM `forest_mass` zones outside every reviewed mask carried by context-toned lobes only (budget 260; hole 9: 49, hole 10: 13; holes without OSM woods stay at 0 — source-only). Telemetry `understory`, `contextMassLobes`; captures `rh2-hole{07,09,10}-*` |
| 7 | Introduce rough hierarchy materials and masks | done | Compiler `compileRoughHierarchy` (visual-artifact.ts, compiler-2, style meridian-v8): `surroundDistanceCm` per rough/ground vertex, classes `rough_secondary` (≥10 m) / `rough_outer` (≥28 m) with ±3/±6 m blends, slope-only darkening, outer macro ×1.6 in the shader; ground zones painted from source-backed context zones (`GROUND_ZONE_CLASSES`: parking, wetland, ski slope, recreation, open field, buffer grass, native), `uncertain` zones paint nothing; `contextLayerHash` in the artifact + assert gate. Tests: visual-artifact.test.ts (bands, zones, gate). Captures `rh1-hole{02,07,11,17}-*` |
| 8 | Add production camera presets and state-based framing | done | `TERRAIN_PRESETS.tee/approach/green/putting` + `PRODUCTION_CAMERA_STATES` + `productionCameraState(view)` in terrain.ts; the entry context opens in the state for the current shot view; lab exposes the presets; captures `cs1-hole07-{tee,approach,green,putting}-desktop` |
| 9 | Remove permanent debug-style controls from the player view | done | Entry context now: hole pill (area chooser) · Close · View group Terrain / Top / Green · overflow (Reset view, Details and sources). Zoom rail, Side, Profile and the camera tools render only in review. `scripts/golf/course-geometry/capture-player-view.cjs` proves the chrome (`pv1-hole07-*-phone.json` → chrome=[Choose course area, Close, Terrain, Top, Green, More]) |
| 10 | Re-run whole-course signoff (lab + production mode) | partial (human §39 open; production binding closed 2026-09-17) | Lab signoff re-run at v9 after the breakline recompile and the LOD fix: `canaries/v9-lod` (96 captures, 0 errors, every capture within its draw-call budget, `crownLodBasis: screen_px` on perspective presets); diff against `v9-breaklines` worst 0.97 % (side views, the tree LOD), median 0.02 %. Re-run again after the water tune, near budget and asset breakup: `canaries/v9-final` (96 captures, 0 errors, all within budget); diff against `v9-lod` worst 9.4 % (hole 1, its ponds), median 1.6 %. Production mode: the play-round fixture binds the context layer like the lab (`contextLayerFor(course)`); the app's own geometry bundle loader (P6 bindings) does not assemble `terrainByHole`/`contextLayer` yet, so production binding stays a correctness-package item, not a visual one Re-run after the forest-mass clusters (master §39): `canaries/v10-mass` (96 captures, 0 errors, all within budget); diff against `v9-final` worst 4 % (hole 18 Side), median 1 %. Re-run on the `course-terrain-v3` noded meshes: `canaries/v11-noding` (96 captures, 0 errors, all within budget); diff against `v10-mass` worst 0.05 % (the crack pixels), median 0.01 %. `v12-dem` (96 captures, 0 errors) after `context-contact-v3` and per-fragment DEM slope shading: diff against `v11-noding` worst 0.09 % (hole 7 top/terrain, the road-bank wedge), every capture under 0.1 %. `v13-sky` (ribbon draping, below-horizon sky tone) and `v14-normals` (`course-terrain-v4` fixtures without the per-vertex normals array, 37 % smaller per hole): 96 captures each, 0 errors, all within budget, pixel-identical to the previous label at the canary tolerance. `v15-roofs` (hipped roof archetype): 96 captures, 0 errors, all within budget; diff against `v14-normals` worst 0.21 % (hole 9 side, its houses), median 0.008 %, 36 captures identical. `v16-landform` (DEM landform occlusion): 96 captures, 0 errors, all within budget; every capture within the 24-level canary tolerance of `v15-roofs` (the occlusion moves at most 21 levels: the lab A/B at zero tolerance changes 13 % of hole 7 side pixels by more than 8 levels, 4 % of hole 18). `v17-green-mowing` (compiler 8): 96 captures, 0 errors, all within budget, all 96 identical to `v16-landform` at the canary tolerance (the green bands fade out at hole-view scale; the phone green state is the evidence). Whole-course phone audit after v17: `scratchpad/audit-v5`, 18 holes × terrain + green, 36 captures, 0 page errors, 13–14 draw calls each. `v18-terrain-tone` (2026-09-17, after the DEM shelter/exposure tint): 96 captures, 0 errors, every capture within budget; identical to `v17-green-mowing` at the 24-level tolerance because the tone moves at most 14 levels (lab zero-tolerance A/B on hole 7: 18 % of pixels > 4 levels). `v19-path-tone` (muted cart-path tone, master §54): 96 captures, 0 errors, all within budget; identical to `v18` at the 24-level tolerance (the ribbon albedo moves 13 levels). The §39 per-hole answers are drafted from evidence in `2026-09-16-outside-world-review-prompts.md` (derived rows answered, human rows carry their numbers); the human confirmation and the production binding stay open. `v20-crown-shade` (crown self-occlusion, master §50): 96 captures, 0 errors, all within budget, gate unchanged (15 passes); at tolerance 6 a median .7 % of pixels move (max 5.1 %, hole 17 Side phone). `v21-one-tap` (2026-09-16, after the One-Tap marker overlay joined the runtime SVG and HoleSceneFrame gained the stage presentation): 96 captures, 0 errors, all within budget, gate unchanged (1 of the 8 canary holes passes: 15); all 96 pixel-identical to `v20-crown-shade` at the canary tolerance — an empty marker layer costs nothing and moves nothing. Player mode, same day: the production player view (`capture-player-view.cjs`, phone, Terrain + Green states) on all 18 holes → `player/audit-v6/hNN-{terrain,green}-phone.png`: 36 captures, 0 page errors, 9–15 draw calls (390 k–736 k triangles) per hole with the context layer bound, identical chrome on every hole (area, Close, Terrain/Top/Green, More). The old "~350 draws in player mode" figure is gone since the V7 batching; lab and player mode now agree. One-Tap follow-fit + HUD keep-out re-run (2026-09-16): `canaries/v22-follow-fit` and `canaries/v23-hud-reserve` (96 each) pixel-identical to `v21-one-tap`; player matrix `player/audit-v7` (36, phone, terrain + green, 18 holes) pixel-identical to `audit-v6`; One-Tap captures `one-tap/peek-n-peak-upper-07-v5` and `-08-v5` record framing + zoom per step (`docs/plans/2026-09-16-one-tap-status.md`). Production binding (2026-09-17): the app's own loader now assembles `terrainByHole` and the context layer (`loadCourseAssets`, 699428f6b), the published Upper assets under `public/course-geometry/peek-n-peak-upper/` are served byte-identical from the preview deployment (46e358357, verified through `vercel curl`), and the live One-Tap round renders the V2 world over them — so production mode is bound the same way as the lab. Whole-course production-mode audit on that path: `capture-one-tap.cjs --world=v2` on all 18 holes (`one-tap/audit-v2-world`, Mac Chromium 390×844): 0 page errors, draws 10–29 tee / 8–23 green, geometry 10.5–19.1 MB per hole, chrome identical on every hole; the mount compile (4.6–13.4 s per hole, now halved: hole 7 8.5 → 4.0 s, bd010b92a) is the open performance item, tracked in the Meridian V2 tracker (Task 20). Re-run 2026-09-17 morning on c83cb2091, after the forest re-LOD (a085a9fcc), the per-bunker floor shade (fce6348a7) and the five output-identical mount-compile passes: lab matrix `canaries/v24-compile3` (96 captures, 0 errors, every capture within its draw-call budget at 6–10 draws / 73 k–373 k triangles, uncertain gate unchanged at 1 of 8 holes: 15); player mode `player/audit-v8` (`capture-player-view.cjs`, phone, Terrain + Green, all 18 holes: 36 captures, 0 page errors, 9–15 draws, 390 k–736 k triangles, the same chrome on every hole — the same figures as `audit-v6`/`audit-v7`); One-Tap production mode on the V2 world `one-tap/audit-v2-compile3` (18 holes, 0 page errors, mount compile .71–1.46 s, Task 20). The earlier labels no longer exist on disk in this checkout (`output/` was cleared for space), so `v24-compile3`/`audit-v8` are the on-disk baselines for the next diff rather than diffed themselves; the V2 frames were diffed in the V2 tracker (`v2-14-compile3`, 0.00 %). Re-run 2026-09-17 evening on the landing-review head (One-Tap stages and per-round switch, the production-parity fix for rounds without geometry, main merged): lab matrix `canaries/v25-parity` (96 captures, 0 errors, 6–10 draws / 73 k–373 k triangles, every capture within budget, gate unchanged at 1 of 8: 15) — all 96 pixel-identical to `v24-compile3`; player mode `player/audit-v9` (18 holes × Terrain + Green, phone: 36 captures, 0 page errors, 9–15 draws) — all 36 identical to `audit-v8`. Same evening, after the full-circle orbit and pinch-tail fix (0e90557f0): hole 7 canaries `v26-orbit-h7` (12 captures) identical to `v25-parity` (presets unchanged), and the player view orbits hole 7 through 0/90/180/270/360° with 0 page errors (`output/lab-build/camera-orbit/h07`). Re-run 2026-09-17 late evening on 1049be462, after tap-to-measure joined the expanded 3D view (the ruler marker, its dashed link and the overlay reservation the Green pill now honours): lab matrix `canaries/v27-measure` (96 captures, 0 errors, 6–10 draws / 73 k–373 k triangles, every capture within budget, gate unchanged at 1 of 8: 15) — 95 of 96 identical to `v25-parity`, the one movement hole 15 Top phone at 0.00024 % (321 pixels in a 24 × 34 patch, `output/lab-build/diff-v25-v27.json`) — not the measure layer: that frame lost the pin flag glyph. Traced the same night (reproducer kept as `scripts/golf/course-geometry/probe-overlay-still.cjs`, exit 1 on any hidden read or overlay animation at the capture instant): the global reduced-motion rule (`globals.css`, every property on every element a 0.01 ms transition) turned the overlay `<svg>`'s ready-time visibility flip into a cascade, one DOM level per rendering update, so a still frame could catch the deepest nodes hidden — 8 of 60 capture instants read the pin or its glyph paths `visibility: hidden` with the DOM unchanged, two rendering frames always cleared it (0 of 120), and every Top capture is exposed the same way (Top is the default fit, so no easing frames follow the flip) — hole 15 phone was simply the one that flaked tonight. Fix d63243503: `svg[data-slot=terrain-overlay] *` starts no transition (0 of 60 hidden reads after; hole 15 canaries twice identical to `v25-parity`). Signoff re-run on the fix (d63243503): lab matrix `canaries/v28-still` — 96 captures, 0 errors, 6–10 draws / 73 k–373 k triangles, gate unchanged, all 96 pixel-identical to `v25-parity` (`output/lab-build/diff-v25-v28.json`); player mode `player/audit-v11` — 36 captures, 0 page errors, 9–15 draws, 390 k–736 k triangles, identical chrome, all 36 identical to `audit-v9` (`diff-audit-v9-v11.json`). The player and One-Tap capture scripts run under reduced motion too and mount the same slotted overlay (checked on the `?onetap=1` fixture: `svg[data-slot=terrain-overlay]`, `transition-property: none`, no animations after ready), so the same exemption covers them. `v28-still` and `audit-v11` are the go-forward diff baselines; `v27-measure` stays on disk with its flaked `hole-15-top-390x844.png` (do not diff against it). Since `v28-still` is 96/96 identical to `v25-parity`, that baseline held no flaked frame either — the chain `v24-compile3` → `v25-parity` → `v28-still` behind this signoff is clean. Player mode `player/audit-v10` (18 holes × Terrain + Green, phone: 36 captures, 0 page errors, 9–15 draws, 390 k–736 k triangles, the same chrome on every hole) — all 36 identical to `audit-v9` (`output/lab-build/diff-audit-v9-v10.json`). The tap-to-measure behaviour itself is proven in row 3 (lab probes + preview stage taps), not by these still frames. Re-run 2026-09-18 early morning on 4eba1fb9f, after the Upper-only package wiring (`useCourseGeometry` into the standard tracker, the new-round tracker and the review page; the Meridian Live pause fix): lab matrix `canaries/v29-package` — 96 captures, 0 errors, 6–10 draws / 73 k–373 k triangles, every capture within its draw-call budget, gate unchanged at 1 of 8: 15, all 96 pixel-identical to `v28-still` (`output/lab-build/diff-v28-v29.json`); player mode `player/audit-v12` — 36 captures, 0 page errors, 9–15 draws, 390 k–736 k triangles, identical chrome on every hole, all 36 identical to `audit-v11` (`diff-audit-v11-v12.json`). Production mode now has a second path besides the One-Tap round: any Peek'n Peak Upper round on the standard tracker (continue or new) and its review draw the same package through `useCourseGeometry`, proven on preview `helmv3-brhgsvdji` (course frame, 3D ready on expand, tap-to-measure, terrain for the hole on screen + next, 0 geometry requests on every other course; `output/lab-build/upper-only-package-report.md`). `v29-package` and `audit-v12` are the go-forward diff baselines. What remains for this action is human: the §39 per-hole confirmation (draft answers stand in `2026-09-16-outside-world-review-prompts.md`, all 18 holes, figures from the retained context report) and the uncertain-share gate it unlocks. Two denominators appear above: the canary matrix reports the gate over its 8 holes (1 of 8: 15) and the context report over the whole course (1 of 18: 15 at 13.5 %; next 11 at 15.4 %, 16 at 16.2 %); the rest are source-limited, not render-limited. |

## Spec sections

| § | Title | Status | Evidence / remaining |
| --- | --- | --- | --- |
| 0–1 | Summary, product direction | done | Encoded in `meridian-outside-world.md` (two products, hybrid immersive view) |
| 2 | Two products on one world | done | Lab/QA vs `HoleSceneFrame` entry context; separation documented; chrome reduction pending (§36) |
| 3 | Player UI/UX spec | done (v1) | Sparse chrome + View control + state camera landed (actions 8–9); drag/pinch gestures existed. §3.6 bottom-sheet principle: in production the overflow's Details and sources open in the shared `Sheet` (bottom, handle, scrim) over the course instead of expanding the inspector aside, so the default state stays course-first (`data-slot="player-details-sheet"`, `HoleSceneFrame.test.tsx`); review and lab keep the inline inspector. §3.4 double tap: two clean taps within 350 ms and 24 px reset the view to the state camera (verified on the phone player view: pitch 20 → 36 after a tilt drag), so no zoom rail or reset button sits on the canvas. Long-press inspect stays optional (not built) Gestures re-examined on the phone report (2026-09-17 evening, "moving my finger around and going the whole 360 and zoom"): the orbit is a full circle (yaw folded into (−180, 180], `wrapYawDegrees`; presets and the double tap ease the short way round), the orbit integrates from the previous move so a reversal answers at once after a tilt sits on its 20–90° limit, and the finger left on the glass when a pinch ends holds the camera until it lifts (before: every pinch closed with a ~9° unasked turn). `HoleSceneFrame.gestures.test.tsx`; hole 7 orbited 0–360° on the player view with 0 errors. Tap-to-measure (on-course ask, 2026-09-17: "enter the 3D, tap an area on the fairway and it tells you how many yards it is from where you are"): a clean single tap on the expanded terrain lays a ruler from the best position the round holds (YOU → newest BALL/anchor → last resolved shot → the tee, `tap-measure.ts`) to the ground under the finger via the runtime's read-only `pick` — dashed ground line, yardage at the point, and a chip under the hole pill with horizontal yards, ground rise in ft and the origin (`from you` / `from your ball` / `from the tee`; `data-slot="tap-measure"`). The number re-derives every render so it follows YOU; × or a double tap clears it, another hole or area drops it; nothing is recorded. The ruler's box is reserved by the evidence label layout (`SceneMarkerOverlayController.reservedRects` → `extraReserved`), so Green/pin labels move aside. Lab probe (`output/lab-build/tap-measure-probe.mjs`): One-Tap fixture 190 / 321 / 90 yd from YOU, 32 yd after walking 60 m; play fixture from the tee; preview stage (continue round, Meridian Live on, 7th tee): 16/16 taps, 0 errors. Canaries `v27-measure-h7` pixel-identical to `v26-orbit-h7`. |
| 4 | The outside-world problem | done | Measured: per-hole unexplained share in the context report |
| 5 | Taxonomy | done | `context-taxonomy.ts` |
| 6 | Do not fill with random stuff | done | Layer zones are source-only (`basis: source`), every default recorded; doctrine in the doc |
| 7 | What “more full” means | done | Semantic (taxonomy), spatial (zones per hole), textural (rough hierarchy, rendered since v5 with slope darkening and the open-rough bands), environmental (buildings/roads/lifts in the layer) |
| 8 | Landcover reconstruction pass | done | `prepare-context-layer.py` + report; human review pass open |
| 9 | Rough hierarchy | done | Action 7: distance bands, blends, slope darkening, outer macro; palette `roughSecondary`/`roughOuter`/`native` |
| 10 | Forest hierarchy | done | Mass / edge / edge trees (V4) + understory band + context forest mass (action 6); isolated trees come from OSM `natural=tree` when present (none retained for this course) |
| 11 | Forest placement principles | done | Source-backed masks, edge rhythm, taper by depth (V4) |
| 12 | Adjacent-hole context | done | Context features drawn quieter (Meridian V2/V5) |
| 13 | Structures | done | Extrusions with source heights/levels or class defaults (§13.3 default recorded per zone); walls, base sunk into the terrain; hipped roof archetype on small convex footprints, flat elsewhere (master §55, `visual_archetype_by_footprint`, never a source claim) |
| 14 | Cart paths | done | Terrain-conforming ribbons with a shoulder tone, widths from the layer; crossings/bridges as ribbon classes |
| 15–16 | Terrain storytelling, non-playing terrain zones | partial (classes human) | Slope darkening on secondary/outer rough and zones (slope only, no aspect, so albedo stays light-free); ski slopes painted from OSM `landuse=winter_sports`; swales, hollows and valley floors now read from the DEM's relative sky view (landform occlusion, fidelity §5–7) without any class being named; the named terrain-context classes (ridge/swale/…) still await the review pass DEM shelter/exposure tint on the rough hierarchy and plain ground (`terrainTone`, 2026-09-17): sheltered hollows richer, exposed knolls and embankments warmer; albedo only, classes unchanged. |
| 17 | “Not basic slop” rule | enforced (reporting) | Rule in the doc; since 2026-09-17 `capture-visual-canaries.cjs` reads the context report and records each hole's `uncertainShare` and `uncertainGate` (pass < 15 %) per capture plus a report-level pass/fail list, so every signoff states which holes still carry unexplained context. Today 1 of 18 passes (15 at 13.5 %); the rest are source-limited (human §39 classification) |
| 18 | Open rough visual language | done | Three tiers with distinct albedo, roughness and macro scale; blends over metres, never one pixel |
| 19 | Green-complex exterior context | partial | Surround/fringe rings, bunker bowls, edge trees exist; back bank / run-off zones await the review pass Run-off zones now come from slope evidence (fidelity §39–40, `greenComplex.runoff`, hole 18 mound falloff); back banks still await the review pass, and collection hollows need a contour source. |
| 20–21 | Hole identity, per-hole context pass | partial (human §39 open) | Report per hole; identity audit after rendering done three times on all 18 holes (fidelity 55–56, latest audit-v3 2026-09-16): hero/signature read on 1, 2, 7, 8, 9, 11, 16, 17, 18; 4, 5, 13, 15 keep open flanks and par-3 carries marked `uncertain`; 15 tee side and 12/10 bunker/green shapes are source-limited. The human classification pass (§39) is what closes the remaining anonymous share §39 prompt sheet (2026-09-17): `build-context-prompt-sheet.py` writes `docs/plans/2026-09-16-outside-world-review-prompts.md`, one section per hole with the evidence (class areas, derived bands, unexplained share and gate, paths, built context, neighbouring meshes, DEM relief, signature facts) and a draft answer per prompt, each marked `derived` (from the data) or `human` (needs the reviewer); confirmed answers go into the layer's `review.notes`. |
| 22–23 | Production camera composition and shot-context states | done | Action 8 |
| 24 | Labels and overlays | done | Only Green / shot badges are drawn; no category labels |
| 25–27 | Surface differentiation, edge treatment, ground breakup | done | Playing surfaces, bunkers, water, woods (Meridian V2–V5) + rough tiers, ground zones, paths and structures (actions 5, 7) |
| 28–30 | Shadow design, atmospheric depth, colour discipline | done | Fitted shadows, contact shade, capped haze, restrained palette (Meridian V1–V5) |
| 31–33 | Authoring pass, workflow, fidelity tiers | done | Doc + taxonomy + prep script |
| 34 | World-detail rule | done | Uncertain share measured per hole |
| 35 | Quality gates | enforced (reporting; 1 of 18 passes) | Gate defined (uncertain < 15 %, sources present, prompts answered) and reported per capture by the canary script (`uncertainGate`, `report.uncertainGate.pass/fail`); hole 15 passes at 13.5 %, 11 and 16 sit at 15–16 %, the worst are 2 (49 %), 8 (43 %), 7 (41 %). Passing more holes needs the §39 human classification pass, not rendering |
| 36–37 | Player-mode controls, core promise | done | Action 9 |
| 38–43 | Priorities, prompts, definition of good, principle, action list, short version | done | This tracker follows §42 in order; prompts coded |
