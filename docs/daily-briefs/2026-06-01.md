# Daily Review Brief — 2026-06-01

**Reviewer:** helm-review (autonomous)
**Sources:** Sentry (`helm-xs`, production) · Supabase `error_logs` + `admin_events` (Helm-Production `qmnssrrolpinvwjjnufo`) · Vercel runtime logs (`helmv3` production)
**Window:** rolling, last ~3 days
**PR:** `fix/daily-review-2026-06-01` → `main`

---

## TL;DR

No **new** production bugs surfaced today. Every actionable signal traces back to
**PR #197 never being merged** plus **production still running a stale deploy**.

This PR re-lands #197's fix cleanly on the current `main` (#194 `c8699958`),
excluding the unrelated doc commit that polluted #197's branch. The two highest-impact
actions remain **human-only**:

1. **Merge this PR** (branch protection blocks the autonomous runner — no self-approve, `--admin` denied).
2. **Promote `main` → production on Vercel.** Prod is pinned to **#189 (`572f8e0f`)**;
   **#190, #191, #193, #194** are all unpromoted. Promotion alone clears two live Sentry
   issues whose code fix is already on `main` (see "Already fixed, pending deploy").

---

## Production deployment status ⚠️

| | Commit | PR | State |
|---|---|---|---|
| **Current prod target** | `572f8e0f` | #189 | READY / promoted (stale) |
| `main` HEAD | `c8699958` | #194 | not promoted |
| Unpromoted between | `aa879ca4`, `efbfe8dc`, `d8cb9d81`, `c8699958` | #190, #191, #193, #194 | built, not promoted |

Vercel project `helmv3` (team `nick-rinis-projects`). Prod domain `helmsportslabs.com`.
Promotion is a Vercel dashboard / `vercel promote` action — outside the autonomous runner's reach.

---

## Sentry — production issues (6 open)

| # | Issue | Events | Culprit | Verdict |
|---|-------|--------|---------|---------|
| 2K | `causality unknown metric '…'` | 50 | `GET /api/cron/v3/causality-attribute` | **Deferred** — known v3 causality-registry drift (~45 non-canonical metric IDs). Kept reporting on purpose pending deliberate domain mapping. |
| 2J | `triggerPlayerInsightsAfterRound skipped N legacy records (insufficient sample_n)` | 18 | `GET /api/cron/coachhelm-roster-sweep` | **Fixed in this PR** — routine data-quality counter; now `skipSentry` (admin-table trail kept). |
| 1B | `[pattern-miner.thresholds] 0 patterns produced …` | 4 | `GET /api/cron/coachhelm-roster-sweep` | **Fixed in this PR** — routine pipeline signal; now `skipSentry`. |
| 13 | `[pattern-miner.thresholds] 0 patterns produced …` | 12 | `GET /api/cron/coachhelm-roster-sweep` | **Fixed in this PR** — same family as 1B. |
| 20 | `[insights.…] philosophy gate filtered N tier-1 insight(s)` | 3 | `GET /api/cron/coachhelm-roster-sweep` | **Fixed in this PR** — routine post-deploy verification signal; now `skipSentry`. |
| 2N | `ReferenceError: Can't find variable: Notification` | 1 | `/golf/dashboard` | **Already fixed on `main`** (`use-service-worker.ts:148` guards `typeof Notification`). Live event is stale-deploy lag — **clears on promotion.** |

Issues 2J/1B/13/20 will stop opening once this PR's `skipSentry` flag is deployed; they
remain visible in the `error_logs`/`admin_events` admin trace dashboard by design.

---

## Supabase `error_logs` / `admin_events` (last 4 days)

Top recurring signals (server-side cron telemetry; all addressed above):

| Message | Count | Severity | Status |
|---------|------:|----------|--------|
| `pattern-miner.thresholds.starvation` | 122 + 20 + 8 | warning/info | Silenced in Sentry by this PR (kept in admin tables) |
| `triggerPlayerInsightsAfterRound.skip-insufficient` | 44 + 12 + 8 + 8 | warning | Silenced in Sentry by this PR |
| `cron.v3.causality.unknown-metric` (~45 IDs × 4) | many | warning | Deferred (2K) |
| `insights.…gateMetrics` philosophy gate | 8 + 4 | warning | Silenced in Sentry by this PR |
| **`getPlayerFingerprint.rounds failed: column golf_rounds.course_par does not exist`** | **7** | **error** | **Fixed in this PR** (real bug) |
| `[Golf Auth Error]: Password is known to be weak …` | 12 | error | **Already fixed on `main`** (`auth.ts:268` early-returns the HIBP/weak-password case without logging). Pre-fix backlog; last seen 2026-05-29. |

**Not production** (localhost / `redesign/foundation` dev work — excluded):
- `Functions cannot be passed directly to Client Components … function Flag` @ `localhost:3000/golf/dashboard/qualifiers` (×12)
- `Rendered more hooks than during the previous render.` @ `localhost:3000/golf/dashboard/whats-new` (×4)

---

## Vercel runtime logs (production, last 3 days)

Essentially clean. One benign entry:
`GET /golf/admin/crm → 200` with an `AuthApiError` (invalid/expired refresh token) logged
mid-request — recovered (200), single occurrence, no action.

---

## What this PR changes

Re-lands PR #197's fix commit (`711ead45`) on top of current `main`, scoped to 4 files
(the stray #194 doc commit on #197's branch is excluded — #194 is now on `main` anyway):

1. **`player-fingerprint.ts`** — stop selecting the non-existent `golf_rounds.course_par`
   column; use `score_to_par` directly (it already encodes total − course par). Fixes the
   `column golf_rounds.course_par does not exist` error that was silently dropping every
   round from the player fingerprint. Verified against prod schema: `golf_rounds` has
   `score_to_par` and `total_score` but **no** `course_par`.
2. **`server-error-logger.ts`** — add an opt-in `skipSentry?: boolean` to the trace context.
   When set, the trace still writes to `error_logs`/`admin_events` (admin dashboard trail)
   but is NOT sent to Sentry. Default unchanged — every existing caller still reaches Sentry.
3. **`pattern-miner.ts`**, **`insights.ts`** — apply `skipSentry: true` to 4 routine,
   high-frequency CoachHelm cron telemetry signals (threshold starvation, skipped-record
   counters, philosophy-gate counts) that were drowning out real bugs in Sentry.

**Verification:** `npm run typecheck` → exit 0. Change is a strict subset of #197, which
previously passed full CI green (build, RLS, CodeRabbit, Review Gate).

---

## Action items

- [ ] **Human:** approve + squash-merge this PR (branch protection blocks the runner).
- [ ] **Human:** promote `main` → production on Vercel (clears 2N + weak-password deploy-lag,
      lands #190/#191/#193/#194). **Highest-impact action.**
- [ ] Close **PR #197** as superseded by this one (same fix, clean base).
- [ ] **Backlog:** v3 causality-registry mapping for the ~45 non-canonical metric IDs (2K) —
      deliberately still reporting; not a code fix, needs domain decisions.

---

*Generated by helm-review. Fixes implemented and verified locally; merge + promotion require a human.*
