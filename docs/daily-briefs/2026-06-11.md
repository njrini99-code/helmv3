# Daily Health Brief — 2026-06-11

**Author:** nightly health-check (autonomous) · **Repo:** `njrini99-code/helmv3` · **Branch:** `nightly-health-check/2026-06-11`
**Sources reviewed:** Vercel runtime logs + deployment list (`helmv3`, `prj_qPgC4eErTUsaSmv40EiQMNuTpuEV`, env=production, last 24h) · Vercel build logs for failed previews · Codebase grep + read for warning attribution
**Sources NOT reviewed:** Supabase `admin_events` / `error_logs` / `pg_stat_statements` — the Supabase MCP returned `HTTP 403 Host not in allowlist` on OAuth init; cannot run `execute_sql` in this session. Sentry MCP also not loaded (no session error data). **See §5 for the human handoff.**

---

## TL;DR

- **Zero production errors in the 24h window.** Zero `error` / `fatal` runtime events on env=production.
- **Three production-target deployments succeeded** (PR #186 merges + chained main pushes). All `target:"production"` deploys are `READY`.
- **Three failed deploys are all preview-only on unmerged WIP branches** — PR #266 (`feat/coach-team-switcher`) ×2 and PR #265 (`feat/distance-units-meters`) ×1. Not in scope for autonomous fix; branch authors are iterating.
- **Three warnings** in 24h. All three are on the documented skip list (philosophy-gate telemetry, stale-server-action 404, tracer batch progress). No action.
- **No PR fix commits opened.** Per protocol, opening a draft PR with this brief as signal.

---

## 1. 24h totals

| Metric | Count |
|---|---|
| Production runtime `error` + `fatal` events | **0** |
| Production runtime `warning` events | **3** |
| Distinct error patterns | **0** |
| Affected users (from runtime logs) | **0** |
| Production-target deploys (24h) | 3 (all `READY`) |
| Preview deploys failed (24h) | 3 (all unmerged WIP) |
| Cron failure events (`/api/cron/*`) | 0 (all 200) |

---

## 2. Top patterns (3 warnings, all SKIP-list)

| # | Time (UTC) | Path | Status | Level | Origin | Verdict |
|---|---|---|---|---|---|---|
| 1 | 03:45:14 | `GET /api/cron/coachhelm-roster-sweep` | 200 | warning | `insights.triggerPlayerInsightsAfterRound.gateMetrics` — `src/app/golf/actions/insights.ts:3354` | **Skip.** Routine philosophy-gate filter counter; `skipSentry:true`, fires when `tier1GateMetrics.gatedCount > 0`. Telemetry, not a bug. Cron returned 200. |
| 2 | 02:16:26 | `POST /` | 404 | warning | `instrumentation-client.ts:117` filter chain matches `/Failed to find Server Action/` | **Skip.** Stale-server-action 404 from a long-lived tab whose bundle predates the latest deploy — the explicit skip-list pattern. |
| 3 | 00:36:55 | `POST /golf/admin` | 200 | warning | `admin-tracer-data.ts:740` (`console.warn('[Tracer] Batch 1: …')`) | **Skip.** `console.warn` used as info-level batch-progress log. Status 200. Logging-hygiene noise, not a bug. |

No `severity:error` or `severity:fatal` rows surfaced. The two cron skip-list rules (pattern-miner threshold warnings + stale-server-action 404s) plus the audit-tracer info-log fully account for the 24h warning volume.

---

## 3. Performance — DEFERRED

`pg_stat_statements` top-10 slow-query report requires `mcp__supabase__execute_sql`, which is unavailable this session (see §5). No DB query telemetry collected.

If the human runner re-authenticates Supabase, the canonical query is:

```sql
SELECT calls,
       round(mean_exec_time::numeric, 2)  AS mean_ms,
       round(total_exec_time::numeric, 2) AS total_ms,
       left(query, 160)                   AS query
FROM   pg_stat_statements
WHERE  mean_exec_time > 1000
ORDER  BY total_exec_time DESC
LIMIT  10;
```

---

## 4. Deployment health

### Production-target deploys (24h) — all GREEN

| Deployment | Commit | Branch | State |
|---|---|---|---|
| `dpl_2UiXqwPZ4DAgpCHTW1gpX8UWG4XX` | `27f30d3` | main | READY |
| `dpl_6A66iDupMRsfKLymwchymvEzQduF` | `0aac78b` | main | READY |
| `dpl_zoi3kgjkGAH11gkxH2unU3ZZGhzC` | `3f711ea` | main | READY |

### Preview deploys with `state:ERROR` (24h) — all unmerged WIP

| Deployment | PR | Branch | Commit | First error line |
|---|---|---|---|---|
| `dpl_DxHcLX9u6rL1yeeLeZ5t25wUkN7F` | #266 | `feat/coach-team-switcher` | `410e5c0` | `src/app/golf/actions/team-switcher.ts:25` — `Only async functions are allowed to be exported in a "use server" file.` (`export const ACTIVE_TEAM_COOKIE = '…'`) |
| `dpl_3WmpQfLzxS1AZwKg14CXk4fJGgNz` | #266 | `feat/coach-team-switcher` | `b9b696c` | Same root cause as above (earlier commit on the same branch). |
| `dpl_G5WWVDuLy2oyJaCSX5XqKoETBoj8` | #265 | `feat/distance-units-meters` | `d6163e5` | Build failed at webpack stage (build truncated; deferred — branch is unmerged WIP and the author is iterating). |

**Why not auto-fixed by this run:**
- Neither feature branch is in `main` (`src/app/golf/actions/team-switcher.ts` does not exist on `main` — verified). Production was not impacted.
- These are pre-merge build failures on PRs that are still under active human authorship; intervening would conflict with the PR author.
- The `'use server'` defect is a real Next.js 16 rule: all top-level exports from a `'use server'` file must be async functions. The PR author can fix by moving `ACTIVE_TEAM_COOKIE` and `COOKIE_MAX_AGE` to a non-`'use server'` module (e.g. `src/lib/golf/team-switcher-constants.ts`) and re-importing.

### Cron health — GREEN

All `/api/cron/*` invocations in the 24h window returned 200. The only cron-path log line was the philosophy-gate warning above (status 200, telemetry).

---

## 5. Gaps + human handoff

| Capability needed | Status | Workaround / ask |
|---|---|---|
| Supabase MCP (`execute_sql` against `admin_events`, `error_logs`, `pg_stat_statements`) | **BLOCKED** — `mcp__supabase__authenticate` returned `HTTP 403: Host not in allowlist` (server-side allowlist rejected the OAuth init host this session) | Human runner: open `/mcp` and authenticate Supabase manually, then re-run. The previous brief (2026-06-10) shows the depth that's possible with Supabase access. |
| Sentry MCP | Not loaded this session (connecting → never appeared in tools) | Same — human runner can attach Sentry MCP for issue-level grouping with affected_users counts. |
| Failed preview build for `feat/distance-units-meters` | Truncated log; root cause not isolated | PR #265 author owns; defer. |
| Failed preview builds for `feat/coach-team-switcher` | Root cause isolated to `src/app/golf/actions/team-switcher.ts:25` (`export const` of cookie name + max-age in a `'use server'` file) | PR #266 author owns; defer. Fix recipe: extract constants to a non-`'use server'` module. |

---

## 6. Fixes applied this PR

**None.** Per the runner's skip rules, all three 24h production warnings are on the documented exclusion list (philosophy-gate telemetry / stale-server-action 404 / tracer info-log). No `severity:error` runtime events occurred. No production-target deployment failed. The failed previews are on unmerged WIP branches the runner is not authorized to rewrite.

Per the runner protocol ("If you find <2 actionable issues, still open a PR with whatever you have AND the brief — even an empty PR with the brief is signal"), this PR ships the brief as the deliverable. Human review can decide whether to (a) re-run with Supabase access wired up, or (b) close as a clean night.
