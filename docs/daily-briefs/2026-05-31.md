# Daily Review Brief — 2026-05-31

**Author:** helm-review (autonomous) · **Repo:** `njrini99-code/helmv3` · **PR:** [#197](https://github.com/njrini99-code/helmv3/pull/197)
**Sources reviewed:** Sentry (`helm-xs/javascript-nextjs`, prod) · Supabase `error_logs`/`admin_events` + Postgres logs (`Helm-Production`) · Vercel runtime logs + deployments (`helmv3`)

---

## TL;DR

- **No new live production code bugs** surfaced in the Sentry production view. One real, environment-independent bug was found in the **database error tables** and is **fixed in PR #197**.
- **The dominant problem is the deploy gap:** production is still pinned to **`572f8e0f` (#189)**. Four merged PRs (#190, #191, #193, #194) — including the `Notification` crash guard — have **never been promoted**. **A human must promote `main` → production.**
- PR #197 also **silences four non-actionable Sentry telemetry issues** (CoachHelm cron noise) while preserving the admin-table audit trail.
- **A human must approve + squash-merge #197** — the autonomous runner cannot self-approve and `--admin` is not permitted (per established policy).

---

## 1. Production deploy gap (highest impact — action required)

The only Vercel deployment with `target: production` is **`572f8e0f` = #189**. On `main`:

| PR | Commit | Promoted? |
|----|--------|-----------|
| #189 | `572f8e0f` | ✅ **live in prod** |
| #190 | `aa879ca4` | ❌ merged, not promoted |
| #191 | `efbfe8dc` | ❌ merged, not promoted |
| #193 | `d8cb9d81` | ❌ merged, not promoted |
| #194 | `c8699958` | ❌ merged, not promoted |

Every Sentry prod event carries `release: 572f8e0f`. Vercel does **not** auto-promote on merge here. **Recommended action: promote `main` to production.** This single step resolves issue 2N (below) and ships the merged coach-chat fixes.

---

## 2. Sentry — production issues (6 unresolved)

| # | Issue | Events | Verdict |
|---|-------|--------|---------|
| 2N | `ReferenceError: Can't find variable: Notification` (`/golf/dashboard`, iOS Safari) | 12 | **Already fixed on main** (#193 guard at `use-service-worker.ts:148`). Events are from `#189` (pre-guard) → **stale-deploy lag.** Resolved by promotion. No code change. |
| 2K | `causality unknown metric …` (`/api/cron/v3/causality-attribute`) | 150 | **Deferred by design.** ~45 insight metric IDs not yet in the v3 causality registry; this is the coverage-gap signal driving the deliberate `metric-sources.ts` mapping. Left reporting on purpose. |
| 13 | `[pattern-miner.thresholds] 0 patterns produced …` (warn) | ~1,343 | **Silenced in #197** → admin-table only. |
| 1B | `[pattern-miner.thresholds] 0 patterns produced …` (info) | — | **Silenced in #197.** |
| 2J | `triggerPlayerInsightsAfterRound skipped N legacy records` | 18 | **Silenced in #197.** |
| 20 | `philosophy gate filtered N tier-1 insight(s)` | 3 | **Silenced in #197.** |

Issues 13/1B/2J/20 are deliberate operational telemetry (`level: warning, handled: true, 0 users`). They were routed through `logServerError`/`logServerEvent`, which still opens recurring Sentry issues. PR #197 adds an opt-in `skipSentry` flag that keeps the `error_logs`/`admin_events` audit trail but stops the Sentry noise. **They stop recurring once #197 is deployed** (the cron currently runs on the stale `#189` build).

---

## 3. Database error tables (`error_logs` / `admin_events`)

This DB receives writes from **both production and local dev** (dev hits the prod Supabase). Triaged by `url`:

### Real bug — FIXED in #197
- **`column golf_rounds.course_par does not exist`** — `getPlayerFingerprint` (7×, last 5/30 18:39). The select referenced a non-existent column, failing the whole rounds fetch and dropping every round from the Player Game Fingerprint. `golf_rounds.score_to_par` already encodes (total_score − par); switched all score-vs-par math to it. **Environment-independent** (would fail in prod too). Tests 36/36.

### Working as intended (not a bug)
- **`Password is known to be weak and easy to guess`** (`auth.signupAction`, 12×, last 5/29) — the prod Supabase HIBP leaked-password gate rejecting weak passwords. Expected GoTrue behavior. *(Optional follow-up: it is logged at `severity: error`; could be downgraded to `warning` to de-noise the admin error feed.)*

### Dev / redesign noise — NOT production (out of scope)
All **localhost** URLs, from in-flight `redesign/foundation` work behind `NEXT_PUBLIC_REDESIGN` (OFF in prod), exercised by local screenshot automation:
- `cn is not defined` → `src/components/fairway/pages/…PrimaryInstrument` (redesign branch — missing import)
- `Functions cannot be passed directly to Client Components` (`{render: function Flag}`) on `/golf/dashboard/qualifiers`
- `Rendered more hooks than during the previous render` on `/golf/dashboard/whats-new`
- Hydration mismatch in `TeamSettingsClient` (invite-link origin) on `/golf/dashboard/team`

**These belong to the redesign work, not this PR.** Recommend fixing `cn is not defined` in the fairway branch before that flag ships.

### Watch-item (rare prod)
- **React #418** (hydration text mismatch) on `/golf/dashboard/rounds` and `/calendar` — 3 single occurrences, last 5/28–29, minified/unattributed, not recurring. Likely SSR date/locale formatting. Monitor; revisit if frequency rises.

---

## 4. Postgres & Vercel runtime logs

- **Postgres logs (Helm-Production):** clean — only `LOG`-severity connection/checkpoint entries, no `ERROR`/`FATAL`.
- **Vercel runtime logs (production, error/fatal, 24h):** empty — application errors flow to Sentry, not the runtime log stream.

---

## 5. PR #197 — what shipped

| File | Change |
|------|--------|
| `src/app/golf/actions/player-fingerprint.ts` | Drop phantom `course_par`; use `score_to_par` for all score-vs-par math. |
| `src/lib/server-error-logger.ts` | Add opt-in `skipSentry` (admin-tables-only) flag. Default unchanged. |
| `src/lib/coachhelm/v2/mining/pattern-miner.ts` | `skipSentry: true` on threshold-starvation signal. |
| `src/app/golf/actions/insights.ts` | `skipSentry: true` on philosophy-gate + 2 skip-insufficient signals. |

**Verification:** `tsc --noEmit` clean · `eslint` clean · `player-fingerprint` 36/36. CI: `Supabase lint + RLS tests` ✅, `Review Gate / all` ✅, `build` + `CodeRabbit` running.

---

## 6. Action items for a human

1. **Approve + squash-merge PR #197** (autonomous self-approve / `--admin` not permitted by policy).
2. **Promote `main` → production on Vercel** — resolves 2N and ships #190/#191/#193/#194. This is the single highest-impact action.
3. *(Backlog)* Continue the v3 causality metric-registry mapping to retire issue 2K.
4. *(Redesign branch)* Fix `cn is not defined` in `src/components/fairway/pages/…` before `NEXT_PUBLIC_REDESIGN` ships.
