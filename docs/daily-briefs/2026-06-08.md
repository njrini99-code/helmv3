# Daily Review Brief — 2026-06-08

**Author:** nightly-health-check (autonomous) · **Repo:** `njrini99-code/helmv3`
**Sources reviewed:** Vercel runtime logs + deployments (`helmv3`, prod, last 24h)
**Sources blocked:** Supabase MCP (admin_events / error_logs / pg_stat_statements) · Sentry MCP

---

## TL;DR

- **0** error/fatal runtime logs in prod over the last 24h (and none over the last 7d when re-checked).
- **0** failed deployments in the last 24h — every prod-targeted build is `READY` (latest prod: `6b23675e` PR #241, 2026-06-08).
- **2** non-actionable cron warnings (both returned HTTP 200) — these are deliberate operational telemetry, not bugs (see §3).
- **3x** 401s on `/api/cron/v3/standing-backfill` (HEAD + 2x GET inside a 2-second window) — the route is NOT scheduled in `vercel.json` (one-shot operator tool); auth correctly rejected the unsigned caller. External probe / unsigned curl, not a bug.
- **Supabase MCP and Sentry MCP could not be reached from this remote session** (OAuth host-allowlist). DB-side `admin_events` / `error_logs` / `pg_stat_statements` totals are unknown — flagging so a human can authenticate `/mcp` and re-run if desired.
- **No code fixes applied.** Per routine rules: <2 actionable issues → still open a PR with the brief as signal. PR is **draft, doc-only, no source changes**.

---

## 1. 24h totals (Vercel, prod)

| Metric | Count |
|---|---|
| Total error/fatal runtime logs | **0** |
| Distinct error patterns | **0** |
| Affected users | **0** (no error envelopes) |
| 5xx responses | **0** (also 0 over the past 7d) |
| Failed deployments | **0** (20 deploys in last 24h, all `READY`) |
| Cron warning events | **2** (both 200) |
| Cron 4xx (one-shot route, external probe) | **3** (401, expected) |

---

## 2. Top patterns with diagnosis

No error-level patterns. The only non-200 cron traffic and the only warning-level cron logs in the window:

| # | Pattern | Occurrences | Status | Route | Diagnosis |
|---|---|---|---|---|---|
| 1 | `[cron.v3.causality] unknown metric …` | 1 (warning) | 200 | `/api/cron/v3/causality-attribute` | Tunable registry-gap telemetry. `metric-sources.ts` doesn't yet alias certain insight metrics; the cron `console.warn`s per insight and emits an end-of-run info-severity roll-up to `admin_events`. Designed-noise per the in-code comment ("coverage gap … logged structured for observability"). **Skip — not a bug.** Route at `src/app/api/cron/v3/causality-attribute/route.ts:132`. |
| 2 | `[insights.triggerPlayerInsightsAfterRound] …` | 1 (warning) | 200 | `/api/cron/coachhelm-roster-sweep` | Per-player engine reject inside the nightly roster sweep, surfaced via `logServerError(…, 'warning')` and counted into the `failed` summary. Cron returned 200 — the warning is observability, not a fault. **Skip — single-occurrence transient.** Handler at `src/app/api/cron/coachhelm-roster-sweep/route.ts:100`. |
| 3 | 401 on `/api/cron/v3/standing-backfill` | 3 (HEAD+GET+GET at 03:31:45–47) | 401 | `/api/cron/v3/standing-backfill` | Route is NOT scheduled in `vercel.json` (one-shot operator tool, header file says `curl -X POST -H "Authorization: Bearer $CRON_SECRET"`). The triplet HEAD→GET→GET in 2s matches an external probe / un-creded curl. `requireCronAuth` correctly returned 401. **Skip — 4xx with valid auth check is not an error.** |

No other patterns appear in the window.

---

## 3. Performance (pg_stat_statements)

**Not available this run** — Supabase MCP auth failed in this remote session (`Host not in allowlist` from the OAuth proxy). No DB queries executed. The user can authorize via `/mcp` to lift this for the next run.

---

## 4. Deployment health

- 20 deployments in last 24h, **0 failed**, **0 building/error**.
- Production-targeted deploys (last 24h):
  - `dpl_G1PrWNEdw7CZwuYBPJqKVaJbEhEg` — `6b23675e` — Merge PR #241 (stats consistency guards) — READY (current prod)
  - `dpl_2bWd1R6sVRcy8HurJQaqLCvWVvaM` — `6b23675e` — same commit, re-deploy — READY
  - `dpl_E1KVwcXky6B4mK2d14TsGKrkisok` — `c9a9ebb2` — Merge PR #240 (women's SG baseline) — READY
  - `dpl_C8DEREdyxmEqNqtPVEvHBW182Ku3` — `2525ee44` — Merge PR #239 (SG recalibration + CoachHelm regen) — READY
  - `dpl_87dbN58b7u5QLt8Y7UMhXCS16H2q` — `7f249362` — same PR #239 branch — READY
- No first-error lines to extract (zero failures).

---

## 5. Fixes applied

**None** — no actionable error patterns surfaced from Vercel, and Supabase / Sentry were inaccessible from this session.

---

## 6. Blockers / follow-ups for a human

1. **Authorize the Supabase MCP** in this remote session (`/mcp` flow). The Supabase OAuth start fails with `HTTP 403: Host not in allowlist` for the remote callback URL, so `execute_sql` against `admin_events` / `error_logs` / `pg_stat_statements` is unavailable to the autonomous run. Without it, DB-side errors and slow queries are invisible to this brief.
2. **Authorize the Sentry MCP** (same flow) — same OAuth blocker, so this run could not corroborate Vercel-level quietness against Sentry's prod issue list. (Last-known Sentry coverage as of 2026-05-31: Issues 13 / 1B / 2J / 20 silenced in #197; Issue 2K kept on purpose as a v3 causality coverage signal.)
3. **No PR auto-merge.** Leave this draft for human review.
