# P0 Runbook — 2026-05-17

These are operational steps a maintainer must perform; they are not
automatable from the migration PR. Check off each as you complete it.

## 1. Rotate leaked dev DB password

The credential `EHl4yASa9zM1sb1k` was committed in plaintext to
`docs/setup/RUN_ON_YOUR_MACHINE.md` (first commit `76dbf1f3`). The
password was scrubbed from the file in a follow-up commit, but it
remains visible in git history. Anyone with read access to the
repository can recover it.

- [ ] Sign in to Supabase dashboard.
- [ ] Open project ref `dgvlnelygibgrrjehbyc` (if it still exists — may
      be a retired dev/test project; if so, skip to step 1d).
- [ ] Project Settings → Database → "Reset database password".
      Generate a new strong password (`openssl rand -base64 32`).
      Save it in your team password manager under
      "GolfHelm — legacy dev DB (dgvlnelygibgrrjehbyc)".
- [ ] If `dgvlnelygibgrrjehbyc` is no longer in use, pause or delete
      the project entirely from the dashboard. A retired project with
      a leaked credential is still a credential.
- [ ] Audit Supabase access logs for the past 90 days on this project
      for sign of the leaked credential being used externally. Filter
      for non-Vercel / non-team IPs.
- [ ] If concerned about exposure window, consider history-scrubbing
      the commit `76dbf1f3` via `git filter-repo` — coordinate with
      everyone who has local clones, as this rewrites history.

## 2. Verify production cron + internal secrets

The file `.env.production.local:2-3` shows empty values:

```
CRON_SECRET=""
COACHHELM_INTERNAL_SECRET=""
```

`.env.production.local` is a local-machine file; the authoritative
production values live in Vercel. Verify they are NOT empty in production.

- [ ] Run `vercel env ls production` and confirm `CRON_SECRET` is set
      (look for a non-empty `Value` column).
- [ ] Run `vercel env ls production` and confirm
      `COACHHELM_INTERNAL_SECRET` is set.
- [ ] If either is empty:
      ```bash
      openssl rand -hex 32 | pbcopy
      vercel env add CRON_SECRET production
      # paste; confirm
      vercel env add COACHHELM_INTERNAL_SECRET production
      # paste; confirm
      ```
      Then redeploy production: `vercel --prod`.
- [ ] Also confirm `CRON_SECRET` exists on Preview and Development
      scopes (the cron auth helper at `src/lib/cron/auth.ts` fails
      closed on missing, so empty in non-prod is acceptable — but
      explicit values are cleaner).

## 3. Audit GitHub branch protection on main

Phase 3 of the audit found that 17 vitest files / 50 specs fail locally
but CI claims green. Either CI runs a different test path or branch
protection is misconfigured.

- [ ] Open GitHub → repo → Settings → Branches → Branch protection
      rules for `main`.
- [ ] Confirm "Require status checks to pass before merging" is **ON**.
- [ ] Confirm the vitest job from `.github/workflows/ci.yml:62` is in
      the "Required status checks" list under its actual job name.
- [ ] Confirm "Require branches to be up to date before merging" is ON.
- [ ] Confirm "Require pull request reviews before merging" is ON, 1
      approval minimum.
- [ ] If vitest is NOT in the required list, add it.
- [ ] Confirm `lint` and `typecheck` are also required.
- [ ] Confirm "Do not allow bypassing the above settings" is ON (no
      admin bypass).
- [ ] After Plan 02 ships, also require the `e2e` and
      `Supabase lint + RLS tests` jobs.

## 4. Confirm migration deployment plan

The fix migration `20260517000000_fix_critical_rls_policies.sql` will
be applied to production via the project's normal Supabase deployment
process. Before merging the P0 PR:

- [ ] Confirm with team how migrations reach production (manual
      `supabase db push`, CI workflow, or third-party tool).
- [ ] If manual: schedule the push during a low-traffic window AFTER
      applying the migration locally + running both pgTAP suites
      successfully (`supabase test db --file supabase/tests/rls/*.sql`).
- [ ] Have a rollback SQL file ready (the inverse migration: re-add
      the original policies by copy-paste from
      `supabase/migrations/20260427210000_canonical_rls_snapshot.sql`
      lines 1746-1753 and 2805-2809). Do NOT include the inverse as
      a migration on disk — keep it as a private rollback script.
- [ ] Monitor `golf_coach_insights` and `golf_team_coach_staff` write
      rates for 24h after deploy. Sudden drop in
      `golf_team_coach_staff` INSERT rate may indicate the fix is
      rejecting legitimate writes that the app's server actions
      didn't anticipate (specifically: any team-creation path that
      goes through user-context instead of service_role).

## 5. Verify pgTAP suites locally before merge

Docker daemon was off when the P0 PR was authored, so the pgTAP
suites were not run. Verify them before merging:

- [ ] Start Docker.
- [ ] `supabase start` (~2 min first run).
- [ ] `supabase test db --file supabase/tests/rls/golf_team_coach_staff.sql`
      → expect 3 passing.
- [ ] `supabase test db --file supabase/tests/rls/coach_insights_player_update.sql`
      → expect 4 passing.
- [ ] If `golf_team_coach_staff` test fails with "relation does not
      exist", the table is missing from committed migrations — see
      step 6.

## 6. Missing `golf_team_coach_staff` CREATE TABLE (audit follow-up)

The audit discovered that `golf_team_coach_staff` has no `CREATE TABLE`
statement in `supabase/migrations/`. References to it span many
migrations, but the table was apparently created via the Supabase
dashboard SQL editor at some point and never captured in a migration.

- [ ] Dump the current production schema for `golf_team_coach_staff`:
      ```sql
      SELECT column_name, data_type, is_nullable, column_default
      FROM information_schema.columns
      WHERE table_name = 'golf_team_coach_staff'
      ORDER BY ordinal_position;
      ```
- [ ] Author a new migration `20260517005000_create_golf_team_coach_staff.sql`
      with `CREATE TABLE IF NOT EXISTS` matching the production shape.
      This makes local `supabase db reset` reproducible and lets the
      pgTAP suites run against a fresh DB.
- [ ] Order the migration BEFORE the policy fix
      (`20260517000000_fix_critical_rls_policies.sql`) — the policy
      references the table, so the table must exist first. Rename if
      necessary to enforce timestamp ordering.

## 7. Re-run lint on plan author's changes

The audit found 137 uncommitted files on `main` at the time of
authoring (an a11y / design-system sweep in progress). Confirm those
WIP changes don't conflict with the P0 migration:

- [ ] `git stash` your WIP, or commit it.
- [ ] Pull `audit/2026-05-17-p0-rls-criticals` to local.
- [ ] `npm run lint` and `npm run typecheck` against the merged state.
- [ ] Re-apply WIP and resolve any conflicts.
