# Nightly Health Check — 2026-05-24

> Automated production triage of helmv3 (Vercel `nick-rinis-projects/helmv3` + Supabase `qmnssrrolpinvwjjnufo`).
> Window: 24h prior to 2026-05-24 04:10 UTC.

## 24h totals

| Metric | Value |
|---|---|
| Total error-level runtime logs | **0** |
| Distinct error patterns | **0** |
| Affected users | **0** |
| 5xx HTTP responses | **0** (queried `500`, `502`) |
| Failed production deploys | **0** (20/20 recent deploys `READY`) |
| Cron failures (`/api/cron/*`) | **0** (all 200) |
| Warning-level runtime logs | **1** (skipped — pattern-miner.thresholds tunable info) |

## Top 10 error patterns

_None._ Production is fully quiet on the Vercel side for the full 24h window.

The only warning is at `2026-05-23T03:45:48Z` on `GET /api/cron/coachhelm-roster-sweep`:
> `[pattern-miner.thresholds] ...`

Per nightly-check spec, pattern-miner threshold warnings are tunable info logs (not bugs) and are explicitly out of scope.

## Performance: top 3 slow queries (pg_stat_statements)

**Unavailable.** Supabase MCP (`mcp__Supasbase__execute_sql`) returned `Connection terminated due to connection timeout` on every SQL query attempted during this run, including trivial probes (`SELECT 1`). The Supabase `get_advisors` endpoint returned `Project not found`. This blocked all three Supabase data pulls in the spec:

- `admin_events` where `severity in ('error','critical')` — **not pulled**
- `error_logs` since 24h ago — **not pulled**
- `pg_stat_statements` top-10 slow queries — **not pulled**

Vercel MCP was fully functional throughout the run. The Supabase MCP outage is likely an upstream infrastructure issue (the same MCP server's `authenticate`/`complete_authentication` tools are still listed, and the `Supasbase` server is registered against project `qmnssrrolpinvwjjnufo`); a human should re-run the Supabase side of the check once MCP connectivity is restored.

## Deployment health

All 20 most recent deployments are `state: READY`. Most recent production deploy:

| Field | Value |
|---|---|
| Deployment ID | `dpl_4SfeznuWQfmk3ex2HCrMucGzW89q` |
| Commit | `7076e52` |
| Message | Merge PR #36 — `hotfix(coachhelm): Wave 6 — 22 dep tables + Tier-1 philosophy filter` |
| Created | 2026-05-23T22:00 UTC |
| Status | READY |

The 24h window saw a heavy hotfix cadence (PRs #28–#36 — six "wave" CoachHelm hotfixes from the multi-agent audit, plus three migration-correction PRs) and they all shipped clean.

## Fixes applied

_None._ Vercel side surfaced zero actionable runtime errors over 24h, and the Supabase side could not be queried due to the MCP outage above. Per nightly-check spec ("If you find <2 actionable issues, still open a PR with whatever you have AND the brief — even an empty PR with the brief is signal"), this PR contains the brief alone so the routine remains visible in the dashboard.

## Recommended follow-ups for a human

1. **Re-run the Supabase side of this check** once the `Supasbase` MCP server is healthy again (re-issue queries against `admin_events`, `error_logs`, and `pg_stat_statements`). Today's run could not verify the DB side at all.
2. **Investigate the MCP outage itself** — `mcp__Supasbase__execute_sql` returns connection timeouts and `mcp__Supasbase__get_advisors` returns "Project not found"; likely a token/scope or project-ref drift in the MCP server's config.
3. **Pattern-miner thresholds noise** — single warning today, but if this becomes a daily fixture and a real coach's roster ever needs the floor lowered, consider exposing it as a per-team override rather than a global threshold.
