# Nightly Health Check — 2026-05-26

## 24h totals
- **Production runtime errors:** 0
- **Distinct error patterns:** 0
- **Affected users:** 0 (no production traffic recorded — project is currently `live: false` on Vercel)
- **Failed production deployments:** 20 of 20 most-recent deploys in last ~24h are in `ERROR` state
- **Supabase DB inspection:** **blocked** — the Supasbase MCP returns `Connection terminated due to connection timeout` on every SQL query and `Project not found` on `get_advisors`, while `get_project_url` returns `https://dgvlnelygibgrrjehbyc.supabase.co` (note: differs from the project_ref `qmnssrrolpinvwjjnufo` referenced in the agent instructions — MCP is pointed at a different / non-existent project ref this session)

## Top patterns
No production runtime error patterns were observed in the last 24 hours.

The only signal is at the deployment layer (next section).

## Performance — slowest queries
**Not available.** `pg_stat_statements` inspection blocked by the MCP-side query timeout described above. No action possible from code this session.

## Deployment health — DOMINANT SIGNAL

Every Vercel deployment created since ~`2026-05-26 01:09 UTC` (epoch 1779721771412) is failing in **<1 second** with **zero build log events**. The last green deploy was wave15 at `2026-05-25 15:08 UTC`.

```
dpl_CJ52b3EvRGNdjXwEyrgJ8AE5Ck29  wave15   6dec916  2026-05-25 15:08Z  state=READY
dpl_A3pCHoE6Puc3pkyPyevP7ih2Ama6  wave16   1daa6da  2026-05-26 01:09Z  state=ERROR (727ms)
...
dpl_4AiS1ufZVMvrEF8zXQwNoLafkHGd  wave25   605fbcf  2026-05-26 04:06Z  state=ERROR (1018ms)
```

**Signature of failure:** `buildingAt → ready` delta is ~700–1000 ms; `get_deployment_build_logs` returns `events: []`. No build machine is ever allocated. This is **not** a TypeScript / Next.js / code compile error.

| Deployment | Branch / PR | SHA | First error line | First error line |
|---|---|---|---|---|
| dpl_4AiS1ufZVMvrEF8zXQwNoLafkHGd | main (merge wave25, PR #61) | 605fbcf | _(no build logs emitted)_ | — |
| dpl_Anve3eKsq7xCGeWwgVNfiB5Lxr6d | wave25 | da51c4c | _(no build logs emitted)_ | — |
| dpl_EFZbreF7Yitk9wq7VVbe1a1XiLaK | main (merge wave24, PR #60) | 4bae2e2 | _(no build logs emitted)_ | — |
| dpl_2XopRDz6TuTBnEUwNkdQW2WykCUx | main (merge wave23, PR #59) | aaff33a | _(no build logs emitted)_ | — |
| dpl_G5fBcfELz8zrXUUv5oNNah6hACX4 | main (merge wave22, PR #58) | c00bc4a | _(no build logs emitted)_ | — |
| dpl_EevzATaSuGPV1dwanPRPXrCcFRw2 | main (merge wave21, PR #57) | 18fc979 | _(no build logs emitted)_ | — |
| dpl_7SV8EfwriuKMD2SjomuMJ8gjEdg5 | main (merge wave20, PR #56) | b5a256b | _(no build logs emitted)_ | — |
| dpl_HNgjgCNzWKUFtwK6gTmNtUYJUZfG | main (merge wave19, PR #55) | a24e6cf | _(no build logs emitted)_ | — |
| dpl_6nnm1feMD7RsEUkBWfpErJTGmWYn | main (merge wave18, PR #54) | 64bc38a | _(no build logs emitted)_ | — |
| dpl_7uERJySkkGXf9XLrKJ1NpaiM223R | main (merge wave17, PR #53) | 3a0f181 | _(no build logs emitted)_ | — |
| dpl_61MF8GNqjcLN7A8whFVPEC6SRC3H | main (merge wave16, PR #52) | 08f3568 | _(no build logs emitted)_ | — |

**Diagnosis (high confidence):** account-level Vercel constraint — spend cap reached, build concurrency / minute quota exhausted, or org-wide pause. The project also reports `"live": false`. This is **not fixable from code**; the account owner must resolve it on the Vercel dashboard. Once unblocked, the in-flight queue will re-attempt the latest commit (`8735762`, PR #84) and subsequent waves (W26–W42 / PRs #62–#84) which have not yet been seen by Vercel at all.

**Local sanity check:** `npx tsc --noEmit` on current `origin/main` (`8735762`) — **0 errors**. The code itself compiles cleanly; the blockage is purely at the Vercel build-provisioning layer.

## Cron failures
None observed. (Vercel cron failure events are emitted via the runtime-logs surface and there were zero runtime events in the 24h window.)

## Fixes applied
**None this run.** Both potentially-actionable categories are out of reach from the code layer:

1. **Vercel deploy failures (20/20):** infrastructure / account quota — requires human action on the Vercel dashboard. Cannot be remediated by a PR.
2. **Supabase error_logs / admin_events / pg_stat_statements:** the MCP transport is broken in this session (SQL queries time out, `get_advisors` 404s). Cannot be inspected.

Per nightly-health-check spec ("If you find <2 actionable issues, still open a PR with whatever you have AND the brief — even an empty PR with the brief is signal."), this PR carries only the brief.

## Handoff to human

1. **URGENT — unblock Vercel deploys:** check the Vercel dashboard (`nick-rinis-projects/helmv3`) for a spend-cap notice, build-minutes quota, or paused state. Confirm billing / payment method is current. After unblock, re-trigger latest commit on `main` (`8735762`).
2. **Verify Supasbase MCP project_ref:** the MCP currently resolves to `dgvlnelygibgrrjehbyc.supabase.co`, but project documentation references `qmnssrrolpinvwjjnufo`. If the documented project is correct, the MCP config needs to be repointed before the next nightly run can inspect `admin_events` / `error_logs` / `pg_stat_statements`.
3. **After items 1 + 2:** re-run this nightly check so the database-side error inventory can actually be produced.
