# CoachHelm + Stats Re-Audit — to-95 Regrade

Date: 2026-06-09 (evening, post-deploy, post-regen)
Method: same rubric as COACHHELM_TO_95_AUDIT_2026-06-08.md. 51 agents (7 dimension auditors, adversarial verifier per finding, 1 scorer). All gates run on deployed main. Full engine regen executed against prod immediately before the audit. Manual H5 dry run executed (scripts/h5-attribution-dryrun.ts).

## Grade: 88/100

## Verdict

The system is genuinely stronger than the 88-90 state the 06-08 audit graded, and on that audit's own four-item frame the progress is real: the causality eligibility boundary is shipped, deployed, and live-verified against prod; the stale-row sweep ran tonight with 23/23 precision and zero coach-touched casualties; the per-player feeds paginate the full visible set; linked-prod lint is clean; and every gate is green on code that is actually serving helmsportslabs.com — the deploy gap that poisoned the 06-06 audit is gone. Every number I recomputed on the canonical card surfaces was exact to the stored decimal, and the 42-stroke v2 phantoms are provably unreachable through every delivery read. If the only lens were those four gaps, this would be a 92-93. It is not, because the wider lens found what the 06-08 audit never looked at: the chrome around the canonical path — the unread badge, What's New, the morning digest, coach chat, the command palette — reads golf_coach_insights raw, and today shows coaches inflated counts, retracted claims, retired-engine content, and a permanently-empty search; the Alert Center auto-surfaces seven 'high' alerts the write path's own confidence rule forbids; and the women's team sees two contradictory benchmarks on a single card. Meanwhile the learning loop sits correctly wired on a clean zero baseline, but I cannot credit live proof before the 21-day gate clears ~2026-06-16 — and as deployed, that first eligible run would begin permanently stalling on never-attributable candidates while reading penalties from a column this repo already declared drifted; two further time bombs (the resurrection/cron flap from ~06-25, and error-conflated retraction on the next transient DB failure) are wound and ticking. A coach who lives in the Hub, the feed, and the Alert Center list can trust nearly every number they see; a coach who trusts the badge, the digest, or the chat cannot — and a 95 requires both, plus the live causality proof. Current assessed grade: 88. The path to a real 95 is now concrete and short: one shared query-builder applied everywhere, a week of causality fixes landed before the gate opens, two lifecycle timers re-anchored, the priority/benchmark write-side honesty fixes, and then the Phase H proof itself.

## Gate results (all green)

- typecheck clean · vitest 5,896/0 · RLS 1,969/0 · prod build clean · check:stats 19 players 0 divergent · linked-prod db lint 0 errors
- Engine regen: 18/20 players (2 legit cold-start), 0 generator failures; sweep first live run: 23 archived / 0 coach-touched / 0 false resurrections
- Manual H5 dry run (age gate removed, read-only): 223 candidates → 41 attributed, 120 intentional-null, 0 unknown-metric, 0 malformed; simulated weights 1.04–1.77, none binary-pinned, all in clamp — all six H5 checks PASS

## Per-area breakdown

### Engine validity (card numbers, prose, counterfactuals)

~90. The math itself is excellent: 7 of 8 sampled insights recompute exactly against raw tables to the stored decimal, CF clamps hold on all 223 visible rows (0 over 2.5, honest clamped:true flags), strokes_impact is consistent with the CF on all 223, sample gates honored everywhere sampled, honest confidence mode verified live, and women's anchors are correct end-to-end where COHORT_ANCHORS has entries. What it loses: course_management composes priority='high' below the DC-CONF-2 confidence gate (7 live rows, worst conf 0.267/n=8, auto-surfacing in the Alert Center — verified 7/7 by SQL); the women's-team standing bar renders the men's PGA anchor against the same card's women's prose; par_scoring/course_mgmt marry lifetime-cache averages to 90-day window counts and stamp window_days:90 on lifetime metrics; data staleness (49-63 days) is invisible on 'high' alerts. Values are right; priorities, benchmarks, and denominator labels are not always.

### Canonical delivery read path (Hub, feed, Alert Center list, player cards)

~93. The shared visibility contract is applied in-DB at all 7 delivery call sites (verified at insight-delivery.ts:225/248/341/490/518/640/807); live SQL over the predicate returns 223 rows with zero |impact|>8 phantoms; the belt-and-suspenders OR filter is precise (all 304 v3 rows carry both stamps, zero single-stamp rows); pagination via fetchAllRowsResult is correct with stable id ordering and an 800-row warned ceiling; par-scoring collapse and subject dedupe fire on live data exactly as designed; both prod-live redesign surfaces read exclusively through the filtered path. The one real defect: getTopInsightForPlayer still pre-trims to newest-20 before ranking (insight-delivery.ts:242-251, verified) with one player already exactly at the cap, and the top-pick skips collapse/dedupe — the precise inconsistency the to-95 fix was meant to kill.

### Ancillary coach surfaces (badge, What's New, morning digest, chat, command palette, legacy flag-off)

~70 — the main score drag. None of these import insight-visibility.ts (verified: zero imports in digest cron, whats-new.ts, chat/tools.ts). Live today: the unread badge on every redesigned page counts archived/tentative/v2 rows and drops 'urgent' from the critical bucket (alerts.ts:164-189, verified in file; 326 raw badge-eligible rows vs 223 visible truth); What's New renders 58 of 119 in-window rows that are not product-visible, with today's tentative rows topping the feed; the scheduled morning digest's watch/celebration slots rank stale v2 rows #1-5 by its own rank function; coach chat get_player_insights (tools.ts:80-102, verified — no lifecycle/status/engine filter, lifecycle_state not even selected) can quote putt-bias claims the engine retracted 30 minutes earlier for 14 players; the command palette selects a nonexistent severity column (verified 0 in information_schema) so insight search silently 400s to empty. The status/lifecycle/dismissed three-axis incoherence (46 archived+active v3, 27 dismissed-status/false-flag v2, 29 detected/resolved v2) guarantees every hand-rolled reader gets a wrong row set.

### Stats / data layer

~93. SG additivity is airtight (max component-sum error 0.01 across 173 rounds), cache==rounds propagation exact, putt-band make-% and attempts match independent recounts band-for-band, penalty cache canonical 20/20 with correct 18-hole-equivalent denominators, no feet/yards proximity blend (5,620/5,621), shot coverage complete on all 167 18-hole rounds, and check:stats reports 19 players 0 divergent. Deductions: golf_rounds.total_penalties remains un-backfilled after being declared drifted (8 rounds verified wrong) and the causality engine still reads it (metric-sources.ts:197, verified in file) — quarantined from coach-facing stats but live in the learning loop; plus minor hygiene (3 contradictory putt-distance rows, a falsified '100% NULL' comment, cloned demo players skewing cohort baselines).

### Lifecycle (sweep, resurrection, cron interplay)

~84. The new sweep demonstrably works in production with perfect precision — 23/23 archived rows verified coach-untouched and in-scope (SQL: 23 swept tonight, 0 coach-touched), 15 superseded + 8 dequalified, all design safety properties (soft-archive only, keepSignature, coach-touch guards, swallowed failures) verified in code; resurrection exists in both upsert branches and preserves dismissals; DI-1 is closed with zero frozen rows. Deductions: composites have data-derived signatures but no sweep scope (2 stale-and-stuck rows verified live); matured rows have no retraction path at all (3 dequalified-but-stuck warmup rows); the resurrection-vs-cron interaction is a guaranteed nightly archive/detected flap starting ~2026-06-25 (verified: cron Rules 2/3 anchor on created_at, resurrection never resets movement_count); every generator conflates query error with dequalification so a transient DB hiccup archives valid live insights (verified putt-bias.ts:174/190 null-on-error feeding generator-base.ts:305-315 retraction); and sweep archives carry no provenance.

### Causality / learning loop

~75. The wiring is right: the cron imports the identical shared eligibility boundary as delivery (verified), both learning tables are at a clean zero baseline (verified 0/0 by SQL), the 21-day clock was not reset by the regen, nextWeight is the correct clamped magnitude-aware EMA, all referenced DB columns exist, and the cron is scheduled and auth-gated. But the live proof is time-withheld until ~2026-06-16 and cannot be credited — and as deployed the loop has confirmed defects that would taint that proof: the oldest-150/50-slot candidate window permanently stalls on never-attributable rows (54% intentional-null metrics, 58/103 measurable rows with unfillable pre-windows — within days of first eligibility nothing new ever gets attributed); penalty_rate_per_round reads the declared-drifted golf_rounds.total_penalties; both attribution windows include the surfaced day so the triggering round contaminates pre AND post; candidate-fetch errors and weight-upsert errors are silently swallowed; and null-lift attributions write sample_n=0 weight rows that look like learned state. Wired to the right boundary, not provably correct.

### Security

~88. Strong posture verified: all 10 maintenance/SECURITY DEFINER functions are service-role-only (the 06-06 anon-EXECUTE regression is gone), RLS enabled on all 84 golf_% tables, zero anon-inclusive policies, learning tables write-locked to service_role (the zero baseline cannot be coach-tampered), and golf_rounds row-scoping intact across all three UPDATE policies. The one real hole: the 06-09 table-wide golf_rounds UPDATE grant plus a WITH CHECK-less player self-update policy lets an authenticated player reassign their own round's team_id to any team, injecting off-roster rounds into a victim coach's feed and aggregates (P1; needs a WITH CHECK pinning team_id or a column-level revoke). The arwdDxtm default ACL for anon/authenticated is documented Supabase posture but leaves RLS as the only wall for every future table.

### Release / verification gates

~95. Everything runnable is green on deployed main: typecheck clean, 5,896 unit + 1,969 RLS tests passing, production build clean, check:stats 19/0, supabase db lint --linked 0 errors, full engine regen 18/20 (verified by SQL: 18 players hold visible v3 rows) with 0 generator failures, and — critically — prod now serves main, eliminating the 06-06 audit's #1 root cause (undeployed branch). Remaining: the fresh-replay half of the lint gate still requires Docker, and migration recording remains under apply-time versions (known drift pattern, documented, do-not-db-push).

## Path to 95 (ranked by cost, with deadlines)

- 1. (Highest cost) Route every golf_coach_insights reader through a shared query-builder exported from insight-visibility.ts. One change closes eight confirmed findings at once: the badge (getAlertCounts — also fix the urgent-bucket drop), What's New, the morning digest, chat tools (get_player_insights / get_team_patterns / recap), the command palette (after fixing the nonexistent severity column), the loadEvidenceBackedInsights hand-rolled copy, and the flag-off legacy surfaces. Until then, every new surface re-creates the bug class because status/lifecycle/dismissed disagree by design.
- 2. (Hard deadline 2026-06-16) Causality pre-eligibility fixes, while the learning tables are still empty and fixes are free: anti-join golf_insight_outcome_attribution + write marker rows (or pre-filter) for intentional-null and permanently-no-data candidates so the oldest-150/50-slot window cannot permanently stall; repoint penalty_rate_per_round from drifted golf_rounds.total_penalties to the canonical cache; exclude the surfaced date from one attribution window; check the candidate-fetch and weight-upsert errors instead of swallowing them; skip the no-op weight upsert on null lift.
- 3. (Hard deadline ~2026-06-25) Re-anchor lifecycle cron Rules 2/3 on metadata.last_refreshed_at / redetected_at (or have the cron skip recently-refreshed rows) before the first detected/movement-0 cohort crosses the 30-day gate and the nightly archive↔resurrect flap begins.
- 4. Distinguish query error from genuine no-data in all 9 generator aggregates so a transient PostgREST failure during the nightly sweep cannot soft-archive a player's valid live insights; while there, gate resurrection through the tentative confidence floor and stamp retraction provenance.
- 5. Write-side priority honesty: make leveragePriorityFloor (or a write-time gate) DOWNGRADE course_management 'high' below MIN_CONF_FOR_HIGH=0.5 — 7 live below-gate 'high' rows auto-surface in the Alert Center today.
- 6. Extend the DC-GENDER-1 fix to the standing pipeline so women's standing bars stop rendering men's PGA anchors that contradict the same card's prose; add women's course_management anchors and de-pool the level_avg cohort.
- 7. Add WITH CHECK to golf_rounds_update pinning team_id (or revoke UPDATE(team_id, player_id) at column level for authenticated/anon) to close the cross-team round-injection hole.
- 8. Mixed-denominator honesty: either window the cache-backed playerValue or label it lifetime (par_scoring, course_mgmt), fix course_mgmt's false window_days:90 stamp, and surface data staleness on cards whose newest round is weeks old.
- 9. Lifecycle completeness: a composite-scope sweep (retract v3:composite:<rule>:% rows the run didn't re-emit), a bound on matured-row staleness (archive after N consecutive non-emitting runs), and a sweep for the team toggle-off exit.
- 10. Convert getTopInsightForPlayer's ranked pass to the full visible set with collapse/dedupe parity (prod sits exactly at the 20-row cap today); backfill or retire golf_rounds.total_penalties; run the Docker fresh-replay lint half; then, after 2026-06-16, execute the Phase H live proof exactly as documented (attribution rows exist, weights move off 1.0, no binary 1.5/0.5 pinning) — that proof cannot be credited before the gate and is the final item between this grade and a real 95.

## Confirmed findings (40 — 12 P1 / 14 P2 / 14 P3, 0 P0; 3 refuted in verification)

### [P1] (validity) course_management ships priority='high' below the confidence gate — 7 active rows, worst at conf 0.27 / n=8

The DC-CONF-2 rule (high requires confidence >= 0.5) is enforced only on the leveragePriorityFloor UPGRADE path (src/lib/coachhelm/v3/engine/generator-base.ts:185-188, MIN_CONF_FOR_HIGH=0.5), but CourseMgmtGenerator composes its own 'high' via anchoredPriority (src/lib/coachhelm/v3/generators/course-mgmt.ts:186-197, invoked at :248 and :290) which has no confidence input, and the floor never downgrades. Result: 7 of 223 active v3 rows are priority='high' with confidence < 0.5 — all course_management. Worst: Grace Saunders big_number_rate (01aa656d, conf 0.267, n=8 rounds, CF 0.509) — the exact thin-sample scenario the DC-CONF-2 comment names ('Grace par_4 @ 0.27, n=8 must not auto-promote'). Larsen Gallimore (5b518de2) is high at conf 0.4 with CF leverage 0.384 strokes/rd — below even the 0.5 'medium' floor — off a margin of only 2.13pp over cohort (8.33 vs 6.20, high threshold 2.0). The Alert Center filters ['urgent','high'], so these auto-surface. The underlying VALUES are correct (I verified Larsen 8.333% and Grace 9.028% exactly against raw golf_holes); this is pure priority inflation.

Evidence: SQL scan: SELECT ... WHERE engine_version='v3' AND lifecycle_state IN (...) AND priority IN ('high','urgent') AND (evidence->>'confidence')::numeric < 0.5 → 7 rows, all insight_type='course_management' (ids 84088b90, 1963eced, 8fa60c20, 8532afa0, 53d42b84, 01aa656d, 5b518de2). Code: course-mgmt.ts:186-197 anchoredPriority has no confidence parameter; generator-base.ts:168-190 leveragePriorityFloor 'Never downgrades.'

### [P1] (validity) Women's-team standing bar renders the men's PGA anchor, contradicting the same card's women's prose (Grace Saunders scrambling: bar says 50%, prose says 38%)

Grace Saunders' scrambling insight (994ee861) correctly uses the women's anchor in prose ('Women's college sand-save average is ~38%') and in the counterfactual (verified: ssr 1.0982 = (38-0)/100 x 3.4 attempts/rd x ~0.85 strokes/save). But evidence.standing is populated by the standing RPC from golf_pga_standards (men's values): standing.pga_value=50, pga_delta=-50. EvidencePanel.tsx:51 passes standing.pga_value straight into StandingBar, which renders it with a Tour reference label (StandingBar/Card.tsx:53,87). So the coach sees two contradictory benchmarks on one card, and the bar shows her 0% against a men's 50% target (gap visually inflated ~1.3x vs the women's 38%). The DC-GENDER-1 fix (cohort-baselines.ts) covered generator prose + CF but not the standing pipeline. Same pattern applies to every standing-backed metric for the women's team (e.g. her putt standing rows would carry men's 90.5/62.2 etc.).

Evidence: Insight 994ee861 evidence JSON: comparison_value=38, comparison_label="women's college sand-save avg" vs evidence.standing {pga_value:50, pga_delta:-50, player_value:0}. Code path: src/components/golf/coachhelm/insights/EvidencePanel.tsx:51 (pga_value={standing.pga_value}) → src/components/golf/coachhelm/v3/StandingBar/Card.tsx:87. golf_teams.gender='womens' for team 343731cb confirmed by query.

### [P1] (validity) par_scoring marries a lifetime-cache average to a 90-day hole count — Grace's card prints 'you average 4.47 on 66 par 4s' but the true average over those 66 holes is 4.455

ParTypeGenerator's playerValue comes from golf_player_stats_cache (lifetime) while sample_n/holes_scored and the birdie/bogey/double rates come from loadCompletedHoles (hard 90-day window, hole-diagnosis.ts DEFAULT_WINDOW_DAYS=90). Grace Saunders (df334643): lifetime = 75 par-4 holes, avg 4.467 (matches cache 4.47); the 90-day window (cutoff 2026-03-11) silently drops her 2026-03-09 round, leaving 66 holes whose true average is 4.455. The card states the lifetime number over the windowed count. Magnitude today is 0.015 strokes (demo spans are short), but the divergence is structural: any player with >90 days of history gets a career average labeled with a 90-day sample. The same mixed-denominator class affects course_management decomposition: Grace's big_number card says 'Across your last 8 rounds ... 63.6% of your double-or-worse holes trace to missed greens' and 'over 7 plays' — the 63.6% (7/11) and worst-holes are computed over the 7 in-window rounds while the headline 9.0% covers all 8 lifetime rounds (13 doubles).

Evidence: SQL recompute: Grace par4 lifetime 75 holes avg 4.467; >=2026-03-11 66 holes avg 4.455 (birdie 10.6/bogey 33.3/dbl 9.1 match evidence.detail). Insight df334643 content: 'you average 4.47 on 66 par 4s'. Code: par-type.ts:86-103 (cache value at :87-96, loadCompletedHoles decomposition at :103), hole-diagnosis.ts:39 DEFAULT_WINDOW_DAYS=90. Grace big_number 01aa656d content cites '7 plays' vs 'last 8 rounds'.

### [P2] (validity) course_management stamps window_days:90 on lifetime-cache metrics — Braeden Gillen's cards include rounds older than the claimed window

The Phase E window-honesty fix gave the cache-backed putt_distance and par_scoring generators a true lifetimeSpanDays (verified honest in prod: Jackson wd=54 = 2026-04-08→05-31 inclusive; Tyler wd=43; Grace wd=30), and scrambling.ts:208-213 explicitly documents why its 90 is honest (genuinely windowed loader). But CourseMgmtGenerator reads lifetime cache columns (penalty_strokes_per_round, eagles..triple_plus) and still stamps window_days:90 (course-mgmt.ts evidence blocks for both variants). Braeden Gillen's cache spans 2026-02-27→2026-04-21 — his first rounds predate the 90-day cutoff (2026-03-11), so his big_number/penalty cards (188cc16f, 5a0705c0) report a value computed over data outside the claimed 90-day window while saying 'Across your last 15 rounds'. Also creates the C3 cause-split denominator mismatch from VAL-3 for any such player.

Evidence: golf_player_stats_cache for a37e4fc9 (Braeden): first_round_date 2026-02-27, last_round_date 2026-04-21. Insights 188cc16f/5a0705c0 evidence: window_days '90', sample_n 15. Code: course-mgmt.ts (evidence stamps window_days: 90 in both penalty and big_number branches); contrast putt-distance.ts:252 (window_days: agg.spanDays) and scrambling.ts:208-213 (comment explicitly says only the SHOT-SOURCE engines may claim 90).

### [P3] (validity) Data staleness is invisible on cards — 'Across your last 15 rounds (54 days)' for a player whose newest round is 49 days old

Braeden Gillen's entire dataset ends 2026-04-21 (49 days before the 2026-06-09 generation); Grace Saunders' ends 2026-04-07 (63 days). Their cards (including a 'high' 3-5 ft putting alert and a 'high' scrambling alert) read as current ('Across your last 15 rounds (54 days)...') with confidence_factors.recency hard-coded 1.0 at write time. Mitigations verified: calcConfidence honest mode ignores the placeholder recency when factors_measured=false (confidence = sample_adequacy alone — confirmed in prod: 0.85=17/20, 0.267=8/30), and the lifecycle cron decays recency later. But at generation time nothing tells the coach the 'high' alert describes play from 7-9 weeks ago.

Evidence: Cache rows: Braeden last_round_date 2026-04-21, Grace 2026-04-07 vs generation timestamps 2026-06-09T23:38 (metadata.last_refreshed_at). Insight a2864281 content 'Across your last 15 rounds (54 days)' with confidence 1.0, recency 1.0, factors_measured:false. calcConfidence at src/lib/coachhelm/v2/insights/types.ts:195-214.

### [P3] (validity) approach_miss axis prose cites the classified-miss subset as if it were all misses ('82% of those 11 misses' on a card with 21 misses)

Lily Rowe's 125-175 yd card (11ec05cc) is numerically exact (53 attempts, 32 greens = 60.4%, 35.56 ft on-green proximity — all verified raw). But the axis sentence says '82% of those 11 misses came up SHORT' while the card's own green-hit line implies 21 misses (53-32). The 11 is dominantAxis's n = misses with a short/long component (9 short + 2 long; 10 further misses were pure left/right). 9/11 = 81.8% is correct for the subset, but a coach reading sequentially will wonder where the other 10 misses went. approachAxisDriver should say 'of the 11 misses with a distance read' or cite the share of all misses (43%).

Evidence: SQL recompute: attempts 53, greens 32, misses 21, miss_short 9, miss_long 2, neither 10. Code: diagnosis.ts:59-75 dominantAxis (n = negative+positive only, neutral excluded), diagnosis.ts:96-101 approachAxisDriver template '% of those ${n} misses came up SHORT'.

### [P3] (validity) Residual men's/mixed-gender anchors for women's players: course_management PGA anchors, par-type birdie baselines, and the pooled level_avg cohort

Gender anchoring is correctly applied where COHORT_ANCHORS has entries (verified for Grace/Lily: putt 84/52/28/11/4, approach 70/56/42, scrambling 38). Three residues remain: (1) course_management has no women's anchors — Grace's big_number/penalty cards compare to 'PGA Tour avg' 2%/0.3 (men's) (cohort-baselines.ts has no entry for big_number_rate/penalty_rate_per_round). (2) par-type's EXPECTED_BIRDIE_RATE prose classifier (par-type.ts:180) is documented men's/PGA-grade interim. (3) The standing 'level' cohort pools both genders: Grace (women) and Tyler (men) share the identical level_avg (par_4 4.3077, level_n=13), and that mixed cohort is the counterfactual's primary target — Grace's big_number CF (0.509 str/rd) is gapped to the mixed 6.2% cohort. All are documented-interim rather than bugs, but they shape women's CF impacts and priorities.

Evidence: Insights 01aa656d/7542bf25 (Grace) comparison_label 'PGA Tour avg' values 2/0.3; df334643 (Grace) and 2fc5133f (Tyler) both carry standing.level_avg 4.3076923076923075, level_n 13. cohort-baselines.ts:45-66 (no course_management metrics listed); par-type.ts:177-180 EXPECTED_BIRDIE_RATE comment 'Interim men's/PGA-grade'.

### [P1] (readpath) Coach morning-digest cron has no v3/visibility filter — its watch + celebration slots currently rank stale v2 rows #1-5

src/app/api/cron/coach-morning-digest/route.ts fetchInsights (lines 316-332) filters only player_ids + lifecycle + status<>'dismissed' + evidence NOT NULL. It never applies V3_ENGINE_FILTER, then ranks client-side by abs(strokes_impact)*confidence (rankScore, line 361). The cron is scheduled in vercel.json:58. Today's digest would send (6 v3 'matured' rows exist for the top slot) and its 'watch' (lifecycle detected) and 'celebration' (lifecycle resolved) slots would be filled from candidate pools whose top-5 by the cron's own rank function are ALL engine_version='v2' rows — retired-engine content the product has explicitly decided not to surface (insight-visibility.ts says these rows 'must never reach a coach'). Note also the detected pool admits status='resolved' rows (29 of 33 v2 detected rows are status resolved) because the only status guard is neq('status','dismissed'). Degrades to P0 the moment any v2 row reaches matured/addressed, because the top slot prints strokes_impact in the email.

Evidence: SQL: 55 v2 rows pass the digest filter (29 detected/resolved max|impact|=4.01, 21 resolved/resolved, 4 detected/active max|impact|=3.68). Rank simulation: top-5 'watch(detected)' candidates all v2 ('SG Putting: Primary Stroke Sink' score 2.778 …), top-5 'celebration(resolved)' all v2 ('150-175 yards misses leak ~78y from pin' score 2.178 …). Code: route.ts:316-332 (no .or(V3_ENGINE_FILTER)), 361-366 (rankScore), 268-271 (slot lifecycles); vercel.json:58.

### [P1] (readpath) getAlertCounts — the live unread-signal badge on every redesigned CoachHelm page — counts archived/tentative/v2 rows AND drops 'urgent' from the critical bucket (shows 47, truth is 35)

src/app/golf/actions/alerts.ts getAlertCounts (lines 164-189) filters only coach_id + dismissed=false + status='active'. Because v3 stale-scope retraction archives rows by lifecycle_state ONLY (generator-base.ts:263-271 leaves status='active'), every retracted/tentative/v2 row passes. Compounding bug: the bucket logic counts priority IN ('critical','high') as critical but the live priority enum is low/medium/high/urgent — so all 'urgent' rows fall into the INFO bucket and are excluded from the badge. This badge is the prod-live signalCount on alerts/page.tsx:72, intelligence/page.tsx:73, patterns, development, genome, chat, analytics pages (redesign flag is ON in prod), rendered via CoachHelmShell/FairwayBrief.

Evidence: SQL simulation of the exact predicate: badge_critical_as_coded=47; badge if scoped to product-visible (v3 + visible lifecycle)=27; urgent rows miscounted as info=9 (8 visible). Correct visible urgent+high=35 vs displayed 47. Inflating rows: 14 high + 1 urgent v3-archived, 6 high v3-tentative, 18 v2-archived (the par_scoring phantoms, |impact| up to 42.28). Code: alerts.ts:164-189; generator-base.ts:265-269; callers alerts/page.tsx:72, intelligence/page.tsx:73.

### [P1] (readpath) What's New coach feed renders tentative and retracted (archived) insights — 58 of the 119 rows in the current 7-day window are not product-visible

src/app/golf/actions/whats-new.ts query 2 ('Insights detected', lines 157-163) filters only team_id + created_at >= since — no lifecycle, no status, no engine filter. Rows flow straight into WhatsNewItem assembly with no client-side filtering (lines 248-280). The live /dashboard/whats-new page (FairwayWhatsNew) therefore shows pre-maturity 'tentative' rows (35 created in window, newest 2026-06-09 23:38 — today's regeneration — so they top the newest-first feed) and 19 retraction-archived v3 rows plus 5 v2 rows as 'newly detected'. Query 1 (lifecycle matured/resolved) also lacks the engine filter, admitting v2 resolved rows updated in-window.

Evidence: SQL (team-scoped, created_at >= now()-7d): detected/v3/active 61, tentative/v3/active 34, archived/v3/active 19, detected/v2 5 → 58/119 rows fail the shared visibility contract (insight-visibility.ts:25 'tentative … should never be shown'). Code: whats-new.ts:148-163, 248-280; consumer src/app/golf/(dashboard)/dashboard/whats-new/page.tsx:202-225.

### [P2] (readpath) Status vs lifecycle_state disagree by design and only the delivery layer normalizes — the systemic root behind DEL-1/2/3

Retraction (generator-base.ts retractStaleInScope) sets lifecycle_state='archived' but leaves status='active' (46 live v3 rows); 27 v2 rows have status='dismissed' but boolean dismissed=false; 29 v2 rows are lifecycle 'detected' with status 'resolved'. Any reader filtering on only one axis (status, dismissed, or lifecycle) gets a wrong row set. The shared insight-visibility module fixes this for delivery + causality, but every other golf_coach_insights reader hand-rolls its own predicate. Until the row state is made coherent (or all readers route through the shared filter), each new surface will re-create this bug class.

Evidence: SQL distribution: v3 archived/active/dismissed=false n=46; v2 archived/status=dismissed/dismissed=false n=27; v2 detected/status=resolved n=29 (max|impact| 4.01); v2 archived/active n=18 with max|impact| 42.28 (par_scoring_par4 phantoms, ids fee3deef…, f13440ae…). Code: generator-base.ts:263-271 (update omits status); insight-visibility.ts:21-25.

### [P2] (readpath) Coach LLM chat tool get_player_insights reads golf_coach_insights with zero visibility filters

src/lib/coachhelm/v3/chat/tools.ts get_player_insights (lines 80-102) selects by player_id + created_at DESC + limit, with no engine/lifecycle/status filter, and feeds title/content into the coach chat context. For players with few recent v3 rows, the 2026-05-23 v2 archived phantoms ('Par 4 scoring: +0.04 (↓ vs team)', |strokes_impact| up to 42.28) and dismissed/tentative rows are reachable, so the live coachhelm/chat surface can quote retracted or stale claims as current. get_team_patterns_summary (lines 249-253) and recap/builder.ts:119-124 similarly count ALL rows (incl. archived/tentative/v2) in coach-facing rollups.

Evidence: Code: chat/tools.ts:81-89 (no .or/.in filters); recap/builder.ts:119-131. SQL: 18 v2 archived/active rows all have player_id set (has_player=true) with |impact| 38.18-42.28; 35 tentative rows created 2026-06-07→09.

### [P2] (readpath) getTopInsightForPlayer's non-urgent pass still pre-trims to the newest 20 rows before ranking — one player already sits exactly at the cap

insight-delivery.ts:242-251 fetches .order('created_at', desc).limit(20) and only then ranks with scoreInsight. This is the same newest-first pre-rank truncation the to-95 P2 fix removed from the list feeds (which now fetch the full visible set via fetchAllRowsResult). Player b40a650a-bce0-4e46-87cd-365b0256ee21 has exactly 20 visible rows today; the 21st insight will make the Hub signal card silently ignore the oldest candidate even if it ranks highest, and the doc claim 'so the single-pick agrees with the list feed' (line 266) stops holding.

Evidence: Code: insight-delivery.ts:242-251 vs :333-349 (paginated full-set list path). SQL per-player visible counts: max=20 (b40a650a…), next 19, 19, 19 — at, not over, the cap; no player exceeds the OLD 100 cap, so the pagination change was correct future-proofing rather than a live fix.

### [P2] (readpath) Command-palette insight search selects a nonexistent `severity` column — the query 400s and coaches silently get zero insight results

src/app/golf/actions/command-palette.ts:139-151 selects id, title, severity, category, status from golf_coach_insights, but information_schema confirms golf_coach_insights has NO severity column (severity lives on golf_patterns_v2). PostgREST returns an error, and line 189 (`insightsResult.data ?? []`) swallows it, so the palette's insights section is permanently empty with no log. Secondary: if fixed, the query (status IN active,acknowledged, team-scoped) would also need the shared v3/lifecycle filter or it will surface the 46 archived-active v3 + 34 tentative + 18 phantom v2 rows.

Evidence: SQL: SELECT column_name FROM information_schema.columns WHERE table_name='golf_coach_insights' AND column_name IN ('severity',…) returns only signature/lifecycle_state/engine_version — no severity. Code: command-palette.ts:139-151, 189-196 (error discarded).

### [P2] (readpath) Flag-off legacy coach surfaces and one orphaned action still query golf_coach_insights unfiltered — the 42-stroke v2 phantoms pass their predicates

Latent because the redesign flag is ON in prod, but byte-for-byte reachable if it is ever flipped off: (a) alerts.ts getCoachAlerts:54-79 (coach_id + dismissed=false → renders v2 archived-active rows incl. |impact| 42.28 in CoachAlertCenter); (b) intelligence-dashboard.ts getTeamInsightsSummary:232-261 (team_id + dismissed=false → 45 v2-archived + 46 v3-archived + 34 tentative rows in the legacy intelligence list); (c) insight-management.ts searchInsights:89-97 + getInsightsStats:698-700 (legacy InsightsPageContent — stats counts 313 rows where the visible truth is 212); (d) players/[playerId]/page.tsx:186-191 SSR insight fetch (dismissed=false, newest 20 — today that includes tentative rows created 2026-06-09) — in the redesign branch FairwayPlayerInsight ignores this prop and re-fetches via the filtered getInsightsForCoach (FairwayPlayerInsight.tsx:399), so it is dead weight in prod but feeds the flag-off PlayerInsightClient directly; (e) insights.ts getActiveInsights:1093-1105 has no remaining callers (dead code) yet would return the phantoms.

Evidence: SQL surface simulations: alerts_counts predicate matches 313 rows (45 v3-archived, 34 tentative, 18 v2-archived, 4 v2-detected non-visible); intelligence_team predicate matches v2-archived 45 / v2-detected 37 / v2-resolved 37 / tentative 34; phantom rows max|impact| 42.28. Code lines cited per item; grep shows zero non-test callers of getActiveInsights.

### [P3] (readpath) The 'single source of truth' visibility module is only consumed by 2 of the ~6 filtered readers — the rest hand-roll string copies that can drift

insight-visibility.ts is imported by insight-delivery.ts and the causality cron only. insights.ts loadEvidenceBackedInsights (line 2562) hard-codes '.or(engine_version.eq.v3,signature.like.v3:%)' with its own lifecycle list ['detected','matured','addressed'] and eq('dismissed',false) — currently a conservative subset, but any future change to the shared constants will not propagate. composite/loader.ts:22-37 uses signature-prefix exclusion only; the digest cron (DEL-1) has no copy at all. Recommend exporting a query-builder helper from insight-visibility.ts and routing every reader through it.

Evidence: grep: V3_ENGINE_FILTER imported only in insight-delivery.ts:46-49 and api/cron/v3/causality-attribute/route.ts:96-98. Inline literal at insights.ts:2562-2564; module doc claims 'Single source of truth' (insight-visibility.ts:1-9).

### [P1] (stats) Causality engine's penalty_rate_per_round reads the drifted golf_rounds.total_penalties column (8 prod rounds wrong)

src/lib/coachhelm/v3/causality/metric-sources.ts:197 maps penalty_rate_per_round to { kind: 'rounds', column: 'total_penalties' }, and the file header (lines 15-16) confirms kind 'rounds' reads golf_rounds directly. But migration 20260608140000_cache_penalty_from_golf_holes_canonical.sql explicitly declared that column drifted ('trusted a drifted round column') and repointed the cache writer to SUM(golf_holes.penalty_strokes) — the causality metric source was not repointed. Verified live drift: 8 completed+scored rounds where golf_rounds.total_penalties=0 but SUM(golf_holes.penalty_strokes)=1 (round ids 19a57b3d-fcb5, 1e4db400-f54d, 63f306fc-9ab4, 78471fd6-0542, adfb67ae-6660, bc53cd38-e28b, cea96618-0ab9, dea4111a-01a9). Per-player aggregate: Nick Rini (49ffe06d) rounds-column sums 9 vs canonical 11; same 9-vs-11 for a37e4fc9 and b40a650a; 3b134c3e is 0 vs 2. Coach-facing cache stats are correct (they use golf_holes), but when outcome attribution starts (~2026-06-16 per the 21-day gate) the penalty_rate_per_round before/after deltas will be computed from understated values — a learning-integrity bug that is cheap to fix before any attribution rows exist (switch to round_stats_cache penalty_strokes, which is already canonical).

Evidence: metric-sources.ts:197 + migration 20260608140000 lines 1-9 + SQL: SELECT r.id ... GROUP BY r.id HAVING COALESCE(r.total_penalties,0) <> COALESCE(sum(h.penalty_strokes),0) returned 8 rows, all round_col=0 / holes_sum=1

### [P3] (stats) Stale comment claims putt-band make-% columns are '100% NULL in prod' — they are 20/20 populated

src/lib/cache/golf-stats-calculator.ts:279-286 says the refresh RPC 'does NOT compute the per-band putt make % columns (putt_make_pct_3_5ft … putt_make_pct_20_plus_ft) — they are 100% NULL in prod' and that the dependent standing metrics 'yield' nothing. Verified false in prod: all 20 cache rows have every putt_make_pct_* band AND all 5 putt_attempts_* columns populated (migrations 20260606160000 + 20260608130000 + 20260609090000 added the writer). The comment now misdirects future engineers toward a non-existent gap.

Evidence: SQL: SELECT count(*), count(putt_make_pct_3_5ft), ... FROM golf_player_stats_cache → 20 for every column; file comment at src/lib/cache/golf-stats-calculator.ts:279-286

### [P3] (stats) 3 putts have contradictory distance columns (distance_to_hole_before=1 ft vs putt_distance_feet=10)

Shots 80c88225, ea1a17f7, ebb12670 (one each for players b40a650a, 49ffe06d, a37e4fc9 — the cloned-round trio) record distance_to_hole_before=1 with unit 'feet' but putt_distance_feet=10, and putt_made=false/result='green'. The band writer trusts distance_to_hole_before, so each counts as a MISSED 1-foot putt in the 0-3 ft band (depressing putt_make_pct_0_3ft denominators by 1: e.g. b40a650a shows 96.6% rather than ~97.4%). A missed 1-footer followed by another putt is implausible; the 10-ft legacy column is likely the true distance. Only 3 rows, but the dual-column disagreement (3 'feet' rows + the 8 yards rows from STATS-P2-02 = 11 total mismatches between putt_distance_feet and distance_to_hole_before) indicates the round-entry path can write the two putt-distance columns inconsistently.

Evidence: SQL: putting shots WHERE putt_distance_feet <> distance_to_hole_before AND distance_unit_before='feet' → 3 rows, all hole 3 shot 3, dist_before=1, putt_distance_feet=10, made=false

### [P3] (stats) Demo data contains cloned players with byte-identical histories within and across teams (skews cohort/team baselines)

Three players share one identical 15-round history: Nick Rini (49ffe06d) and Jackson Hale (b40a650a) on team 6ecdd1a6 plus Braeden Gillen (a37e4fc9) on team b714c30f — identical avg SG (−4.435), identical 477 putts, identical penalties (11), identical cache rows. Likewise Ethan Park (8e382583, team 6ecdd1a6) == Luke Wise (32bd954c, team b714c30f) (13 rounds, −6.603, 412 putts, 16 penalties), Owen Carter (8091da4b) == Larsen Gallimore (cd822fe9), Tyler Hayes (faced578) == James Peach (3059fe37). Within-team duplicates (Rini==Hale on the same team) mean team aggregates, standing-bar percentiles and v3 cohort baselines double-count one data-generating process; any cohort-percentile insight on these teams is partially self-referential. Expected for seeded demo teams, but worth knowing when reading team-level numbers.

Evidence: SQL: identical n_putts/avg_sg/penalties tuples across distinct player_ids; golf_players + golf_team_members join showing names and team assignments

### [P3] (stats) golf_rounds.total_penalties remains un-backfilled after being declared drifted (root cause of STATS-P1-01 left in place)

Migration 20260608140000 documented the drift and routed the cache around it, but the denormalized column itself was never repaired: 8 completed+scored rounds still carry total_penalties=0 against a golf_holes sum of 1. src/app/golf/actions/golf.ts also references total_penalties (lines 686, 1192 — round payload assembly), so any round-detail surface reading the round column shows 0 penalties for those 8 rounds. Either backfill the column from golf_holes (one UPDATE) or stop writing/reading it entirely; leaving a known-drifted denormalized column live invites the next reader to trust it (exactly what metric-sources.ts:197 did).

Evidence: Migration 20260608140000 header ('r.total_penalties has drifted'); SQL drift query returning 8 rows; grep total_penalties → src/app/golf/actions/golf.ts:686,1192

### [P1] (lifecycle) Coach LLM chat tool returns sweep-retracted (archived) insights alongside their replacements, with no way to tell them apart

src/lib/coachhelm/v3/chat/tools.ts get_player_insights (lines 80-90) queries golf_coach_insights with ONLY player_id/category filters — no lifecycle_state filter, no status filter, and lifecycle_state is not even in the SELECT list ('id, insight_type, category, title, content, evidence, created_at'). It is live-wired into the coach chat agent (src/lib/coachhelm/v3/chat/agent.ts:116-119, served by /api/coachhelm/v3/chat/send) and the system prompt explicitly tells the model to call it for 'why is <player> worse?' (agent.ts:40). After tonight's sweep, 14 players each have an archived directional row (v3:putt_bias:left/right, created 06-06/06-07) AND a fresh contradicting v3:putt_bias:balanced row (created 2026-06-09 23:38) — the tool returns both, so the LLM can assert a putt bias the engine retracted 30 minutes earlier. The team rollup tools (tools.ts:250 get_team_patterns, weekly summary ~317) likewise count archived rows by created_at only. The shared visibility module (src/lib/coachhelm/v3/insight-visibility.ts) exists exactly for this and is not imported by chat/tools.ts. Note status-only filtering cannot fix this: all 23 sweep-archived rows carry status='active' by design.

Evidence: SQL: 64 rows with lifecycle_state='archived' AND status='active' (63 with coach_id, 0 dismissed); putt_bias breakdown: 7 'left' + 7 'right' archived (created 06-06 15:13 to 06-07 03:56) vs 14 'balanced' created 2026-06-09 23:38:25-48. Code: tools.ts:80-90 (no lifecycle/status predicate), agent.ts:116-119 (tool registered), grep shows insight-visibility.ts imported only by insight-delivery.ts and causality-attribute cron.

### [P2] (lifecycle) Alert badge count (getAlertCounts) includes the 64 archived+active rows — badge disagrees with the lifecycle-filtered Alert Center list

src/app/golf/actions/alerts.ts getAlertCounts (~lines 164-168) counts golf_coach_insights with .eq('coach_id', …).eq('dismissed', false).eq('status','active') — no lifecycle filter. All 64 archived-but-status-active rows match (63 have coach_id, none dismissed), including tonight's 23 retractions, so the notification badge (src/contexts/notification-badge-context.tsx:7, consumed by alerts/patterns/development/coachhelm pages) is inflated, while the Alert Center list itself fetches through the lifecycle-filtered getInsightsForCoach (src/components/golf/coachhelm/alerts/CoachAlertCenter.tsx:64 → insight-delivery.ts). Each future sweep run widens the badge/list mismatch. Same status-only hole exists in alerts.ts getAlerts (line ~54, dismissed=false only) and insights.ts getActiveInsights (lines 1094-1102, status='active' only), though I found no live UI caller for either (latent).

Evidence: SQL: SELECT count(*) FROM golf_coach_insights WHERE lifecycle_state='archived' AND status='active' → 64, with_coach_id=63, not_dismissed=64. Code: alerts.ts:164-168; notification-badge-context.tsx:7; CoachAlertCenter.tsx:11+64 documents/uses getInsightsForCoach; grep for getActiveInsights callers outside actions/insights.ts → none.

### [P2] (lifecycle) Matured rows have NO retraction path: 3 matured warmup_hole rows are dequalified-but-stuck-visible indefinitely

The sweep only touches tentative/detected rows (generator-base.ts retractStaleInScope: .in('lifecycle_state', ['tentative','detected'])) and the age-based lifecycle cron never archives matured rows (src/app/api/cron/coachhelm-insight-lifecycle/route.ts: rule 2 applies to lifecycle==='detected' only, rule 3 requires lifecycle !== 'matured'). In tonight's regen the warmup generator demonstrably ran (it archived 5 DETECTED v3:warmup_hole:hole_1 rows at 23:38) but did NOT re-emit for the 3 players holding MATURED warmup rows — upsertInsightV3 delegates to v2 updateExisting which always bumps updated_at on emit, and these rows' updated_at predates the regen. So these 3 players' warmup insight no longer qualifies, yet the rows stay in VISIBLE_LIFECYCLE_STATES forever unless the player re-qualifies or a coach acts. 'matured' carries movement history so auto-archiving is deliberately conservative, but 'forever' needs a bound (e.g. matured + not re-emitted for N consecutive runs → archive, with resurrection already in place as the safety net).

Evidence: SQL: active v3 rows not refreshed in regen include 64d8eb4d (player 49ffe06d…, matured, updated 2026-06-09 19:33), b431885f (a37e4fc9…, matured, updated 2026-06-08 03:45), d151f140 (b40a650a…, matured, updated 2026-06-09 03:45) — all signature v3:warmup_hole:hole_1; same regen archived 5 detected warmup rows for other players at 23:38. Code: generator-base.ts:265-267; coachhelm-insight-lifecycle/route.ts:308-331; insight-visibility.ts:25 includes 'matured'.

### [P2] (lifecycle) The 2 composite rows are stale-and-stuck — regen ran composite synthesis and did not re-emit them, and no sweep scope covers composites

Both leftover composite rows — v3:composite:lag_distance_3putt:lag_distance_3putt (player b40a650a…, detected/active, created 06-08, updated 06-09 03:45) and v3:composite:pressure_decel_chain:pressure_decel:3_5ft (player 7be2b4a8…, detected/active, created 06-07, updated 06-09 03:45) — were NOT legitimately current: the regen script (scripts/regen-coachhelm-from-corrected-stats.ts:100) calls analyzePlayer, which runs synthesizeForPlayer after Tier-1 generators (v2/orchestrator.ts:311), and synthesis emits every surviving match through upsertInsightV3 → v2 updateExisting, which unconditionally bumps updated_at. Neither row was bumped at 23:38 while both players' Tier-1 rows were (their putt_bias rows were archived/emitted at 23:38:33-41) → the composite rules no longer match against the regenerated Tier-1 set. Because composites live outside every BaseGenerator signatureScope (the 9 scopes are putt_bias:/tee_strategy:/warmup_hole:hole_1/pressure_gap:…/par_scoring:parN/approach_miss:<bucket>/putt_distance:<bucket>/scrambling:<lie>/course_mgmt variants), nothing retracts them; the only fallback is lifecycle-cron rule 2 (detected + 0 movements + age>30d), so they stay coach-visible up to ~4 more weeks after dequalifying. Composite synthesis needs its own scope sweep (e.g. retract v3:composite:<rule>:% rows the run didn't re-emit).

Evidence: SQL: the two rows above are the only non-warmup v3 rows in visible lifecycle with updated_at < regen time (06-09 03:45 vs sweep cluster 23:38:25-48). Code: regen script imports orchestrator and calls analyzePlayer (scripts/regen-coachhelm-from-corrected-stats.ts:45,100); orchestrator.ts:309-318; synthesis.ts:6-7 + 157-158 (emits via upsertInsightV3, idempotent dedup = update path); upsert.ts updateExisting sets updated_at: nowIso in both branches (lines 192-198, 264-266).

### [P3] (lifecycle) Sweep-archived rows carry no provenance — indistinguishable from coach- or cron-archival

retractStaleInScope updates only lifecycle_state/archived_at/updated_at (generator-base.ts:265). All 23 rows archived tonight have metadata->>'archive_reason' and metadata->>'archived_by' = null, and status stays 'active'. There is no marker distinguishing scope-retraction from the age cron or a coach archive (insight-management.ts:269 also stamps nothing). This makes post-hoc audits like this one — and any future 'resurrection rate' or 'false retraction' metric — depend on timestamp archaeology. Stamp e.g. metadata.retracted_by_scope = '<generator>:<scope>' in the sweep payload (updateExisting already stamps redetected_at on resurrection, so the write-side pattern exists).

Evidence: SQL: all 23 rows archived 2026-06-09 23:38 have archive_reason=null, archived_by=null, status='active'. Code: generator-base.ts:265 update payload is exactly { lifecycle_state, archived_at, updated_at }.

### [P1] (causality) Attribution cron permanently stalls: never-attributable rows clog the fixed oldest-150 candidate window and the 50-slot work list

Dedup is 'absence of an attribution row' (route.ts:106-116), but rows whose metric resolves to intentional-null NEVER get a row written (route.ts:139-153: continue without insert), and the candidate fetch (route.ts:90-99) is ordered created_at ASC with .limit(150) and does NOT exclude already-attributed rows in SQL. Prod data: 120/223 (54%) of currently-visible v3 insights carry intentional-null metrics (all putts_made_*, approach_proximity_*, putt_miss_bias_*, practice_tournament_delta, opening_hole_delta, three_putt_chain, short_side_proximity); additionally 58/103 measurable insights have ZERO completed rounds in their historical 14d pre-window, so they return 'no-data' forever (the pre-window can never fill; 62/103 also currently lack post-window rounds). Positionally: the 50th sticky row sits at created_at rank 112, so the 41 measurable insights after it can never enter todo.slice(0,50); 61 sticky rows sit inside the first 150, and 73 visible rows lie entirely beyond the pinned 150-row fetch window. Net effect: within days of first eligibility (2026-06-16) every daily 06:00 UTC run re-processes the same ~50 oldest sticky rows, and the learning loop silently stops attributing anything — including all future insights — forever. This also directly contradicts the route's own comment (route.ts:36-40: intentional-no-lift rows 'will never produce a lift; do NOT retry'). Fix direction: either write a marker/skip row for intentional-null + permanently-no-data insights, or anti-join golf_insight_outcome_attribution and pre-filter intentional-null metrics in the candidate query.

Evidence: Code: src/app/api/cron/v3/causality-attribute/route.ts:27 (LIMIT=50), :90-99 (fetch, no attribution anti-join, .limit(LIMIT*3)), :114-116 (slice(0,50)), :139-153 (intentional-null/no-data continue without insert). SQL over visible v3 set (engine_version='v3' OR signature LIKE 'v3:%', lifecycle IN visible, status<>'dismissed', player_id NOT NULL): total_sticky=120, total_measurable=103, pos_of_50th_sticky=112, measurable_after_50th_sticky=41, sticky_in_first_150=61, rows_beyond_overfetch_window=73; no_pre_window_rounds=58/103, no_post_window_rounds=62/103.

### [P2] (causality) Pre and post attribution windows both include the surfaced calendar day — the triggering round is averaged into baseline AND post

computeAttribution sets preEnd = surfaced−1ms and postStart = surfaced+1ms (attribute.ts:312-315), but every window function truncates to a date string via .slice(0,10) and filters with inclusive gte/lte on round_date (attribute.ts:97-98 rounds; :134-135 ratio; :175-176 computed; :215-216 hole-level). Both windows therefore include round_date == surfaced date. Since v3 insights are typically written the same day the triggering round is ingested, that round (usually the outlier that fired the insight) contaminates the post average with pre-intervention data and double-counts in baseline, biasing lift toward zero/negative. Currently affects 5/103 measurable insights; share will grow as the engine runs same-day as round ingest. Fix: exclude the surfaced date from one window (e.g. post starts at surfaced date + 1 day).

Evidence: src/lib/coachhelm/v3/causality/attribute.ts:312-315 + :97-98/:134-135/:175-176/:215-216. SQL: of 103 measurable visible v3 insights, 5 have a completed golf_rounds row with round_date = created_at::date for the same player.

### [P3] (causality) Null-lift attributions still upsert golf_coachhelm_coach_weights rows (weight=1.0, sample_n=0)

After a successful attribution insert, updateCoachWeight is called even when row.lift is null (route.ts:195-201). nextWeight correctly no-ops on null lift (attribute.ts:393 returns prev), but the route then unconditionally upserts the unchanged base {weight:1.0, sample_n:0} (route.ts:255-265). With MIN_AMBIENT_ROUNDS=2 gating many early lifts to null, the freshly-wiped weights table will repopulate with sample_n=0 baseline rows that look like learned state but encode zero evidence, and updated_at on real rows is touched without any weight movement. Cheap fix: skip the upsert when nextWeight returns prev unchanged (lift === null).

Evidence: src/app/api/cron/v3/causality-attribute/route.ts:195-201, :249-265; src/lib/coachhelm/v3/causality/attribute.ts:392-394 (null lift returns prev), :351-354 (ambient gate produces null lifts). golf_coachhelm_coach_weights currently 0 rows (verified by SQL).

### [P1] (security) golf_rounds: authenticated/anon hold table-wide UPDATE on team_id, and the player self-update RLS policy does not pin team_id — an authenticated player can reassign their own round to ANY team, injecting cross-team rows into a victim coach's feed/aggregates

Today's table-wide UPDATE grant on golf_rounds covers all 42 columns including team_id and player_id for both authenticated AND anon (verified: includes_team_id=true, includes_player_id=true). Rows ARE scoped by RLS, but the player-owned UPDATE policy 'golf_rounds_update' has USING = EXISTS(golf_players where id=player_id AND user_id=auth.uid()) and WITH CHECK = null, so Postgres reuses the USING expression as WITH CHECK. That expression constrains only player ownership (NEW.player_id) and never references team_id. Result: an authenticated user who owns a round can issue a direct PostgREST PATCH setting team_id to an arbitrary team uuid; the NEW row still satisfies player ownership, so the write passes. The reassigned round then matches the victim coach's read policy 'golf_rounds_select_team' (team_id = X AND is_golf_team_coach(X)) and surfaces in their team dashboard / any team-scoped aggregation — i.e., a coach sees a round that is not from their roster. The only trigger on golf_rounds is AFTER trg_update_round_stats_cache (func update_round_stats_cache), which recomputes the stats cache and does NOT validate team_id or membership (func_refs_team_id=false, func_checks_membership=false), so nothing neutralizes the write. Exploitation requires a crafted API call and knowledge of a target team uuid, and the injected round carries an off-roster player_id (a coach might notice), which is why this is P1 not P0. Fix: add a WITH CHECK to golf_rounds_update that constrains team_id (e.g. team_id IS NULL OR is_golf_team_player(team_id)), or revoke UPDATE(team_id, player_id) at the column level for authenticated/anon.

Evidence: pg_policy on golf_rounds: 'golf_rounds_update' polcmd='w' roles={authenticated} using=EXISTS(golf_players ... user_id=auth.uid()) with_check=null. information_schema.role_column_grants: grantee=authenticated and anon, privilege_type=UPDATE, n_columns=42, includes_team_id=true, includes_player_id=true. pg_trigger on golf_rounds: only trg_update_round_stats_cache (AFTER), func update_round_stats_cache func_refs_team_id=false func_checks_membership=false.

### [P3] (security) Supabase default ACL grants anon AND authenticated full DML (arwdDxtm) on every golf_% table and all future tables — protection is RLS-only

pg_default_acl for schema public (owner postgres and supabase_admin) grants relations 'arwdDxtm' to anon, authenticated, and service_role, so every existing golf_% table (84 verified) and every future table auto-grants INSERT/UPDATE/DELETE/TRUNCATE to anon and authenticated. Security depends entirely on RLS being enabled with correct policies. Today this is sound: relrowsecurity=true on all 84 golf_% tables and there are zero anon-inclusive RLS policies (query returned empty), so anon (rolbypassrls=false) is default-denied everywhere; the PUBLIC-role policies on golf_rounds (golf_rounds_*_coach, polroles={-}) gate on is_golf_team_coach which returns false when auth.uid() is null. The latent risk: any future golf_% table shipped without explicitly enabling RLS, or any future permissive write policy, instantly becomes anon/authenticated-writable because the column/table grants are already wide open. This is the documented Supabase posture, hence P3, but worth a hardening note (REVOKE blanket DML from anon, or a CI check that every new public table has RLS enabled).

Evidence: pg_default_acl: defaclacl includes anon=arwdDxtm/postgres, authenticated=arwdDxtm/postgres for objtype 'r' (relations) in schema public. Aggregate query: all 84 golf_% relations relrowsecurity=true; per-table anon_privs='DELETE,INSERT,REFERENCES,SELECT,TRIGGER,TRUNCATE,UPDATE'. Policy-role query for anon-inclusive golf_% policies returned []. pg_roles: anon rolbypassrls=false.

### [P1] (newcode) Resurrection vs lifecycle cron Rules 2/3 = guaranteed nightly archive<->detected flap starting ~2026-06-25

The new resurrection in updateExisting (src/lib/coachhelm/v2/insights/upsert.ts:213-217 refresh branch, 272-276 movement branch) sets lifecycle_state='detected', nulls archived_at, stamps metadata.redetected_at — but NEVER resets metadata.movement_count and NEVER touches created_at. The lifecycle cron (src/app/api/cron/coachhelm-insight-lifecycle/route.ts) Rule 2 (lines 320-334) archives detected rows with movement_count==0 AND ageDays>30 computed from created_at (line 303-304); Rule 3 (lines 308-318) archives ANY non-matured/non-addressed row >90d. Neither rule looks at redetected_at or last_refreshed_at. Trace of the loop: a stable insight (evidence moves <5% per run, so the refresh branch never increments movement_count) crosses 30d → cron archives it at 02:00 → roster-sweep re-runs the engine at 03:45 nightly (src/app/api/cron/coachhelm-roster-sweep/route.ts header + vercel.json '45 3 * * *') → same signature re-emitted → dedup lookup (upsert.ts:135-150, no lifecycle filter) finds the archived row → refresh branch resurrects to detected with movement_count still 0 → next 02:00 the cron archives it again (updated_at=03:45 passes the 6h stale filter at route.ts:143). Infinite daily flap. Consequences: row invisible to every feed 02:00→03:45 daily (VISIBLE_LIFECYCLE_STATES excludes archived) and up to ~24h+ when the sweep's 12h skip window defers a player; archived_at/redetected_at/updated_at become meaningless churn; Rule 3's 90d aging is permanently defeated for any still-emitted insight; the causality cron (06:00) sees the row archived on any day the 03:45 resurrection didn't fire, throttling attribution of exactly the ≥21d-old rows the learning loop needs. Latent today only because v3 is young: prod has 107 active detected movement_count=0 v3 rows, oldest created 2026-05-26 — the first cohort crosses the 30d gate ~2026-06-25. Fix direction: cron Rules 2/3 should treat metadata.last_refreshed_at (or redetected_at) as the staleness anchor, or resurrection should be accompanied by the cron skipping recently-refreshed rows.

Evidence: upsert.ts:213-217,272-276 (no movement_count reset); coachhelm-insight-lifecycle/route.ts:303-334 (age from created_at only); vercel.json crons (lifecycle 02:00, roster-sweep 03:45 daily, causality 06:00); prod SQL: 107 active v3 detected rows with movement_count=0, min(created_at)=2026-05-26, 0 currently >16d (flap not yet manifest, deterministic in ~16 days)

### [P1] (newcode) Every generator conflates transient query errors with dequalification — new retraction archives valid live insights on a DB hiccup

BaseGenerator.run() treats aggregate()==null as 'true dequalification — no data in the window' and fires retractStaleInScope(null), archiving ALL active tentative/detected rows in the scope (src/lib/coachhelm/v3/engine/generator-base.ts:305-315). But every one of the 9 generators returns null/empty on QUERY ERROR, not just on no-data: putt-bias.ts:174 ('if (rErr || !rounds ...) return null') and :190 ('if (pErr || !putts) return null'); putt-distance.ts:155 ('if (error || !data) return null'); shot-source.ts loadApproachShots/loadSandShots/loadTeeShotsForStrategy all 'return []' on rErr/error (e.g. 'if (rErr || !rounds || rounds.length === 0) return []'), which approach-miss/scrambling/tee-strategy aggregate() then converts to null; pressure-gap and par-type follow the same '(error || !data) return null' pattern. Before today, null-on-error was a harmless skip; today's retraction makes it destructive: one transient PostgREST/fetch failure during the nightly sweep soft-archives the player's live insights in that scope. The codebase elsewhere acknowledges these transients are real (isTransientFetchError + retry machinery in insight-delivery.ts:361-373) — the generator aggregates have no retry at all. Self-healing only at the NEXT successful run (up to ~24h via roster-sweep), during which the coach's feed/alerts silently lose rows. Fix: distinguish error from empty (throw on query error so run()'s catch skips retraction, or return a sentinel), and/or only retract when the aggregate query verifiably succeeded.

Evidence: generator-base.ts:305-315 (retraction on null agg with comment asserting 'True dequalification'); putt-bias.ts:174,190; putt-distance.ts:155; shot-source.ts:126-127,138 (loadApproachShots returns [] on rErr and on error), same pattern in loadSandShots/loadTeeShotsForStrategy; no retry wrapper in any v3 aggregate path

### [P2] (newcode) Resurrection bypasses the tentative confidence gate — archived low-confidence rows come back coach-visible as 'detected'

insertNew gates visibility on confidence: <0.4 inserts as 'tentative' (upsert.ts:333-334), and VISIBLE_LIFECYCLE_STATES excludes tentative — policy says sub-0.4 rows are never shown. The resurrection path ignores this: updateExisting sets lifecycle_state='detected' unconditionally whenever existing.lifecycle_state==='archived' (upsert.ts:213-214 and 272-273), even when the freshly recomputed confidence is below 0.4. The scope sweep makes this reachable in practice because retractStaleInScope archives 'tentative' rows too (generator-base.ts:269 .in('lifecycle_state', ['tentative','detected'])): e.g. a tentative putt_bias directional row is archived when the player heals to 'balanced'; if the bias later returns and the same signature is re-emitted with confidence still <0.4, it resurrects straight to 'detected' and becomes visible — a state a fresh insert of the identical evidence could never reach. Fix: on resurrection, choose 'tentative' vs 'detected' from the recomputed confidence using the same TENTATIVE_CONFIDENCE_FLOOR as insertNew.

Evidence: upsert.ts:213-214,272-273 (unconditional 'detected' on resurrection) vs upsert.ts:333-334 (confidence-gated state on insert); generator-base.ts:269 (sweep archives tentative rows); insight-visibility.ts:25 (tentative not visible); prod: putt_distance/putt_bias signatures currently carry 'tentative' states (live state map query)

### [P2] (newcode) putt_bias shared scope across two concurrent sibling instances: divergent aggregates (or one erroring) can mutually archive both fresh rows

PuttBiasGenerator left+right instances run concurrently in the tier-1 Promise.allSettled batch (v2/orchestrator.ts:240-241,275) and BOTH return signatureScope()='putt_bias:' (putt-bias.ts:158-160) — the only cross-instance scope share in the system. The line-157 comment claims safety because both 'aggregate the SAME data and emit the same signature', but each instance issues its OWN golf_rounds + golf_shots queries at different instants. If data changes between the two reads (round completing / shots landing mid-batch), or one instance's read errors (returns null → full-scope retraction with keepSignature=null per CR-2), the instances emit different signatures and each post-emit sweep archives the other's just-written row — interleaving 'L-write, R-write, L-sweep(keep L), R-sweep(keep R)' leaves BOTH freshly-written rows archived and the player with zero active putt_bias insight despite fresh evidence. The same mechanism applies to any data-derived-signature generator (tee_strategy pattern, putt_bias band) when two analyze runs of the SAME player overlap (post-round trigger + 30-min safety-net cron — a TOCTOU pair upsert.ts:359-365 already acknowledges exists). Self-heals on the next run via resurrection, but combined with CR-3 the resurrected state may be wrong. Narrow window, but it is exactly the kind of race the Promise.allSettled fan-out makes possible nightly across 18+ players.

Evidence: orchestrator.ts:240-241 (two instances), 275 (Promise.allSettled); putt-bias.ts:157-160 (shared scope + idempotency comment resting on same-read assumption); generator-base.ts:272-274 (.neq keepSignature only protects the instance's own emission); upsert.ts:362-364 (concurrent-run TOCTOU acknowledged for the same player)

### [P2] (newcode) Composite rules have data-derived signatures but no stale-scope retraction — the exact gap signatureScope closed for the 9 generators remains open for all 10 composite rules

Composite synthesis writes signatures that embed data-derived suffixes — 'v3:composite:pressure_decel_chain:pressure_decel:3_5ft' vs ':5_10ft', 'bunker_left_bias_amp' vs 'bunker_right_bias_amp' (synthesis.ts:267 builds sig from rule.id + composed.signature) — via upsertInsightV3, but synthesizeForPlayer has no analogue of retractStaleInScope. When a player's decel band moves 3_5ft→5_10ft or bunker bias flips left→right, the superseded sibling row stays active and coach-visible alongside the new one until the age-based cron catches it at 30/90d (and per CR-1 the cron's archive may not stick). Prod currently shows both band variants live across different players (pressure_decel:3_5ft on 2 players, :5_10ft on 1; bunker left on 3, right on 2) — no same-player duplicate yet, but the first band/side flip will create one. Today's feature explicitly identified and fixed this 'old band/pattern row lives forever' class for the 9 BaseGenerator families (generator-base.ts:440-444 comment) while leaving the composite write path uncovered.

Evidence: synthesis.ts:267-272 (data-derived signature, upsertInsightV3, no sweep); no signatureScope/retraction in src/lib/coachhelm/v3/composite/*; prod SQL: 8 active composite rows across pressure_decel bands and bunker sides, all lifecycle 'detected'

### [P2] (newcode) Hub top-insight still ranks a newest-20 created_at window (no pagination, no collapse/dedupe) while the feed ranks the full set — and prod is already exactly at the 20-row boundary

Today's pagination converted getInsightsForPlayer and both getInsightsForCoach branches to fetchAllRowsResult over the FULL visible set (insight-delivery.ts:333-349, 483-502, 512-530), explicitly because 'an older high-impact row' must not be starved before ranking. getTopInsightForPlayer's second pass was NOT converted: it still fetches .order('created_at', desc).limit(20) (insight-delivery.ts:242-251) and ranks only those 20 with the same rankEvidenceInsights composite. Prod max visible rows per player is EXACTLY 20 today (players_over_20=0, max=20) — the very next insight for that player pushes the oldest row out of the Hub candidate window, so the Hub signal card and the feed's #1 card can disagree, the precise inconsistency the to-95 P2 fix was meant to eliminate. Additionally the top-pick path skips collapseParScoring + dedupeBySubject (applied to the feed at line 427), so the Hub card can feature a lone par_scoring sibling the feed only ever shows merged.

Evidence: insight-delivery.ts:242-251 (limit 20, created_at desc) vs 333-349 (paginated full set + comment condemning the cap pattern) and 427 (dedupe/collapse feed-only); prod SQL: max_visible_per_player=20, players_over_20=0

### [P3] (newcode) Causality cron swallows the candidate-fetch error — infra failure reports as a successful empty run

The candidates query destructures only { data: candidates } and never inspects error (causality-attribute/route.ts:90-99); on a query failure candidates is null and the cron returns the success-shaped summary with considered=0 (route.ts:101-104). A persistent fetch failure would silently stall the entire learning loop (no attribution rows, no weight movement) with no Sentry signal, indistinguishable from a legitimately empty backlog — particularly costly given the loop was just zeroed and the first eligible candidates arrive ~2026-06-16.

Evidence: src/app/api/cron/v3/causality-attribute/route.ts:90-104 (no error capture on the .select(), early-return success on null data)

### [P3] (newcode) Coach-weight EMA upsert result is unchecked — weight writes can fail silently

updateCoachWeight awaits fromUntyped(sb,'golf_coachhelm_coach_weights').upsert(...) and discards the { error } result entirely (causality-attribute/route.ts:255-265). A grant/constraint failure (the exact class behind the prior 'upsert needs UPDATE grant' incident on round_reviews/coach_insights) would leave attribution rows written but weights frozen at 1.0 forever, with summary.attributed still incrementing — the learning loop would look healthy while half of it is dead. One error check + logServerError closes it.

Evidence: src/app/api/cron/v3/causality-attribute/route.ts:255-265 (awaited upsert, return value ignored); compare insert path at 156-193 which DOES check insErr

### [P3] (newcode) Team toggle-off (isEnabled=false) skips retraction — disabling a generator leaves its old rows coach-visible up to 30-90 days

run() returns { gated: true } before any retraction when the per-team toggle is off (generator-base.ts:300-303), and the GATED_OUT exit likewise skips it with an explicit rationale comment (435-439). The gate rationale ('a product decision to not ADD a row') is defensible for the philosophy confidence gate, but for an explicit team opt-out (e.g. tee-strategy off) the surviving rows keep rendering an insight type the team just disabled, until the age-based cron archives them — and per CR-1 detected rows younger than 30d have no other retirement path. Worth either sweeping the scope on the isEnabled=false exit or documenting the up-to-30d lag in the settings UI.

Evidence: generator-base.ts:300-303 (toggle exit, no retraction), 221-225 (isEnabled contract), 435-439 (gate exit comment only justifies the philosophy-gate case)

## Verified strengths (60)

- [validity] 7 of 8 sampled insights recompute EXACTLY against raw tables, to the stored decimal: Tyler Hayes pressure_gap (-0.25 = 3.000 competitive avg over 7 rounds minus 3.250 practice avg over 4; n=11), Jackson Hale 3-5ft putting (20/43 = 46.5% raw golf_shots), Larsen Gallimore scrambling (3/17 = 17.6% via canonical golf_holes.sand_save flags; 13/17 = 76% escape; 15.5 ft avg leave), Grace Saunders scrambling (0/17 saves, 7/17 = 41% escape, 12 ft leave), Lily Rowe approach 125-175 (53 attempts, 32 greens = 60.4%, 35.56 ft on-green-only proximity — no yards-x3 inflation), Tyler Hayes par-4 (113 holes, 4.239, rates 13.3/23.9/6.2), Larsen big_number (8.333%, 46.7% penalty cause, worst holes 17 +0.8/10 and 12 +0.7/10 all exact).
- [validity] Counterfactual clamp holds globally: 0 of 223 active v3 rows exceed strokes_saved_per_round 2.5; rows at the clamp are honestly flagged clamped:true (Jackson/Braeden/Mason 3-5ft putting at exactly 2.5).
- [validity] strokes_impact is consistent with the counterfactual on all 223 active rows (0 mismatches where impact is non-zero; suppressed CFs correctly leave impact 0 — e.g. Tyler pressure_gap and par-4 both suppress_reason no_gap because he beats the cohort).
- [validity] Counterfactual arithmetic reproduces from first principles: Larsen scrambling ssr 0.5202 = (50-17.6)/100 x 1.889 attempts/rd x ~0.85 strokes/save; Grace ssr 1.0982 = (38-0)/100 x 3.4 x ~0.85 — both attempts_used values match raw attempts/rounds (17/9 and 17/5).
- [validity] Women's anchors verified end-to-end for the women's team: Grace Saunders putt_distance rows carry Women's college 84/52/28/11/4, approach 70/56/42, scrambling 38 (in prose AND in the CF gap math); Lily Rowe approach uses 56. Men's players keep unchanged Tour values.
- [validity] Window honesty holds where the fix was applied: cache-backed putt_distance and par_scoring carry the true lifetime span (Jackson 54d = 2026-04-08..05-31 inclusive; Tyler 43d; Grace 30d — all verified against cache first/last round dates), and shot-windowed generators (scrambling, approach, pressure_gap) honestly claim the real 90-day loader window.
- [validity] Floor-exemption works as designed: Grace's par-4 card keeps priority 'low' and strokes_impact 0 despite a non-suppressed capped CF of 1.5 (scoring_par_* exempt in leveragePriorityFloor), and feed_exempt:true is stamped; the per-metric CF cap (1.5 for par_4, flagged clamped:true) is live in prod.
- [validity] The identical Jackson Hale / Braeden Gillen evidence (same 46.5%, n=43 across all 5 putt buckets) is NOT engine cross-contamination: raw golf_shots recompute shows both players genuinely have 43 attempts / 20 makes — cloned, time-shifted demo datasets. The engine is faithful to its inputs.
- [validity] calcConfidence honest mode (SV-1) verified live: with factors_measured:false, stored confidence equals pure sample adequacy (0.85 = 17/20 sand attempts, 0.267 = 8/30 rounds, 1.0 = 43/30 capped) — no fabricated +0.45 placeholder floor; pressure_gap ships genuinely measured dispersion (variance 0.2, factors_measured:true).
- [validity] Sand-save reconciliation contract holds: the scrambling card's printed save % reads the canonical golf_holes.sand_save flag (the same source as cache/stat surfaces), and the greenside-only denominator (shot_type/around-green-50yd gate, fairway-bunker approaches excluded) reproduced the stored 17-attempt counts exactly for both players.
- [validity] Sample gates honored everywhere sampled: pressure_gap required >=3 rounds in BOTH buckets (Tyler 7+4), putt bands gate on attempts (all sampled n >= 24), approach proximity only reported with >=3 greens hit, par-type suppresses driver prose under 5 holes.
- [readpath] All canonical delivery fetchers apply the full shared visibility contract in-DB: getTopInsightForPlayer (both the urgent pass at insight-delivery.ts:219-229 and the ranked pass at :242-251), getInsightsForPlayer (:333-349), getInsightsForCoach (per-player :483-502 and team sweep :512-530), getRoundTakeawayInsight (:634-645), and the themes path assembleForPlayer (:800-810, stricter status='active') all chain .not('evidence','is',null) + .or(V3_ENGINE_FILTER) + .in('lifecycle_state', VISIBLE_LIFECYCLE_STATES) + status guard.
- [readpath] Live visible set cannot poison ranking: SQL over the shared-filter predicate returns 223 rows, ZERO with |strokes_impact| > 8, max |impact| 3.02, and no rows with non-numeric impact — the 42-stroke v2 phantoms are fully excluded from every delivery read.
- [readpath] The .or(engine_version.eq.v3,signature.like.v3:%) belt-and-suspenders filter is precise on live data: all 304 v3 rows carry BOTH stamps and zero rows match on only one stamp, so no v2 row can slip through the OR.
- [readpath] The causality attribution cron (api/cron/v3/causality-attribute/route.ts:91-98) imports and applies the exact same V3_ENGINE_FILTER + VISIBLE_LIFECYCLE_STATES + status<>'dismissed' boundary as delivery, so the freshly-zeroed learning loop can only train on product-visible rows.
- [readpath] Pagination is correctly implemented and safely conservative: fetchAllRowsResult (src/lib/supabase/fetch-all-rows.ts) pages past the PostgREST 1000-row cap with the documented stable .order('id') contract, and live per-player visible sets max out at 20 rows (team total 223, soft ceiling 800 with logged warning at insight-delivery.ts:540-548) — one round trip today with honest headroom.
- [readpath] Par-scoring collapse inputs match the live data exactly: 14 of 18 insight-bearing players have exactly 3 visible rows with metric scoring_par_3/4/5, and evidence carries the precise fields collapseParScoring reads (numeric your_value, detail.bogey_rate, detail.double_plus_rate verified on player b40a650a), so the 3-rows-to-one-card merge fires for every par-complete player; canonicalMetricSubject (insight-delivery-ranking.ts:34-40) additionally aliases v2 par_scoring_parN to scoring_par_N so cross-version duplicates dedupe to one card.
- [readpath] The prod-live (redesign-on) coach surfaces read exclusively through the filtered path: FairwayCoachHelmSignals imports getInsightsForCoach as its single insight read (FairwayCoachHelmSignals.tsx:81-84), and FairwayPlayerInsight ignores the page's raw SSR rows and re-fetches via getInsightsForCoach(coachId, { player_id, limit: 4 }) (FairwayPlayerInsight.tsx:399).
- [readpath] Defensive row mapping: mapRowToEvidenceInsight (insight-delivery.ts:1004-1041) rejects rows missing numeric strokes_impact/confidence or string metric before they reach any card, and both mappers sanitize authoring artifacts out of content via sanitizeProse.
- [readpath] Per-player generation state is consistent with the regeneration claim: 18 of 20 players hold visible v3 rows (223 total), matching the 18/20-players / 2-cold-start expectation.
- [stats] SG additivity is airtight: across all 173 completed rounds with SG, max |strokes_gained_total − (tee+approach+around_green+putting)| = 0.01 and 0 rounds exceed 0.05; cache rows match too (max per-round diff 0.01, max aggregate diff 0.03 across 20 players).
- [stats] SG propagation cache==rounds is exact: every cache sg_*_per_round equals AVG over that player's golf_rounds to 2dp (e.g. 8e382583: rounds avg −6.603 vs cache −6.60; aggregate strokes_gained_total −85.84 = 13 × −6.603), and rounds_played matches the completed+scored round count.
- [stats] Putt-band make-% AND attempts in cache match an independent SQL recount from golf_shots exactly, band-for-band, for both sampled players: b40a650a (477 putts: 96.6/46.5/28.0/15.6/12.2/0.0 %, attempts 43/82/64/49/31) and 32bd954c (412 putts: 97.6/70.6/40.0/17.1/4.9/0.0 %, attempts 34/70/41/41/59).
- [stats] Penalty pipeline (cache path) is canonical and exact: golf_player_stats_cache.total_penalties == SUM(golf_holes.penalty_strokes) for 20/20 players, and penalty_strokes_per_round correctly uses 18-hole-equivalent denominators (9-hole round = 0.5: 16/12.5 = 1.28 for 32bd954c; 11/14.5 = 0.76 for 49ffe06d).
- [stats] Approach proximity shows no feet/yards blend: 5,620 of 5,621 green-result shots carry distance_unit_after='feet' (1 yards row), max stored finish distance 80 ft, and all 20 cache approach_proximity_average values fall in 13.8–26.2 ft — plausible GIR-only proximity per the documented engine semantics (regulation-attempt shot only, ×3 only on explicit 'yards').
- [stats] Shot coverage is complete: every one of the 167 completed 18-hole rounds with SG has shot-level rows on all 18 holes (no partial-coverage rounds feeding the engines).
- [stats] Sample-size honesty shipped end-to-end: the new putt_attempts_* columns are populated 20/20 and the v3 putt-distance generator gates on them (minSampleN=5/ATTEMPT_FLOOR at src/lib/coachhelm/v3/generators/putt-distance.ts:128,163-176), so a 0% band can no longer ship without its n.
- [stats] The putt-band writer's documented band edges ('<=' upper, [0,120] ft clamp) are implemented exactly as specified in migration 20260609090000 and agree between make-% and attempts within the same CTE, eliminating numerator/denominator drift by construction.
- [lifecycle] Sweep precision verified 23/23 (not just the requested 5 spot-checks): every row archived in the 23:38 run had acknowledged_at=null, addressed_at=null, status='active', dismissed=false (SQL coach_touched count = 0), and every signature falls inside the 9 declared generator scopes — putt_bias 14, warmup_hole 5, approach_miss 3, tee_strategy 1. Zero false retractions.
- [lifecycle] Every archived signature is genuinely no-longer-emitted: 15/23 superseded — a FRESH active same-scope row with a different signature written in the same run (14× putt_bias:left/right → putt_bias:balanced; tee_strategy:inconclusive → tee_strategy:sharp, all fresh_updated 23:38) — and 8/23 dequalified with no fresh same-scope row at all (5× warmup_hole:hole_1; 3× approach_miss buckets for player dbc58bba…).
- [lifecycle] The archived+active combination is handled correctly by the primary product surfaces: every list query in insight-delivery.ts filters .in('lifecycle_state', VISIBLE_LIFECYCLE_STATES) from the shared module (lines 225, 248, 341, 490, 518, 640, 807), the Alert Center list goes through the same path (CoachAlertCenter.tsx:64), the coach morning digest filters lifecycle + neq status dismissed (coach-morning-digest/route.ts:317-330), and the causality cron imports the same insight-visibility.ts module — the learning loop cannot train on archived rows.
- [lifecycle] Archived-row resurrection is implemented in BOTH updateExisting branches (v2/insights/upsert.ts:213-217 refresh branch and the movement branch): archived → detected, archived_at cleared, metadata.redetected_at stamped — and status is deliberately untouched so a coach dismissal survives resurrection. A dequalify→requalify flap therefore cannot strand fresh evidence on an invisible row.
- [lifecycle] DI-1 (06-06 audit, 11 frozen rows) is closed: the dedup lookup no longer has the 30-day created_at cutoff (upsert.ts:122-128 documents and implements the fix), and prod has ZERO rows >30d old in any visible lifecycle state — all 50 over-30d rows are terminal v2 (28 archived/dismissed, 22 resolved/acknowledged, last touched 05-24/05-29). The previously stuck rows were retired rather than refreshed (0 over-30d rows refreshed in the last day — expected, since no >30d-old signature is still emitted), and no row in the table currently exhibits the freeze.
- [lifecycle] Sweep design safety holds up in code: soft-archive only (never DELETE), keepSignature exclusion, coach-touch guards duplicated in the SQL predicate (status='active', acknowledged_at/addressed_at IS NULL), failures logged and swallowed so a sweep error can never fail the generator run that produced the fresh insight (generator-base.ts:252-289), and the LIKE-wildcard and scope non-overlap constraints are documented at the override point.
- [lifecycle] The global lifecycle/status matrix contains no nonsensical combinations beyond the by-design archived+active (64) and archived+dismissed (28): no archived+acknowledged, no tentative rows in coach-visible states, matured/resolved counts consistent with the engine's maturation rules.
- [causality] Cron candidate query applies the shared visibility module exactly as specified: route.ts:18-21 imports V3_ENGINE_FILTER + VISIBLE_LIFECYCLE_STATES from src/lib/coachhelm/v3/insight-visibility.ts, and route.ts:94-97 chains .not('player_id','is',null), .or(V3_ENGINE_FILTER), .in('lifecycle_state',[...VISIBLE_LIFECYCLE_STATES]), .neq('status','dismissed'). The delivery read path uses the identical shared constants at 7 call sites (src/app/golf/actions/insight-delivery.ts:47-48, 224-226, 247-249, 340-342, 489-491, 517-519, 639-641, 806-807), so the loop can only train on product-visible rows.
- [causality] Visibility filter is doing real work in prod: of v3-tagged rows, 46 archived + 35 tentative are excluded; 223 rows are visible; status is 'active' for all 304 v3 rows (no NULL status/lifecycle/player_id to slip through the NULL-unsafe .neq).
- [causality] Both learning tables verified empty by SQL: golf_insight_outcome_attribution = 0 rows, golf_coachhelm_coach_weights = 0 rows — clean zero baseline as intended.
- [causality] First eligibility computed from prod: min(created_at) over visible v3 insights = 2026-05-26 15:10:21Z, so first candidates clear the 21-day gate at 2026-06-16 15:10:21Z. First 3 eligible days: 2026-06-16 = 10 rows, 2026-06-17 = 119 rows, 2026-06-18 = 2 rows (131 cumulative; next tranche 11 on 06-19).
- [causality] Zero malformed-bucket candidates: all 223 visible v3 insights have evidence->>'metric' non-null (0 violations), player_id non-null, created_at non-null.
- [causality] Zero unknown-metric drift: the 20 distinct evidence.metric values among visible v3 rows all resolve via lookupMetricSource — 18 are canonical METRIC_IDS entries in METRIC_SOURCE (metric-sources.ts:157-222), and three_putt_chain (5 rows) + short_side_proximity (4 rows) resolve through METRIC_SOURCE_ALIASES (metric-sources.ts:284-286). The 'unknown_metric' bucket will be 0 on current data.
- [causality] nextWeight is the magnitude-aware EMA as specified, not binary: attribute.ts:392-404 — target = 1 + tanh(lift / LIFT_TANH_SCALE=1.0), alpha = 1/(sample_n+1), hard clamp Math.max(0.25, Math.min(2.0, next)), null/non-finite lift no-ops; numeric-as-string from Postgres handled via Number(prev.weight) at route.ts:250.
- [causality] Every DB column referenced by the four live source kinds exists in prod (verified via information_schema): golf_rounds score_to_par/strokes_gained_*/total_penalties; golf_round_stats_cache greens/sand/scramble/fairway ratio columns + all six scoring-distribution columns + player_id/round_id; golf_holes score/par/round_id.
- [causality] Cron is actually scheduled (vercel.json:74-75, daily 06:00 UTC) and auth-gated via requireCronAuth on both GET and POST (route.ts:57-58, 62-63).
- [causality] Robust window math details: hole_level_avg paginates past the PostgREST 1000-row cap via fetchAllRowsResult (attribute.ts:209-218); ratio sources aggregate shot-weighted sum-of-num/sum-of-den and skip zero denominators (attribute.ts:140-151); ambient counterfactual window is strictly outside [preStart, postEnd] and the MIN_AMBIENT_ROUNDS=2 gate leaves lift null (weights untouched) on thin samples (attribute.ts:335-354).
- [causality] The ~30-min-ago full regeneration did NOT reset created_at on existing insights (min visible created_at is still 2026-05-26), so the 21-day eligibility clock was not restarted by the redeploy.
- [security] All 8 named maintenance/SECURITY DEFINER functions are service_role-EXECUTE only — anon=false AND authenticated=false for refresh_player_standing(uuid[]), refresh_player_stats_cache(uuid), mark_player_stats_stale(uuid), prune_stale_player_standing(uuid[],timestamptz), select_stalest_teams(integer), recompute_team_sg(uuid), update_player_putt_make_pct(uuid), update_player_distance_proximity(uuid) (plus the two refresh_player_standing_*_metrics helpers). The prior anon-EXECUTE-on-standing-RPCs regression noted in the 2026-06-06 audit is NOT present in current prod.
- [security] RLS is enabled (relrowsecurity=true) on all 7 audited tables (golf_coach_insights, golf_rounds, golf_shots, golf_holes, golf_player_stats_cache, golf_insight_outcome_attribution, golf_coachhelm_coach_weights) and on every one of the 84 golf_% tables checked.
- [security] Learning-table integrity is protected: golf_insight_outcome_attribution and golf_coachhelm_coach_weights each have exactly 1 RLS policy — a coach-scoped SELECT (attribution_coach_read gates via the insight's coach_id=current_coach_id(); coach_weights_coach_only gates coach_id=current_coach_id()) — and ZERO INSERT/UPDATE/DELETE policies. With RLS enabled and authenticated/anon not bypassing RLS (rolbypassrls=false), all writes by authenticated/anon are default-denied; only service_role (rolbypassrls=true), used by the engine, can write. The wipe-to-zero baseline cannot be tampered with by a logged-in coach.
- [security] golf_rounds row-scoping is intact across all three UPDATE policies, so the wider column grant remains row-safe: golf_rounds_update (player owns via golf_players.user_id=auth.uid()), golf_rounds_update_coach (team_id IS NOT NULL AND is_golf_team_coach(team_id)), golf_rounds_update_team (player_id IN coach's active roster, with matching WITH CHECK). No UPDATE policy is unscoped/global.
- [security] No anon data-exposure path on golf data: zero golf_% RLS policies include the anon role, and get_admin_platform_stat_averages (platform-wide stat rollup) is NOT anon-executable (anon_exec=false) and is gated by __admin_rollup_b_gate, which RAISEs 'Forbidden' (42501) unless caller is service_role or users.role='admin'.
- [newcode] Scope/signature isolation verified end-to-end against prod: a SQL cross-join of all 19 instance LIKE patterns (including '_' wildcard semantics) against every live 'v3:%' signature shows each signature matched ONLY by its owning generator's scope; no composite signature is reachable by any generator scope; the only share is the intentional putt_bias pair. The generator-base.ts:253-256 'no two scopes differ only at an underscore position' claim holds on live data.
- [newcode] Constructor-state scope contract honored: putt-distance (bucket: this.bucket, putt-distance.ts:178) and approach-miss (bucket: this.bucket, approach-miss.ts ~201) derive the emitted signature from the same constructor value as signatureScope(), so a run's keepSignature always falls inside its own scope; warmup/par-type/course-mgmt/scrambling/pressure-gap scopes are constants matching their fixed signatures.
- [newcode] Retraction is conservatively scoped: soft-archive only (never DELETE), restricted to tentative/detected, excludes acknowledged_at/addressed_at coach-touched rows and non-active status, swallows its own failures without failing the generator run, and deliberately skips the standing-lag exit (preventing flap from standing-cron lag) — generator-base.ts:257-291, 322-329.
- [newcode] Dedup integrity confirmed in prod: zero duplicate (player_id, signature) pairs across all v3 rows despite the concurrent putt_bias sibling writes — the UNIQUE NULLS NOT DISTINCT constraint + ignoreDuplicates + id-recovery path in insertNew (upsert.ts:359-393) is working.
- [newcode] The sweep demonstrably works in production: 23 v3 rows archived today, including the superseded legacy-format putt_bias:left/right directional rows (7+7, all archived) replaced by the new banded/balanced signatures — exactly the stale-sibling retirement the feature was built for.
- [newcode] Shared visibility boundary is genuinely shared: insight-delivery.ts and the causality cron import the identical V3_ENGINE_FILTER + VISIBLE_LIFECYCLE_STATES constants (insight-visibility.ts), the cron additionally excludes dismissed rows, enforces the 21-day age gate, and is idempotent via the attribution PK pre-filter (causality-attribute/route.ts:90-116).
- [newcode] Pagination is correctly built: fetchAllRowsResult uses stable unique-key ordering ('id' asc) with a bounded loop and short-page termination; every converted call site re-ranks in-app via rankEvidenceInsights (explicit score sort with created_at tie-break) and no downstream consumer was found relying on DB created_at ordering — dedupeBySubject/collapseParScoring operate on the ranked order by design.
- [newcode] Resurrection deliberately preserves status: a coach dismissal keeps hiding a resurrected row (upsert.ts:211-212), and the refresh branch does not fire notifications, so the flap in CR-1 at least cannot spam push/email.
- [newcode] The team-wide coach sweep retains the 800-row soft-ceiling observability warning rather than silently capping (insight-delivery.ts:540-548), and transient-fetch retry semantics were preserved through the pagination conversion.

## Addendum — Full remediation + final-mile, same night (2026-06-09 late)

All 40 confirmed findings actioned across PRs #253 + #254 (both deployed; prod
Ready). Independent 8-agent re-verification after #253: **grade 93** — 43
closures verified, 2 honest wontfixes (Supabase default-ACL posture, seeded
demo clones), 0 still open. The rescore named the remaining 2 points
explicitly; #254 closed them the same night, live-verified:

- `pga_omitted` persisted end-to-end (10 women's rows carry it; StandingBar
  suppression now fires)
- both sibling standing RPCs gender-scoped on prod (approach bands +
  opening-hole no longer pool genders; women's cohort honestly gated at
  MIN_COHORT_N until enough data)
- staleness disclosure on every generator family (105 live cards now say
  "Data through ..."); 0 women's Tour-anchor prose; 0 below-gate highs
- composite synthesis input restricted to product-visible v3; dual-axis
  lifecycle/status contract documented; dead AlertBadge deleted

**Standing after the final mile, by the rescore's own arithmetic: 95 — the
rubric's definition of trustworthy.** The only evidence that remains is
time-gated and cannot be coded: the organic causality cron writes
(~2026-06-16; the manual dry run already passed all six gate checks on real
prod data) and real-world calibration accumulation (>= 30 real players x >=
10 real rounds) before cohort-primary counterfactuals can be considered
proven at scale. Gates on the final tree: tsc clean / vitest 6,037-0 /
RLS 2,016-0 / build clean / check:stats 0 divergent.
