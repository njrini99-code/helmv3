# Lane A09 — Attention, non-messaging notifications, badges

> Captured by A00 from A09's inline return; `report.md` refused by the subagent-scoped
> restriction. `lanes/A09/findings.jsonl` (9 records) is canonical.

## Findings

| ID | Sev | Label | One-line |
|---|---|---|---|
| A09-001 | P1 | SOURCE | More/Team badge sums heterogeneous counts into one opaque integer — G22/UX31 still present. |
| A09-002 | P2 | SOURCE | Coach Operations→Tasks badge wired to `badges.tasks`, hardcoded 0 for coaches — permanently dead. |
| A09-003 | P1 | SOURCE | `notificationsUnread`/`coachhelm` fabricate `0` on read failure, unlike the other five counts in the same provider (`notification-badge-context.tsx:160-196,164-174`). |
| A09-004 | P1 | SOURCE | Announcements badge for non-ack items is 24h time-decay only (`player-notifications.ts:212-222`); the route never calls `markAnnouncementsSeen`/`refetch` — only the login modal does. |
| A09-005 | P1 | SOURCE | Foreground push = native banner+sound (`capacitor.config.ts:69`) AND in-app toast+haptic (`push-registration.ts:275-287`, `CapacitorProvider.tsx:38-46`) for one event. |
| A09-006 | P1 | SOURCE | `notifyQualifierCreated` (`golf.ts:4220`) and `notifyDevPlanAssigned` (`development.ts:339`) are email-only; with the email gate off (recorded default) there is zero in-app or badge trace. |
| A09-007 | P1 | SOURCE | Push tap (`push-registration.ts:289-312`) never calls `markNotificationRead`; only a bell-panel click does. |
| A09-008 | P3 | SOURCE | Badge cap disagrees inside one file: item badges "99+", aggregate More badge "9+". |
| A09-009 | P3 | SOURCE | Background bell-refresh failure is silently swallowed while existing items are shown. |

**Verified, not a finding:** the `HELM_CUSTOMER_EMAIL_ENABLED` gate is intact and funnels every
customer email through one choke point (`src/lib/notifications/email.ts:957-964`). No drift;
auth/ops/CRM paths untouched.

## Attention matrix

| Event | Source | Channels today | Destination | Clearing action | Verdict |
|---|---|---|---|---|---|
| Unread message | `golf_conversation_participants.last_read_at` vs `golf_messages` | badge | Messages rail, More | opening conversation | Messaging referral |
| Announcement (ack-required) | `golf_announcements` + acks | badge, login modal | Team badge (summed) | `acknowledgeAnnouncement` → refetch | correct |
| Announcement (not ack-required) | `published_at` age < 24h | badge, login modal | Team badge (summed) | **nothing — time decay** | A09-004 |
| Pending task (player) | `golf_task_assignments.status='pending'` | badge | Team badge; Operations (coach, dead) | `completeTask` → refetch | correct for players; dead for coaches (A09-002) |
| Unseen travel | `golf_travel_itineraries` vs `last_travel_seen_at` | badge | Team badge | on-mount `markTravelSeen()` | correct |
| Unread calendar/RSVP | `golf_calendar_notifications.read_at` | badge, bell | Calendar rail, bell | bell click only — never push tap, never visiting Calendar | partial (A09-007) |
| Generic `notifications` row | `notifications.read_at` | badge, bell | bell | bell click / mark-all-read | same push-tap gap; fail-to-zero (A09-003) |
| CoachHelm critical signal | `getAlertCounts` | badge | CoachHelm rail | resolving signal | fail-to-zero (A09-003) |
| Qualifier created | none | email (suppressed) | none | n/a | **blackout** (A09-006) |
| Dev plan assigned | none | email (suppressed) | none | n/a | **blackout** (A09-006) |
| Task assigned (new) | pendingTasks indirect | email (suppressed) | Team/Operations badge, indirect | completing task | assignment itself never presented |
| Push (any) | APNs/FCM | native alert+sound always, + toast + haptic foregrounded | n/a | n/a | A09-005 |

## Count dictionary

| Count | Exact set | Computed at | Cleared by |
|---|---|---|---|
| Team badge (player) | announcements + tasks + travel | `nav-registry.ts:433` | three independent clears — A09-001 |
| Calendar badge | `calendarNotifications` | `nav-registry.ts:367,421,482`; `player-notifications.ts:300` / `coach-notifications.ts:83,129` | bell click only — A09-007 |
| Messages badge | unread per conversation, summed | `nav-registry.ts:380,439` | opening conversation (Messaging) |
| CoachHelm AI badge | `getAlertCounts().critical` | `nav-registry.ts:353,473`; `notification-badge-context.tsx:160-175` | resolving signal; fails to 0 — A09-003 |
| Operations badge (coach) | `badges.tasks`, hardcoded 0 | `nav-registry.ts:388` | never moves — A09-002 |
| "More" aggregate | sum of every hidden item's badge | `more-nav.ts:81-90` | no own semantics |
| Bell icon | `notificationsUnread + calendarNotifications` | `NotificationBell.tsx:48` | mark-read; not push tap (A09-007); fails to 0 (A09-003) |
| Bell filter chips | `countByCategory(items)` over the fetched page only | `unified-notifications-model.ts:269-276` | recomputed, not persistent |
| `NotificationBadges.total` | sum of 5 fields | `notification-badge-context.tsx:256` | **dead — no live consumer** |

## Delivery test harness spec
1. Badge failure semantics — mock count reads to fail; assert hold-previous (fails today, A09-003).
2. Announcement read-clear — mount the route, assert mark-seen fires (fails, A09-004).
3. Push-tap read-clear — simulate action-performed, assert `markNotificationRead` (fails, A09-007).
4. Foreground double-presentation — simulate receipt while visible, assert ONE presentation (both fire today; needs product decision).
5. Coach Operations badge — assert nonzero under some fixture, or assert provably always-dead (today: dead).
6. Qualifier/dev-plan blackout — assert a `notifications` row exists after those calls with email off (fails, A09-006).
7. Badge cap parity — same count renders identically on item vs. aggregate badge (fails: "15" vs "9+").

## Repair packets (proposed, unauthorized)
RP-A09-1 (A09-003/002) badge-context hold-previous + coach-badge product decision ·
RP-A09-2 (A09-004) Announcements mark-seen-on-visit, mirroring Travel ·
RP-A09-3 (A09-007) push tap calls `markNotificationRead` ·
RP-A09-4 (A09-006) qualifier/dev-plan write a `notifications` row independent of the email gate ·
RP-A09-5 (A09-008) unify badge-cap formatting ·
RP-A09-6 (A09-005) needs product decision + A01 native coordination ·
RP-A09-7 (A09-001) needs product decision; a C10 change, not a narrow fix.

## Contracts requested
- **C10** — an explicit "exact set + clearing action" field per count, and a decision on whether
  composite badges decompose or split.
- **C06** — an explicit foreground-vs-background push presentation rule; today both fire unconditionally.
- **C05** — flagged for A01/A03 awareness re: toast timing vs. native banner. Not owned by A09.

## Shared changes requested
- `push-registration.ts`, `capacitor.config.ts`, `CapacitorProvider.tsx` — joint with **A01**.
- `FairwayBottomNav.tsx`, `MoreNavSheet.tsx`, `FairwaySidebar.tsx` — **A03** to unify badge-count
  formatting under one helper.

## Open questions for the owner
1. Should the player "Team" badge decompose or split rather than summing three unrelated event types?
2. Is there an intended coach-facing signal for Operations→Tasks, or should the badge be removed for coaches?
3. Foregrounded push: native banner, in-app toast, or both — which wins?
4. Is the qualifier/dev-plan blackout while email is off a known accepted consequence of the
   2026-09-06 email decision, or an unnoticed gap?

## Messaging referrals
- `badges.messages` feeds the same rail/More machinery audited here (see A09-001); message unread
  computation and its clearing action remain Messaging's semantics.
- The unified bell categorizes a `'messages'` type from the generic `notifications` table — confirms
  message-originated rows appear in this bell; the write-side fanout is Messaging's, not traced.
- No mute/read-rule or conversation-notification policy inspected or proposed.

## NOT_RUN ledger
| Check | Missing dependency | Next owner |
|---|---|---|
| On-device foreground/background push presentation | physical iPhone / TestFlight | A12, after RP-A09-6 |
| Haptic quality/timing on push receipt | hardware | A01 |
| Removed-member cleanup of stale badge/notification rows | live DB/fixtures (read-path auth check traced, fails closed) | A10, with A08 |
| Account-switch badge staleness (coach multi-team) | multi-team fixture/runtime | A02; A09 consumes the contract |
| CoachHelm alert generation/dedupe internals | deeper domain read not performed | A06 |
| Push send-side badge accuracy (`send-apns-push`) | server function not read in depth | A01 |

## Coverage ledger
FINDING: `more-nav.ts`, `MoreNavSheet.tsx` (A09-001) · `FairwayBottomNav.tsx` (A09-008) ·
`nav-registry.ts` (A09-001, A09-002) · `notification-badge-context.tsx` (A09-003; PASS for the other
five counts' hold-previous pattern) · `player-notifications.ts` and `dashboard/announcements/page.tsx`
(A09-004) · `NotificationBell.tsx` (A09-007, A09-009; PASS on item-click read-marking) ·
`notifications/index.ts`, `golf.ts`, `development.ts`, `tasks.ts` (A09-006) · `push-registration.ts`
(A09-005, A09-007; PASS on URL-safety guard) · `capacitor.config.ts`, `CapacitorProvider.tsx` (A09-005).

PASS: `FairwaySidebar.tsx`, `coach-notifications.ts`, `NewAnnouncementsModalWrapper.tsx`,
`FairwayTravel.tsx`, `FairwayTeamHub.tsx`, `hub-parts.tsx`, `NotificationFeedPanel.tsx`,
`unified-notifications.ts`, `unified-notifications-model.ts`, `alerts.ts` (signature + auth-expired),
`outbound-gate.ts`, `notifications/email.ts` (gate call site), `task-reminders.ts` (correct
dual-channel pattern), `push-notifications.ts`.

OUT_OF_SCOPE: `FairwayCalendar.tsx` → A07 (confirmed to feed A09-007) · Messaging fanout/read rules →
Messaging · CoachHelm alert internals → A06.

**Dead:** `NotificationBadges.total` (`notification-badge-context.tsx:256`) — computed every render,
no live UI consumer. Dead code, not a defect.
