# INC-2026-08-02-01 — New Round: 9-hole selection silently overridden to 18 holes; duplicate/malformed course library records — filed, never fixed, bulk-closed without a linked PR

- **id:** INC-2026-08-02-01
- **feature_id:** round_tracking (registry key: `golf_round_lifecycle`; primary code: new-round setup flow, course library selection)
- **first_seen:** 2026-08-02T11:25:11Z (issue #1226 `createdAt`, `gh issue view 1226 --json createdAt`)
- **resolved: unresolved as of 2026-08-21** (this record's authoring date). Closed 2026-08-17T01:47:08Z with `stateReason: COMPLETED`, `closedByPullRequestsReferences: []`; only comment on the issue is CodeRabbit's automated "related PRs" listing (naming historically-merged, unrelated-in-number PRs: #285, #286, #290, #292, #339) — no human comment, no linked fix commit found referencing `#1226` as of 2026-08-21.
- **severity:** MEDIUM-HIGH (a player's explicit hole-count choice being silently overridden is a direct correctness failure in round setup, with knock-on effects for any 9-hole-scoped stat or qualifier comparison)
- **classification:** FUNCTIONAL_FAILURE (primary — explicit user selection silently ignored) — **status of the underlying defect is unknown**, recorded as a thin-evidence finding per this mining pass's mandate
- **status:** **UNKNOWN as of 2026-08-21 (this record's authoring date) / likely still open.** No verification evidence exists in available records either way.

## Summary

Issue #1226 reported two related defects in the new-round setup flow:

1. **9-hole selection is silently overridden to 18 holes.** A player
   choosing to record a 9-hole round had that choice discarded, with the
   round instead set up for 18 holes — no error, no confirmation dialog
   about the override, just a different outcome than what was selected.
2. **Duplicate/malformed course records in the course library.** The same
   underlying course-library data-quality issue this window's #916/#1230-
   adjacent findings also touched (course entities without a canonical,
   de-duplicated identity).

## Timeline

| date (UTC) | event |
|---|---|
| 2026-08-02T11:25:11Z | Issue #1226 filed (`gh issue view 1226 --json createdAt`) |
| 2026-08-02 – 2026-08-17, no dated activity found | No comment or commit activity found in available records |
| 2026-08-17T01:47:08Z | Closed, `stateReason: COMPLETED`, `closedByPullRequestsReferences: []`, no linked PR |

## Root cause

**Not established in available records.** The issue body (not fully
retrieved by this miner beyond title and the closing-state metadata) would
need to be read in full to state a mechanism; no commit, PR, or human
comment narrows it further. This is recorded as an open finding, not a
diagnosed-and-unfixed one — unlike INC-2026-07-26-01 (shot_tracking) or
INC-2026-08-07-01 (stats_analytics), which at least reached a documented
root cause before stalling, this issue shows no evidence of ever being
investigated past the initial report.

## User impact

- As filed: a player attempting to set up a 9-hole round would end up with
  an 18-hole round instead, with no warning — the round's structure would
  not match what was actually played. Any downstream logic that scopes
  stats, pace-of-play, or qualifier comparisons by hole count would be
  working from an incorrect round shape from the moment of creation.
- Severity and current status both unconfirmed; this record exists to flag
  the gap in verification, not to assert the defect is definitely still
  present.

## Repair

**None found, checked as of 2026-08-21** (this record's authoring date and
the date `closedByPullRequestsReferences` was re-checked for this issue).
No commit references `#1226` in available git history as of that date. No
PR's `closedByPullRequestsReferences` links it (re-confirmed empty on a
same-day re-check, unlike the closely-related qualifiers finding
INC-2026-08-03-01 where a re-check corrected an earlier miss). The only
comment is CodeRabbit's automated related-PR listing, whose named PRs
(#285, #286, #290, #292 — all course-library/new-round-flow features merged
well before this issue was filed, plus #339 which is INC-2026-06-22-01, an
unrelated stats-timeout fix) predate #1226's 2026-08-02T11:25:11Z filing and
cannot be its fix.

## Verification evidence

**None found as of 2026-08-21** (this record's authoring date and the date
this check was run). This is the thinnest-evidence incident in this mining
pass — a real-sounding, plausible, MEDIUM-HIGH severity defect with no
investigation trail and no fix trail. Recorded per this pass's explicit
instruction to be honest about thin evidence rather than omit or fabricate
resolution.

## Signals

- Issue #1226, "New Round: 9-hole selection is silently overridden to 18
  holes; duplicate/malformed course records in the library," filed
  2026-08-02.

## Recurrence risk

Unknown — cannot assess recurrence risk for a defect with no confirmed fix,
since "recurrence" presupposes a prior resolution. Treat as a standing,
unverified risk until directly checked against current `main`.

## Invariant candidate

A behavioral, not purely SQL, check — hole-count fidelity is a UI/action-
layer property, not something visible from `golf_rounds` alone unless
`expected_holes` (seen in the admin tracer types elsewhere in this codebase)
disagrees with what was actually selected at creation time, which isn't
independently recorded:

```
INV-ROUND-005 (needs a UI/E2E check, not pure SQL): a new-round setup flow
where the player selects "9 holes" should produce a round with
expected_holes=9, not 18. This requires either an E2E assertion on the
setup flow itself, or — if the selection is logged anywhere at creation
time — a comparison between the logged selection and the resulting
golf_rounds.expected_holes value.
```

Severity: MEDIUM-HIGH if reproducible; unconfirmed.

## Replay candidate

Premature. No root cause is established, so there is nothing yet to encode
into a fixture. The correct next step is reproduction against current
`main` (does selecting 9 holes in the new-round flow still produce an
18-hole round?) before any replay or fix work is scoped.

## Status

**UNKNOWN as of 2026-08-21** (this record's authoring date). Filed
2026-08-02T11:25:11Z, never investigated further in available records,
closed 2026-08-17T01:47:08Z via the same bulk-closure pattern as
INC-2026-08-07-01 (stats_analytics), with no linked fix. Recommend this be
reproduced against current `main` before being treated as either resolved
or confirmed still broken.
