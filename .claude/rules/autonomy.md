# Execution

Follow AGENTS.md for authorization, workspace ownership, and completion.
Do the requested work end to end, report relevant progress, and state any
unverified outcome. Configuration repairs are ordinary authorized edits.

`guard-git.mjs` and `guard-sql.mjs` cover specific destructive command
shapes; they are not parsers or complete security boundaries. The generated
`docs/CONTROL_PLANE_ENFORCEMENT.md` reports actual hook wiring.
