#!/usr/bin/env node

/**
 * HelmDev CLI Entry Point
 * 
 * Usage:
 *   helmdev [appDir]              Start in supervised mode
 *   helmdev --autonomous          Start in autonomous mode
 *   helmdev --megathink           Run MegaThink only
 *   helmdev --analyze             Run analysis only
 */

import('../src/index.js');
