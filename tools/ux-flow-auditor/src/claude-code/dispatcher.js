/**
 * ClaudeCodeDispatcher - Sends tasks to Claude Code in Cursor
 * 
 * This creates comprehensive context files and task prompts
 * that Claude Code can use to fix issues autonomously.
 * 
 * The dispatcher:
 * - Writes context files to .helmdev/context/
 * - Creates a task prompt in .helmdev/tasks/
 * - Watches for completion signals
 * - Validates results
 */

import { promises as fs } from 'fs';
import path from 'path';
import chokidar from 'chokidar';

export class ClaudeCodeDispatcher {
  constructor(options) {
    this.projectRoot = options.projectRoot;
    this.helmdevDir = options.helmdevDir;
    this.graph = options.graph;
    this.patterns = options.patterns;
    this.memory = options.memory;
    
    this.tasksDir = path.join(this.helmdevDir, 'tasks');
    this.contextDir = path.join(this.helmdevDir, 'context');
    this.resultsDir = path.join(this.helmdevDir, 'results');
  }

  /**
   * Dispatch a task to Claude Code
   */
  async dispatch(task, context) {
    // Ensure directories exist
    await fs.mkdir(this.tasksDir, { recursive: true });
    await fs.mkdir(this.contextDir, { recursive: true });
    await fs.mkdir(this.resultsDir, { recursive: true });
    
    // 1. Write context files
    await this.writeContextFiles(context);
    
    // 2. Generate and write the master prompt
    const prompt = this.generateMasterPrompt(task, context);
    await fs.writeFile(
      path.join(this.tasksDir, 'current-task.md'),
      prompt
    );
    
    // 3. Write machine-readable task spec
    await fs.writeFile(
      path.join(this.tasksDir, 'current-task.json'),
      JSON.stringify(task, null, 2)
    );
    
    // 4. Create a trigger file for Claude Code
    await this.createTriggerFile(task);
    
    console.log(`
╔══════════════════════════════════════════════════════════════════════════════╗
║ 📤 TASK READY FOR CLAUDE CODE                                                ║
╠══════════════════════════════════════════════════════════════════════════════╣
║                                                                              ║
║ 📋 Task: ${task.title.slice(0, 60).padEnd(60)} ║
║                                                                              ║
║ Context files written to: .helmdev/context/                                  ║
║ Task prompt: .helmdev/tasks/current-task.md                                 ║
║                                                                              ║
║ 🔧 In Cursor with Claude Code, run:                                          ║
║                                                                              ║
║    Read .helmdev/tasks/current-task.md and complete the task                ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
`);
    
    return true;
  }

  /**
   * Write all context files
   */
  async writeContextFiles(context) {
    // 1. Task context
    await fs.writeFile(
      path.join(this.contextDir, 'task-context.md'),
      context.taskContextMd || this.generateTaskContext(context)
    );
    
    // 2. Related files
    if (context.file?.relatedFiles?.length) {
      await fs.writeFile(
        path.join(this.contextDir, 'related-files.md'),
        this.generateRelatedFilesContext(context.file)
      );
    }
    
    // 3. Patterns
    if (context.patterns?.length) {
      await fs.writeFile(
        path.join(this.contextDir, 'patterns.md'),
        this.generatePatternsContext(context.patterns)
      );
    }
    
    // 4. Memory context
    if (context.memory) {
      await fs.writeFile(
        path.join(this.contextDir, 'memory.md'),
        this.generateMemoryContext(context.memory)
      );
    }
    
    // 5. Style guide
    if (context.style) {
      await fs.writeFile(
        path.join(this.contextDir, 'style-guide.md'),
        this.generateStyleContext(context.style)
      );
    }
    
    // 6. Project overview
    await fs.writeFile(
      path.join(this.contextDir, 'project-overview.md'),
      this.generateProjectOverview(context.codebase)
    );
  }

  /**
   * Generate the master prompt for Claude Code
   */
  generateMasterPrompt(task, context) {
    const priorityEmoji = '🔴'.repeat(Math.min(Math.ceil(task.priority / 20), 5)) + 
                          '⚪'.repeat(5 - Math.min(Math.ceil(task.priority / 20), 5));
    
    return `# 🤖 HelmDev Autonomous Task

> **IMPORTANT**: This task was generated by HelmDev, an autonomous development system.
> Read all context files in \`.helmdev/context/\` before proceeding.

---

## Task: ${task.title}

**ID:** \`${task.id}\`
**Type:** ${task.type}
**Severity:** ${task.severity.toUpperCase()}
**Priority:** ${priorityEmoji} (${task.priority}/100)
**Estimated Time:** ${task.estimated_time}

---

## 📁 File Location

**File:** \`${task.file_path}\`
**Line:** ${task.line_number || 'N/A'}

---

## ❗ Problem Description

${task.description}

${task.problem_explanation ? `### Why This Matters

${task.problem_explanation}` : ''}

${task.user_impact ? `### User Impact

${task.user_impact}` : ''}

---

## ✅ Fix Instructions

${task.fix_steps?.map((step, i) => `${i + 1}. ${step}`).join('\n') || task.suggestion || 'See code suggestion below.'}

${task.fix_code ? `### Suggested Code

\`\`\`tsx
${task.fix_code}
\`\`\`` : ''}

---

## 📚 Context Files (READ THESE FIRST)

Before making changes, read these context files for full understanding:

| File | Purpose |
|------|---------|
| \`.helmdev/context/task-context.md\` | Full task context and file information |
| \`.helmdev/context/related-files.md\` | Content of related files |
| \`.helmdev/context/patterns.md\` | **CRITICAL** - Patterns to follow |
| \`.helmdev/context/memory.md\` | What has worked/failed before |
| \`.helmdev/context/style-guide.md\` | Code style preferences |
| \`.helmdev/context/project-overview.md\` | Project structure overview |

---

## 🎯 Acceptance Criteria

After completing this task, verify:

- [ ] The issue described above is fixed
- [ ] Code compiles without errors (\`npm run build\`)
- [ ] No new linting warnings
- [ ] Code follows patterns in \`patterns.md\`
- [ ] Code matches style in \`style-guide.md\`

---

## 📝 Completion Protocol

When you have completed this task:

1. Save all modified files
2. Verify the build passes: \`npm run build\`
3. Create a result file at \`.helmdev/results/${task.id}.json\`:

\`\`\`json
{
  "taskId": "${task.id}",
  "status": "completed",
  "filesModified": ["list/of/files.tsx"],
  "summary": "Brief description of changes made",
  "approach": "The approach used to fix the issue",
  "issues": [],
  "suggestions": []
}
\`\`\`

---

## 🚨 Important Guidelines

1. **READ CONTEXT FIRST** - The context files have critical information
2. **FOLLOW PATTERNS** - Match existing codebase patterns exactly
3. **CHECK MEMORY** - Learn from past successes and failures
4. **MATCH STYLE** - Follow the style guide preferences
5. **BE THOROUGH** - Don't leave partial implementations
6. **TEST YOUR WORK** - Verify the fix actually resolves the issue

---

## 🔗 Related Information

${context.file ? `
### File Purpose
${context.file.purpose || 'Unknown'}

### Dependencies
${context.file.dependencies?.slice(0, 5).map(d => `- \`${d}\``).join('\n') || 'None'}

### Dependents
${context.file.dependents?.slice(0, 5).map(d => `- \`${d}\``).join('\n') || 'None'}
` : ''}

---

*HelmDev is watching for completion. Save your work and create the result file when done.*
`;
  }

  /**
   * Generate task context markdown
   */
  generateTaskContext(context) {
    const { task, file, codebase } = context;
    
    return `# Task Context

## Current Task

**Type:** ${task.type}
**Severity:** ${task.severity}
**File:** \`${task.file_path}\`
**Line:** ${task.line_number || 'N/A'}

### Problem
${task.description}

### Fix Suggestion
${task.suggestion || 'See main task file for instructions.'}

## File Information

${file ? `
**Purpose:** ${file.purpose || 'Unknown'}
**Route:** ${file.route || 'Not a route'}
**Patterns Used:** ${file.patterns?.join(', ') || 'None detected'}
**Features:** ${file.features?.join(', ') || 'None detected'}
**Complexity Score:** ${file.complexity || 'Unknown'}

### File Content

\`\`\`tsx
${file.content || '// File content not available'}
\`\`\`
` : 'File information not available.'}

## Project Summary

${codebase ? `
- **Total Files:** ${codebase.totalFiles}
- **Total Routes:** ${codebase.totalRoutes}
- **Total Lines:** ${codebase.totalLines}

### Most Used Patterns
${codebase.patterns?.slice(0, 5).map(p => `- ${p.name}: ${p.count} files`).join('\n') || 'None detected'}

### Most Used Features
${codebase.features?.slice(0, 5).map(f => `- ${f.name}: ${f.count} files`).join('\n') || 'None detected'}
` : 'Codebase summary not available.'}
`;
  }

  /**
   * Generate related files context
   */
  generateRelatedFilesContext(fileContext) {
    let md = `# Related Files

> These files are related to the task and may need to be referenced or modified.

`;
    
    if (fileContext.relatedFiles) {
      for (const file of fileContext.relatedFiles) {
        md += `
## \`${file.path}\`

**Relationship:** ${file.relationship}

\`\`\`tsx
${file.content?.slice(0, 3000) || '// Content not available'}
\`\`\`

---
`;
      }
    }
    
    return md;
  }

  /**
   * Generate patterns context
   */
  generatePatternsContext(patterns) {
    let md = `# Codebase Patterns

> **CRITICAL:** Follow these patterns exactly when making changes.
> Consistency is essential for maintainability.

`;
    
    for (const pattern of patterns) {
      md += `
## ${pattern.name}

**Category:** ${pattern.category}
**Used in:** ${pattern.usedIn?.length || 0} files

### Description
${pattern.description || 'No description available.'}

### Template
\`\`\`tsx
${pattern.template || '// No template available'}
\`\`\`

${pattern.example ? `
### Example from Codebase
\`\`\`tsx
${pattern.example}
\`\`\`
` : ''}

### When to Use
${pattern.whenToUse?.map(w => `- ${w}`).join('\n') || '- Follow existing usage patterns'}

### Common Mistakes
${pattern.mistakes?.map(m => `- ❌ ${m}`).join('\n') || '- No common mistakes documented'}

---
`;
    }
    
    return md;
  }

  /**
   * Generate memory context
   */
  generateMemoryContext(memory) {
    return `# Project Memory

> Historical context and lessons learned from past work.
> Use this to avoid repeating mistakes and leverage successful approaches.

## What Has Worked

${memory.successfulFixes?.length ? memory.successfulFixes.map(f => `
### ${f.type}
- **Approach:** ${f.approach}
- **Duration:** ${f.duration ? Math.round(f.duration / 1000) + 's' : 'Unknown'}
`).join('\n') : 'No successful fixes recorded yet.'}

## What Has Failed

${memory.failedFixes?.length ? memory.failedFixes.map(f => `
### ${f.type}
- **Attempted:** ${f.approach}
- **Why it failed:** ${f.reason}
`).join('\n') : 'No failures recorded.'}

## Key Insights

${memory.insights?.length ? memory.insights.map(i => `- ${i}`).join('\n') : 'No insights recorded yet.'}
`;
  }

  /**
   * Generate style guide context
   */
  generateStyleContext(style) {
    return `# Style Guide

> Match these style preferences when writing code.

## Code Style Preferences

| Setting | Value |
|---------|-------|
| Quotes | ${style.quotes || 'single'} |
| Semicolons | ${style.semicolons ? 'Yes' : 'No'} |
| Indentation | ${style.indentation || 'spaces'} |
| Indent Size | ${style.indentSize || 2} |
| Trailing Commas | ${style.trailingCommas || 'es5'} |

## Naming Conventions

- **Components:** PascalCase (e.g., \`MyComponent\`)
- **Files:** kebab-case (e.g., \`my-component.tsx\`)
- **Hooks:** camelCase with 'use' prefix (e.g., \`useMyHook\`)
- **Utilities:** camelCase (e.g., \`formatDate\`)
- **Types:** PascalCase (e.g., \`UserProfile\`)
- **Constants:** SCREAMING_SNAKE_CASE (e.g., \`MAX_ITEMS\`)

## Import Order

1. React/Next.js imports
2. Third-party libraries
3. Internal components (\`@/components/...\`)
4. Internal utilities (\`@/lib/...\`, \`@/utils/...\`)
5. Types (\`@/types/...\`)
6. Styles

## Component Structure

\`\`\`tsx
// 1. 'use client' directive (if needed)
'use client'

// 2. Imports (in order above)
import { useState } from 'react'

// 3. Types
interface Props {
  // ...
}

// 4. Component
export function ComponentName({ prop }: Props) {
  // 5. Hooks first
  const [state, setState] = useState()
  
  // 6. Derived state/memos
  
  // 7. Effects
  
  // 8. Handlers
  const handleClick = () => {}
  
  // 9. Render
  return (
    <div>
      {/* JSX */}
    </div>
  )
}
\`\`\`
`;
  }

  /**
   * Generate project overview
   */
  generateProjectOverview(codebase) {
    if (!codebase) {
      return `# Project Overview\n\nProject information not available.`;
    }
    
    return `# Project Overview

## Summary

- **Framework:** Next.js App Router
- **Language:** TypeScript
- **Total Files:** ${codebase.totalFiles}
- **Total Routes:** ${codebase.totalRoutes}
- **Total Lines:** ${codebase.totalLines}
- **Average Complexity:** ${codebase.avgComplexity?.toFixed(1) || 'N/A'}

## Project Structure

\`\`\`
src/
├── app/                    # Next.js App Router pages
│   ├── (dashboard)/       # Dashboard route group
│   ├── api/               # API routes
│   └── ...
├── components/            # Reusable components
│   ├── ui/               # shadcn/ui components
│   └── ...
├── lib/                   # Utility functions
└── types/                 # TypeScript types
\`\`\`

## Most Connected Files (Hotspots)

${codebase.hotspots?.slice(0, 5).map(h => `
- **\`${h.path}\`**
  - Imported by: ${h.importedBy} files
  - Imports: ${h.imports} files
  - Complexity: ${h.complexity}
`).join('\n') || 'No hotspots detected.'}

## Technology Stack

- **UI:** shadcn/ui + Tailwind CSS
- **Forms:** React Hook Form + Zod
- **State:** React hooks / Zustand
- **Data Fetching:** Server Components / SWR
- **Database:** Supabase
`;
  }

  /**
   * Create a trigger file for Claude Code
   */
  async createTriggerFile(task) {
    const triggerContent = `# HelmDev Task Ready

A new task is ready for Claude Code to complete.

## Quick Start

1. Read the task: \`.helmdev/tasks/current-task.md\`
2. Read context files in \`.helmdev/context/\`
3. Make the changes
4. Create result file when done

## Task Summary

**${task.title}**

File: \`${task.file_path}\`
Line: ${task.line_number || 'N/A'}

---

Open \`.helmdev/tasks/current-task.md\` for full instructions.
`;
    
    await fs.writeFile(
      path.join(this.projectRoot, '.helmdev-task'),
      triggerContent
    );
  }

  /**
   * Watch for task completion
   */
  watchForCompletion(taskId, timeout = 300000) {
    return new Promise((resolve, reject) => {
      const resultFile = path.join(this.resultsDir, `${taskId}.json`);
      
      // Set up file watcher
      const watcher = chokidar.watch(this.resultsDir, {
        ignoreInitial: true,
      });
      
      // Watch for result file creation
      watcher.on('add', async (filePath) => {
        if (filePath === resultFile) {
          try {
            const content = await fs.readFile(filePath, 'utf8');
            const result = JSON.parse(content);
            watcher.close();
            resolve(result);
          } catch (error) {
            // File exists but couldn't be parsed yet
          }
        }
      });
      
      // Set timeout
      setTimeout(() => {
        watcher.close();
        resolve({ status: 'timeout', taskId });
      }, timeout);
    });
  }
}
